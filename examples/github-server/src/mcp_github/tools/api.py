"""api tools for github."""

import inspect
import json
import sys
import importlib
import importlib.util
from pathlib import Path

from mcp.server.fastmcp import FastMCP

from mcp_github.backend import BackendError

# Instance cache for reusing class instances across tool calls
_instance_cache: dict[str, object] = {}


def _get_or_create_instance(mod, class_name: str, **init_kwargs):
    """Get a cached class instance or create a new one."""
    cache_key = f"{mod.__name__}.{class_name}"
    if init_kwargs:
        # When explicit init args are provided, always create fresh
        cls = getattr(mod, class_name)
        return cls(**init_kwargs)
    if cache_key not in _instance_cache:
        cls = getattr(mod, class_name)
        try:
            _instance_cache[cache_key] = cls()
        except TypeError:
            # __init__ requires arguments we don't have — try with empty defaults
            import inspect as _ins
            sig = _ins.signature(cls.__init__)
            kwargs = {}
            for name, param in sig.parameters.items():
                if name == "self":
                    continue
                if param.default is not _ins.Parameter.empty:
                    kwargs[name] = param.default
                elif param.annotation is str or param.annotation == "str":
                    kwargs[name] = ""
                elif param.annotation is int or param.annotation == "int":
                    kwargs[name] = 0
                elif param.annotation is bool or param.annotation == "bool":
                    kwargs[name] = False
                elif param.annotation is list or param.annotation == "list":
                    kwargs[name] = []
                elif param.annotation is dict or param.annotation == "dict":
                    kwargs[name] = {}
                else:
                    kwargs[name] = None
            _instance_cache[cache_key] = cls(**kwargs)
    return _instance_cache[cache_key]


def _setup_import_path(codebase_path: str):
    """Add codebase to sys.path for imports."""
    codebase = Path(codebase_path)
    for path in [str(codebase), str(codebase.parent)]:
        if path not in sys.path:
            sys.path.insert(0, path)


def _load_source_module(codebase_path: str, module_path: str):
    """Load a module from the source codebase, avoiding package name collisions.

    Strategy:
    1. If codebase is inside a proper Python package (has __init__.py in parent),
       use the package's canonical import path.
    2. Otherwise, use importlib.util.spec_from_file_location to load directly
       from the source file, bypassing sys.modules name conflicts.
    """
    codebase = Path(codebase_path)

    # Check if codebase is itself a Python package (e.g., /path/to/wand/)
    if (codebase / "__init__.py").exists():
        # This is a package — import using package.module path
        package_name = codebase.name
        parent_dir = str(codebase.parent)
        if parent_dir not in sys.path:
            sys.path.insert(0, parent_dir)
        full_module = f"{package_name}.{module_path}" if module_path != package_name else package_name
        try:
            return importlib.import_module(full_module)
        except ImportError:
            pass

    # For standalone files, load directly by file path to avoid name collisions
    parts = module_path.split(".")
    file_path = codebase / (parts[-1] + ".py") if len(parts) == 1 else codebase / "/".join(parts[:-1]) / (parts[-1] + ".py")
    if not file_path.exists():
        file_path = codebase / (module_path.replace(".", "/") + ".py")
    if not file_path.exists():
        # Final fallback: regular import
        return importlib.import_module(module_path)

    # Use a collision-safe name in sys.modules
    safe_name = f"_mcp_src_{codebase.name}_.{module_path}"
    if safe_name in sys.modules:
        return sys.modules[safe_name]

    spec = importlib.util.spec_from_file_location(safe_name, file_path,
        submodule_search_locations=[str(codebase)])
    if spec is None or spec.loader is None:
        return importlib.import_module(module_path)
    mod = importlib.util.module_from_spec(spec)
    sys.modules[safe_name] = mod
    spec.loader.exec_module(mod)
    return mod


def register_tools(server: FastMCP, backend) -> None:
    """Register api tools with the server."""

    @server.tool()
    async def meta_root(
    ) -> str:
        """GET / - GitHub API Root"""
        # HTTP GET /
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_list_global_advisories(
        ghsa_id: str | None = None,
        type: str | None = None,
        cve_id: str | None = None,
        ecosystem: str | None = None,
        severity: str | None = None,
        cwes: dict | None = None,
        is_withdrawn: bool | None = None,
        affects: dict | None = None,
        published: str | None = None,
        updated: str | None = None,
        modified: str | None = None,
        epss_percentage: str | None = None,
        epss_percentile: str | None = None,
        before: str | None = None,
        after: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        sort: str | None = None,
    ) -> str:
        """GET /advisories - List global security advisories"""
        # HTTP GET /advisories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if ghsa_id is not None:
            query_params["ghsa_id"] = str(ghsa_id)
        if type is not None:
            query_params["type"] = str(type)
        if cve_id is not None:
            query_params["cve_id"] = str(cve_id)
        if ecosystem is not None:
            query_params["ecosystem"] = str(ecosystem)
        if severity is not None:
            query_params["severity"] = str(severity)
        body = cwes if isinstance(cwes, dict) else {"data": cwes}
        if is_withdrawn is not None:
            query_params["is_withdrawn"] = str(is_withdrawn)
        body = affects if isinstance(affects, dict) else {"data": affects}
        if published is not None:
            query_params["published"] = str(published)
        if updated is not None:
            query_params["updated"] = str(updated)
        if modified is not None:
            query_params["modified"] = str(modified)
        if epss_percentage is not None:
            query_params["epss_percentage"] = str(epss_percentage)
        if epss_percentile is not None:
            query_params["epss_percentile"] = str(epss_percentile)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if sort is not None:
            query_params["sort"] = str(sort)
        try:
            return await backend.request(
                "GET",
                "/advisories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_get_global_advisory(
        ghsa_id: str,
    ) -> str:
        """GET /advisories/{ghsa_id} - Get a global security advisory"""
        # HTTP GET /advisories/{ghsa_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["ghsa_id"] = str(ghsa_id)
        try:
            return await backend.request(
                "GET",
                "/advisories/{ghsa_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_authenticated(
    ) -> str:
        """GET /app - Get the authenticated app"""
        # HTTP GET /app
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/app",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_create_from_manifest(
        code: str,
    ) -> str:
        """POST /app-manifests/{code}/conversions - Create a GitHub App from a manifest"""
        # HTTP POST /app-manifests/{code}/conversions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["code"] = str(code)
        try:
            return await backend.request(
                "POST",
                "/app-manifests/{code}/conversions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_update_webhook_config_for_app(
        url: str | None = None,
        content_type: str | None = None,
        secret: str | None = None,
        insecure_ssl: dict | None = None,
    ) -> str:
        """PATCH /app/hook/config - Update a webhook configuration for an app"""
        # HTTP PATCH /app/hook/config
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if url is not None:
            query_params["url"] = str(url)
        if content_type is not None:
            query_params["content_type"] = str(content_type)
        if secret is not None:
            query_params["secret"] = str(secret)
        body = insecure_ssl if isinstance(insecure_ssl, dict) else {"data": insecure_ssl}
        try:
            return await backend.request(
                "PATCH",
                "/app/hook/config",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_webhook_config_for_app(
    ) -> str:
        """GET /app/hook/config - Get a webhook configuration for an app"""
        # HTTP GET /app/hook/config
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/app/hook/config",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_webhook_deliveries(
        per_page: int | None = None,
        cursor: str | None = None,
    ) -> str:
        """GET /app/hook/deliveries - List deliveries for an app webhook"""
        # HTTP GET /app/hook/deliveries
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if cursor is not None:
            query_params["cursor"] = str(cursor)
        try:
            return await backend.request(
                "GET",
                "/app/hook/deliveries",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_webhook_delivery(
        delivery_id: int,
    ) -> str:
        """GET /app/hook/deliveries/{delivery_id} - Get a delivery for an app webhook"""
        # HTTP GET /app/hook/deliveries/{delivery_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["delivery_id"] = str(delivery_id)
        try:
            return await backend.request(
                "GET",
                "/app/hook/deliveries/{delivery_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_redeliver_webhook_delivery(
        delivery_id: int,
    ) -> str:
        """POST /app/hook/deliveries/{delivery_id}/attempts - Redeliver a delivery for an app webhook"""
        # HTTP POST /app/hook/deliveries/{delivery_id}/attempts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["delivery_id"] = str(delivery_id)
        try:
            return await backend.request(
                "POST",
                "/app/hook/deliveries/{delivery_id}/attempts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_app_installation_requests(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /app/installation-requests - List installation requests for the authenticated app"""
        # HTTP GET /app/installation-requests
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/app/installation-requests",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_installations(
        per_page: int | None = None,
        page: int | None = None,
        since: str | None = None,
        outdated: str | None = None,
    ) -> str:
        """GET /app/installations - List installations for the authenticated app"""
        # HTTP GET /app/installations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if since is not None:
            query_params["since"] = str(since)
        if outdated is not None:
            query_params["outdated"] = str(outdated)
        try:
            return await backend.request(
                "GET",
                "/app/installations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_delete_installation(
        installation_id: int,
    ) -> str:
        """DELETE /app/installations/{installation_id} - Delete an installation for the authenticated app"""
        # HTTP DELETE /app/installations/{installation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        try:
            return await backend.request(
                "DELETE",
                "/app/installations/{installation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_installation(
        installation_id: int,
    ) -> str:
        """GET /app/installations/{installation_id} - Get an installation for the authenticated app"""
        # HTTP GET /app/installations/{installation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        try:
            return await backend.request(
                "GET",
                "/app/installations/{installation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_create_installation_access_token(
        installation_id: int,
        repositories: list | None = None,
        repository_ids: list | None = None,
        permissions: dict | None = None,
    ) -> str:
        """POST /app/installations/{installation_id}/access_tokens - Create an installation access token for an app"""
        # HTTP POST /app/installations/{installation_id}/access_tokens
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        if repositories is not None:
            query_params["repositories"] = str(repositories)
        if repository_ids is not None:
            query_params["repository_ids"] = str(repository_ids)
        body = permissions if isinstance(permissions, dict) else {"data": permissions}
        try:
            return await backend.request(
                "POST",
                "/app/installations/{installation_id}/access_tokens",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_unsuspend_installation(
        installation_id: int,
    ) -> str:
        """DELETE /app/installations/{installation_id}/suspended - Unsuspend an app installation"""
        # HTTP DELETE /app/installations/{installation_id}/suspended
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        try:
            return await backend.request(
                "DELETE",
                "/app/installations/{installation_id}/suspended",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_suspend_installation(
        installation_id: int,
    ) -> str:
        """PUT /app/installations/{installation_id}/suspended - Suspend an app installation"""
        # HTTP PUT /app/installations/{installation_id}/suspended
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        try:
            return await backend.request(
                "PUT",
                "/app/installations/{installation_id}/suspended",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_delete_authorization(
        client_id: str,
        access_token: str,
    ) -> str:
        """DELETE /applications/{client_id}/grant - Delete an app authorization"""
        # HTTP DELETE /applications/{client_id}/grant
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["client_id"] = str(client_id)
        if access_token is not None:
            query_params["access_token"] = str(access_token)
        try:
            return await backend.request(
                "DELETE",
                "/applications/{client_id}/grant",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_check_token(
        client_id: str,
        access_token: str,
    ) -> str:
        """POST /applications/{client_id}/token - Check a token"""
        # HTTP POST /applications/{client_id}/token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["client_id"] = str(client_id)
        if access_token is not None:
            query_params["access_token"] = str(access_token)
        try:
            return await backend.request(
                "POST",
                "/applications/{client_id}/token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_reset_token(
        client_id: str,
        access_token: str,
    ) -> str:
        """PATCH /applications/{client_id}/token - Reset a token"""
        # HTTP PATCH /applications/{client_id}/token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["client_id"] = str(client_id)
        if access_token is not None:
            query_params["access_token"] = str(access_token)
        try:
            return await backend.request(
                "PATCH",
                "/applications/{client_id}/token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_delete_token(
        client_id: str,
        access_token: str,
    ) -> str:
        """DELETE /applications/{client_id}/token - Delete an app token"""
        # HTTP DELETE /applications/{client_id}/token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["client_id"] = str(client_id)
        if access_token is not None:
            query_params["access_token"] = str(access_token)
        try:
            return await backend.request(
                "DELETE",
                "/applications/{client_id}/token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_scope_token(
        client_id: str,
        access_token: str,
        target: str | None = None,
        target_id: int | None = None,
        repositories: list | None = None,
        repository_ids: list | None = None,
        permissions: dict | None = None,
    ) -> str:
        """POST /applications/{client_id}/token/scoped - Create a scoped access token"""
        # HTTP POST /applications/{client_id}/token/scoped
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["client_id"] = str(client_id)
        if access_token is not None:
            query_params["access_token"] = str(access_token)
        if target is not None:
            query_params["target"] = str(target)
        if target_id is not None:
            query_params["target_id"] = str(target_id)
        if repositories is not None:
            query_params["repositories"] = str(repositories)
        if repository_ids is not None:
            query_params["repository_ids"] = str(repository_ids)
        body = permissions if isinstance(permissions, dict) else {"data": permissions}
        try:
            return await backend.request(
                "POST",
                "/applications/{client_id}/token/scoped",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_by_slug(
        app_slug: str,
    ) -> str:
        """GET /apps/{app_slug} - Get an app"""
        # HTTP GET /apps/{app_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["app_slug"] = str(app_slug)
        try:
            return await backend.request(
                "GET",
                "/apps/{app_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def classroom_get_an_assignment(
        assignment_id: int,
    ) -> str:
        """GET /assignments/{assignment_id} - Get an assignment"""
        # HTTP GET /assignments/{assignment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["assignment_id"] = str(assignment_id)
        try:
            return await backend.request(
                "GET",
                "/assignments/{assignment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_assignments_by_assignment_id_accepted_assignments(
        assignment_id: int,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /assignments/{assignment_id}/accepted_assignments - List accepted assignments for an assignment"""
        # HTTP GET /assignments/{assignment_id}/accepted_assignments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["assignment_id"] = str(assignment_id)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/assignments/{assignment_id}/accepted_assignments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def classroom_get_assignment_grades(
        assignment_id: int,
    ) -> str:
        """GET /assignments/{assignment_id}/grades - Get assignment grades"""
        # HTTP GET /assignments/{assignment_id}/grades
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["assignment_id"] = str(assignment_id)
        try:
            return await backend.request(
                "GET",
                "/assignments/{assignment_id}/grades",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def classroom_list_classrooms(
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /classrooms - List classrooms"""
        # HTTP GET /classrooms
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/classrooms",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def classroom_get_a_classroom(
        classroom_id: int,
    ) -> str:
        """GET /classrooms/{classroom_id} - Get a classroom"""
        # HTTP GET /classrooms/{classroom_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["classroom_id"] = str(classroom_id)
        try:
            return await backend.request(
                "GET",
                "/classrooms/{classroom_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def classroom_list_assignments_for_a_classroom(
        classroom_id: int,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /classrooms/{classroom_id}/assignments - List assignments for a classroom"""
        # HTTP GET /classrooms/{classroom_id}/assignments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["classroom_id"] = str(classroom_id)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/classrooms/{classroom_id}/assignments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codes_of_conduct_get_all_codes_of_conduct(
    ) -> str:
        """GET /codes_of_conduct - Get all codes of conduct"""
        # HTTP GET /codes_of_conduct
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/codes_of_conduct",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codes_of_conduct_get_conduct_code(
        key: str,
    ) -> str:
        """GET /codes_of_conduct/{key} - Get a code of conduct"""
        # HTTP GET /codes_of_conduct/{key}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["key"] = str(key)
        try:
            return await backend.request(
                "GET",
                "/codes_of_conduct/{key}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def credentials_revoke(
        credentials: list,
    ) -> str:
        """POST /credentials/revoke - Revoke a list of credentials"""
        # HTTP POST /credentials/revoke
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if credentials is not None:
            query_params["credentials"] = str(credentials)
        try:
            return await backend.request(
                "POST",
                "/credentials/revoke",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def emojis_get(
    ) -> str:
        """GET /emojis - Get emojis"""
        # HTTP GET /emojis
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/emojis",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_enterprises_by_enterprise_actions_cache_retention_limit(
        enterprise: str,
        max_cache_retention_days: int | None = None,
    ) -> str:
        """PUT /enterprises/{enterprise}/actions/cache/retention-limit - Set GitHub Actions cache retention limit for an enterprise"""
        # HTTP PUT /enterprises/{enterprise}/actions/cache/retention-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if max_cache_retention_days is not None:
            query_params["max_cache_retention_days"] = str(max_cache_retention_days)
        try:
            return await backend.request(
                "PUT",
                "/enterprises/{enterprise}/actions/cache/retention-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_enterprises_by_enterprise_actions_cache_retention_limit(
        enterprise: str,
    ) -> str:
        """GET /enterprises/{enterprise}/actions/cache/retention-limit - Get GitHub Actions cache retention limit for an enterprise"""
        # HTTP GET /enterprises/{enterprise}/actions/cache/retention-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/actions/cache/retention-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_enterprises_by_enterprise_actions_cache_storage_limit(
        enterprise: str,
        max_cache_size_gb: int | None = None,
    ) -> str:
        """PUT /enterprises/{enterprise}/actions/cache/storage-limit - Set GitHub Actions cache storage limit for an enterprise"""
        # HTTP PUT /enterprises/{enterprise}/actions/cache/storage-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if max_cache_size_gb is not None:
            query_params["max_cache_size_gb"] = str(max_cache_size_gb)
        try:
            return await backend.request(
                "PUT",
                "/enterprises/{enterprise}/actions/cache/storage-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_enterprises_by_enterprise_actions_cache_storage_limit(
        enterprise: str,
    ) -> str:
        """GET /enterprises/{enterprise}/actions/cache/storage-limit - Get GitHub Actions cache storage limit for an enterprise"""
        # HTTP GET /enterprises/{enterprise}/actions/cache/storage-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/actions/cache/storage-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_enterprises_by_enterprise_actions_oidc_customization_properties_repo(
        enterprise: str,
        custom_property_name: str,
    ) -> str:
        """POST /enterprises/{enterprise}/actions/oidc/customization/properties/repo - Create an OIDC custom property inclusion for an enterprise"""
        # HTTP POST /enterprises/{enterprise}/actions/oidc/customization/properties/repo
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if custom_property_name is not None:
            query_params["custom_property_name"] = str(custom_property_name)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/actions/oidc/customization/properties/repo",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_enterprises_by_enterprise_actions_oidc_customization_properties_repo(
        enterprise: str,
    ) -> str:
        """GET /enterprises/{enterprise}/actions/oidc/customization/properties/repo - List OIDC custom property inclusions for an enterprise"""
        # HTTP GET /enterprises/{enterprise}/actions/oidc/customization/properties/repo
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/actions/oidc/customization/properties/repo",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_enterprises_by_enterprise_actions_oidc_customization_properties_repo_by_custom_property_name(
        enterprise: str,
        custom_property_name: str,
    ) -> str:
        """DELETE /enterprises/{enterprise}/actions/oidc/customization/properties/repo/{custom_property_name} - Delete an OIDC custom property inclusion for an enterprise"""
        # HTTP DELETE /enterprises/{enterprise}/actions/oidc/customization/properties/repo/{custom_property_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["custom_property_name"] = str(custom_property_name)
        try:
            return await backend.request(
                "DELETE",
                "/enterprises/{enterprise}/actions/oidc/customization/properties/repo/{custom_property_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_create_configuration_for_enterprise(
        enterprise: str,
        name: str,
        description: str,
        advanced_security: str | None = None,
        code_security: str | None = None,
        dependency_graph: str | None = None,
        dependency_graph_autosubmit_action: str | None = None,
        dependency_graph_autosubmit_action_options: dict | None = None,
        dependabot_alerts: str | None = None,
        dependabot_security_updates: str | None = None,
        code_scanning_options: dict | None = None,
        code_scanning_default_setup: str | None = None,
        code_scanning_default_setup_options: dict | None = None,
        code_scanning_delegated_alert_dismissal: str | None = None,
        secret_protection: str | None = None,
        secret_scanning: str | None = None,
        secret_scanning_push_protection: str | None = None,
        secret_scanning_validity_checks: str | None = None,
        secret_scanning_non_provider_patterns: str | None = None,
        secret_scanning_generic_secrets: str | None = None,
        secret_scanning_delegated_alert_dismissal: str | None = None,
        secret_scanning_extended_metadata: str | None = None,
        private_vulnerability_reporting: str | None = None,
        enforcement: str | None = None,
    ) -> str:
        """POST /enterprises/{enterprise}/code-security/configurations - Create a code security configuration for an enterprise"""
        # HTTP POST /enterprises/{enterprise}/code-security/configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if advanced_security is not None:
            query_params["advanced_security"] = str(advanced_security)
        if code_security is not None:
            query_params["code_security"] = str(code_security)
        if dependency_graph is not None:
            query_params["dependency_graph"] = str(dependency_graph)
        if dependency_graph_autosubmit_action is not None:
            query_params["dependency_graph_autosubmit_action"] = str(dependency_graph_autosubmit_action)
        body = dependency_graph_autosubmit_action_options if isinstance(dependency_graph_autosubmit_action_options, dict) else {"data": dependency_graph_autosubmit_action_options}
        if dependabot_alerts is not None:
            query_params["dependabot_alerts"] = str(dependabot_alerts)
        if dependabot_security_updates is not None:
            query_params["dependabot_security_updates"] = str(dependabot_security_updates)
        body = code_scanning_options if isinstance(code_scanning_options, dict) else {"data": code_scanning_options}
        if code_scanning_default_setup is not None:
            query_params["code_scanning_default_setup"] = str(code_scanning_default_setup)
        body = code_scanning_default_setup_options if isinstance(code_scanning_default_setup_options, dict) else {"data": code_scanning_default_setup_options}
        if code_scanning_delegated_alert_dismissal is not None:
            query_params["code_scanning_delegated_alert_dismissal"] = str(code_scanning_delegated_alert_dismissal)
        if secret_protection is not None:
            query_params["secret_protection"] = str(secret_protection)
        if secret_scanning is not None:
            query_params["secret_scanning"] = str(secret_scanning)
        if secret_scanning_push_protection is not None:
            query_params["secret_scanning_push_protection"] = str(secret_scanning_push_protection)
        if secret_scanning_validity_checks is not None:
            query_params["secret_scanning_validity_checks"] = str(secret_scanning_validity_checks)
        if secret_scanning_non_provider_patterns is not None:
            query_params["secret_scanning_non_provider_patterns"] = str(secret_scanning_non_provider_patterns)
        if secret_scanning_generic_secrets is not None:
            query_params["secret_scanning_generic_secrets"] = str(secret_scanning_generic_secrets)
        if secret_scanning_delegated_alert_dismissal is not None:
            query_params["secret_scanning_delegated_alert_dismissal"] = str(secret_scanning_delegated_alert_dismissal)
        if secret_scanning_extended_metadata is not None:
            query_params["secret_scanning_extended_metadata"] = str(secret_scanning_extended_metadata)
        if private_vulnerability_reporting is not None:
            query_params["private_vulnerability_reporting"] = str(private_vulnerability_reporting)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/code-security/configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_get_configurations_for_enterprise(
        enterprise: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
    ) -> str:
        """GET /enterprises/{enterprise}/code-security/configurations - Get code security configurations for an enterprise"""
        # HTTP GET /enterprises/{enterprise}/code-security/configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/code-security/configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_enterprises_by_enterprise_code_security_configurations_defaults(
        enterprise: str,
    ) -> str:
        """GET /enterprises/{enterprise}/code-security/configurations/defaults - Get default code security configurations for an enterprise"""
        # HTTP GET /enterprises/{enterprise}/code-security/configurations/defaults
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/code-security/configurations/defaults",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_update_enterprise_configuration(
        enterprise: str,
        configuration_id: int,
        name: str | None = None,
        description: str | None = None,
        advanced_security: str | None = None,
        code_security: str | None = None,
        dependency_graph: str | None = None,
        dependency_graph_autosubmit_action: str | None = None,
        dependency_graph_autosubmit_action_options: dict | None = None,
        dependabot_alerts: str | None = None,
        dependabot_security_updates: str | None = None,
        code_scanning_default_setup: str | None = None,
        code_scanning_default_setup_options: dict | None = None,
        code_scanning_options: dict | None = None,
        code_scanning_delegated_alert_dismissal: str | None = None,
        secret_protection: str | None = None,
        secret_scanning: str | None = None,
        secret_scanning_push_protection: str | None = None,
        secret_scanning_validity_checks: str | None = None,
        secret_scanning_non_provider_patterns: str | None = None,
        secret_scanning_generic_secrets: str | None = None,
        secret_scanning_delegated_alert_dismissal: str | None = None,
        secret_scanning_extended_metadata: str | None = None,
        private_vulnerability_reporting: str | None = None,
        enforcement: str | None = None,
    ) -> str:
        """PATCH /enterprises/{enterprise}/code-security/configurations/{configuration_id} - Update a custom code security configuration for an enterprise"""
        # HTTP PATCH /enterprises/{enterprise}/code-security/configurations/{configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["configuration_id"] = str(configuration_id)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if advanced_security is not None:
            query_params["advanced_security"] = str(advanced_security)
        if code_security is not None:
            query_params["code_security"] = str(code_security)
        if dependency_graph is not None:
            query_params["dependency_graph"] = str(dependency_graph)
        if dependency_graph_autosubmit_action is not None:
            query_params["dependency_graph_autosubmit_action"] = str(dependency_graph_autosubmit_action)
        body = dependency_graph_autosubmit_action_options if isinstance(dependency_graph_autosubmit_action_options, dict) else {"data": dependency_graph_autosubmit_action_options}
        if dependabot_alerts is not None:
            query_params["dependabot_alerts"] = str(dependabot_alerts)
        if dependabot_security_updates is not None:
            query_params["dependabot_security_updates"] = str(dependabot_security_updates)
        if code_scanning_default_setup is not None:
            query_params["code_scanning_default_setup"] = str(code_scanning_default_setup)
        body = code_scanning_default_setup_options if isinstance(code_scanning_default_setup_options, dict) else {"data": code_scanning_default_setup_options}
        body = code_scanning_options if isinstance(code_scanning_options, dict) else {"data": code_scanning_options}
        if code_scanning_delegated_alert_dismissal is not None:
            query_params["code_scanning_delegated_alert_dismissal"] = str(code_scanning_delegated_alert_dismissal)
        if secret_protection is not None:
            query_params["secret_protection"] = str(secret_protection)
        if secret_scanning is not None:
            query_params["secret_scanning"] = str(secret_scanning)
        if secret_scanning_push_protection is not None:
            query_params["secret_scanning_push_protection"] = str(secret_scanning_push_protection)
        if secret_scanning_validity_checks is not None:
            query_params["secret_scanning_validity_checks"] = str(secret_scanning_validity_checks)
        if secret_scanning_non_provider_patterns is not None:
            query_params["secret_scanning_non_provider_patterns"] = str(secret_scanning_non_provider_patterns)
        if secret_scanning_generic_secrets is not None:
            query_params["secret_scanning_generic_secrets"] = str(secret_scanning_generic_secrets)
        if secret_scanning_delegated_alert_dismissal is not None:
            query_params["secret_scanning_delegated_alert_dismissal"] = str(secret_scanning_delegated_alert_dismissal)
        if secret_scanning_extended_metadata is not None:
            query_params["secret_scanning_extended_metadata"] = str(secret_scanning_extended_metadata)
        if private_vulnerability_reporting is not None:
            query_params["private_vulnerability_reporting"] = str(private_vulnerability_reporting)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        try:
            return await backend.request(
                "PATCH",
                "/enterprises/{enterprise}/code-security/configurations/{configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_delete_configuration_for_enterprise(
        enterprise: str,
        configuration_id: int,
    ) -> str:
        """DELETE /enterprises/{enterprise}/code-security/configurations/{configuration_id} - Delete a code security configuration for an enterprise"""
        # HTTP DELETE /enterprises/{enterprise}/code-security/configurations/{configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["configuration_id"] = str(configuration_id)
        try:
            return await backend.request(
                "DELETE",
                "/enterprises/{enterprise}/code-security/configurations/{configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_enterprises_by_enterprise_code_security_configurations_by_configuration_id(
        enterprise: str,
        configuration_id: int,
    ) -> str:
        """GET /enterprises/{enterprise}/code-security/configurations/{configuration_id} - Retrieve a code security configuration of an enterprise"""
        # HTTP GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["configuration_id"] = str(configuration_id)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/code-security/configurations/{configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_attach_enterprise_configuration(
        enterprise: str,
        configuration_id: int,
        scope: str,
    ) -> str:
        """POST /enterprises/{enterprise}/code-security/configurations/{configuration_id}/attach - Attach an enterprise configuration to repositories"""
        # HTTP POST /enterprises/{enterprise}/code-security/configurations/{configuration_id}/attach
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["configuration_id"] = str(configuration_id)
        if scope is not None:
            query_params["scope"] = str(scope)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/code-security/configurations/{configuration_id}/attach",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_enterprises_by_enterprise_code_security_configurations_by_configuration_id_defaults(
        enterprise: str,
        configuration_id: int,
        default_for_new_repos: str | None = None,
    ) -> str:
        """PUT /enterprises/{enterprise}/code-security/configurations/{configuration_id}/defaults - Set a code security configuration as a default for an enterprise"""
        # HTTP PUT /enterprises/{enterprise}/code-security/configurations/{configuration_id}/defaults
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["configuration_id"] = str(configuration_id)
        if default_for_new_repos is not None:
            query_params["default_for_new_repos"] = str(default_for_new_repos)
        try:
            return await backend.request(
                "PUT",
                "/enterprises/{enterprise}/code-security/configurations/{configuration_id}/defaults",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_enterprises_by_enterprise_code_security_configurations_by_configuration_id_repositories(
        enterprise: str,
        configuration_id: int,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        status: str | None = None,
    ) -> str:
        """GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}/repositories - Get repositories associated with an enterprise code security configuration"""
        # HTTP GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["configuration_id"] = str(configuration_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if status is not None:
            query_params["status"] = str(status)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/code-security/configurations/{configuration_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_list_alerts_for_enterprise(
        enterprise: str,
        state: str | None = None,
        severity: str | None = None,
        ecosystem: str | None = None,
        package: str | None = None,
        epss_percentage: str | None = None,
        has: dict | None = None,
        assignee: str | None = None,
        scope: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /enterprises/{enterprise}/dependabot/alerts - List Dependabot alerts for an enterprise"""
        # HTTP GET /enterprises/{enterprise}/dependabot/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if state is not None:
            query_params["state"] = str(state)
        if severity is not None:
            query_params["severity"] = str(severity)
        if ecosystem is not None:
            query_params["ecosystem"] = str(ecosystem)
        if package is not None:
            query_params["package"] = str(package)
        if epss_percentage is not None:
            query_params["epss_percentage"] = str(epss_percentage)
        body = has if isinstance(has, dict) else {"data": has}
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if scope is not None:
            query_params["scope"] = str(scope)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/dependabot/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_teams_create(
        enterprise: str,
        name: str,
        description: str | None = None,
        sync_to_organizations: str | None = None,
        organization_selection_type: str | None = None,
        group_id: str | None = None,
    ) -> str:
        """POST /enterprises/{enterprise}/teams - Create an enterprise team"""
        # HTTP POST /enterprises/{enterprise}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if sync_to_organizations is not None:
            query_params["sync_to_organizations"] = str(sync_to_organizations)
        if organization_selection_type is not None:
            query_params["organization_selection_type"] = str(organization_selection_type)
        if group_id is not None:
            query_params["group_id"] = str(group_id)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_teams_list(
        enterprise: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /enterprises/{enterprise}/teams - List enterprise teams"""
        # HTTP GET /enterprises/{enterprise}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_memberships_list(
        enterprise: str,
        enterprise_team: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships - List members in an enterprise team"""
        # HTTP GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/teams/{enterprise-team}/memberships",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_memberships_bulk_add(
        enterprise: str,
        enterprise_team: str,
        usernames: list,
    ) -> str:
        """POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/add - Bulk add team members"""
        # HTTP POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/add
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        if usernames is not None:
            query_params["usernames"] = str(usernames)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/teams/{enterprise-team}/memberships/add",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_memberships_bulk_remove(
        enterprise: str,
        enterprise_team: str,
        usernames: list,
    ) -> str:
        """POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/remove - Bulk remove team members"""
        # HTTP POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/remove
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        if usernames is not None:
            query_params["usernames"] = str(usernames)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/teams/{enterprise-team}/memberships/remove",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_memberships_remove(
        enterprise: str,
        enterprise_team: str,
        username: str,
    ) -> str:
        """DELETE /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username} - Remove team membership"""
        # HTTP DELETE /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_memberships_add(
        enterprise: str,
        enterprise_team: str,
        username: str,
    ) -> str:
        """PUT /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username} - Add team member"""
        # HTTP PUT /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "PUT",
                "/enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_memberships_get(
        enterprise: str,
        enterprise_team: str,
        username: str,
    ) -> str:
        """GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username} - Get enterprise team membership"""
        # HTTP GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_organizations_get_assignments(
        enterprise: str,
        enterprise_team: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations - Get organization assignments"""
        # HTTP GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/teams/{enterprise-team}/organizations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_organizations_bulk_add(
        enterprise: str,
        enterprise_team: str,
        organization_slugs: list,
    ) -> str:
        """POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/add - Add organization assignments"""
        # HTTP POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/add
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        if organization_slugs is not None:
            query_params["organization_slugs"] = str(organization_slugs)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/teams/{enterprise-team}/organizations/add",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_organizations_bulk_remove(
        enterprise: str,
        enterprise_team: str,
        organization_slugs: list,
    ) -> str:
        """POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/remove - Remove organization assignments"""
        # HTTP POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/remove
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        if organization_slugs is not None:
            query_params["organization_slugs"] = str(organization_slugs)
        try:
            return await backend.request(
                "POST",
                "/enterprises/{enterprise}/teams/{enterprise-team}/organizations/remove",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_organizations_delete(
        enterprise: str,
        enterprise_team: str,
        org: str,
    ) -> str:
        """DELETE /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org} - Delete an organization assignment"""
        # HTTP DELETE /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "DELETE",
                "/enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_organizations_add(
        enterprise: str,
        enterprise_team: str,
        org: str,
    ) -> str:
        """PUT /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org} - Add an organization assignment"""
        # HTTP PUT /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "PUT",
                "/enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_team_organizations_get_assignment(
        enterprise: str,
        enterprise_team: str,
        org: str,
    ) -> str:
        """GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org} - Get organization assignment"""
        # HTTP GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["enterprise-team"] = str(enterprise_team)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_teams_update(
        enterprise: str,
        team_slug: str,
        name: str | None = None,
        description: str | None = None,
        sync_to_organizations: str | None = None,
        organization_selection_type: str | None = None,
        group_id: str | None = None,
    ) -> str:
        """PATCH /enterprises/{enterprise}/teams/{team_slug} - Update an enterprise team"""
        # HTTP PATCH /enterprises/{enterprise}/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["team_slug"] = str(team_slug)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if sync_to_organizations is not None:
            query_params["sync_to_organizations"] = str(sync_to_organizations)
        if organization_selection_type is not None:
            query_params["organization_selection_type"] = str(organization_selection_type)
        if group_id is not None:
            query_params["group_id"] = str(group_id)
        try:
            return await backend.request(
                "PATCH",
                "/enterprises/{enterprise}/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_teams_delete(
        enterprise: str,
        team_slug: str,
    ) -> str:
        """DELETE /enterprises/{enterprise}/teams/{team_slug} - Delete an enterprise team"""
        # HTTP DELETE /enterprises/{enterprise}/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "DELETE",
                "/enterprises/{enterprise}/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def enterprise_teams_get(
        enterprise: str,
        team_slug: str,
    ) -> str:
        """GET /enterprises/{enterprise}/teams/{team_slug} - Get an enterprise team"""
        # HTTP GET /enterprises/{enterprise}/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["enterprise"] = str(enterprise)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "GET",
                "/enterprises/{enterprise}/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_public_events(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /events - List public events"""
        # HTTP GET /events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_get_feeds(
    ) -> str:
        """GET /feeds - Get feeds"""
        # HTTP GET /feeds
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/feeds",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_create(
        files: dict,
        description: str | None = None,
        public: dict | None = None,
    ) -> str:
        """POST /gists - Create a gist"""
        # HTTP POST /gists
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if description is not None:
            query_params["description"] = str(description)
        body = files if isinstance(files, dict) else {"data": files}
        body = public if isinstance(public, dict) else {"data": public}
        try:
            return await backend.request(
                "POST",
                "/gists",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list(
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /gists - List gists for the authenticated user"""
        # HTTP GET /gists
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/gists",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list_public(
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /gists/public - List public gists"""
        # HTTP GET /gists/public
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/gists/public",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list_starred(
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /gists/starred - List starred gists"""
        # HTTP GET /gists/starred
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/gists/starred",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_update(
        gist_id: str,
        description: str | None = None,
        files: dict | None = None,
    ) -> str:
        """PATCH /gists/{gist_id} - Update a gist"""
        # HTTP PATCH /gists/{gist_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        if description is not None:
            query_params["description"] = str(description)
        body = files if isinstance(files, dict) else {"data": files}
        try:
            return await backend.request(
                "PATCH",
                "/gists/{gist_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_delete(
        gist_id: str,
    ) -> str:
        """DELETE /gists/{gist_id} - Delete a gist"""
        # HTTP DELETE /gists/{gist_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        try:
            return await backend.request(
                "DELETE",
                "/gists/{gist_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_get(
        gist_id: str,
    ) -> str:
        """GET /gists/{gist_id} - Get a gist"""
        # HTTP GET /gists/{gist_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_create_comment(
        gist_id: str,
        body: str,
    ) -> str:
        """POST /gists/{gist_id}/comments - Create a gist comment"""
        # HTTP POST /gists/{gist_id}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "POST",
                "/gists/{gist_id}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list_comments(
        gist_id: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /gists/{gist_id}/comments - List gist comments"""
        # HTTP GET /gists/{gist_id}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_update_comment(
        gist_id: str,
        comment_id: int,
        body: str,
    ) -> str:
        """PATCH /gists/{gist_id}/comments/{comment_id} - Update a gist comment"""
        # HTTP PATCH /gists/{gist_id}/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        path_params["comment_id"] = str(comment_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "PATCH",
                "/gists/{gist_id}/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_delete_comment(
        gist_id: str,
        comment_id: int,
    ) -> str:
        """DELETE /gists/{gist_id}/comments/{comment_id} - Delete a gist comment"""
        # HTTP DELETE /gists/{gist_id}/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "DELETE",
                "/gists/{gist_id}/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_get_comment(
        gist_id: str,
        comment_id: int,
    ) -> str:
        """GET /gists/{gist_id}/comments/{comment_id} - Get a gist comment"""
        # HTTP GET /gists/{gist_id}/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list_commits(
        gist_id: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /gists/{gist_id}/commits - List gist commits"""
        # HTTP GET /gists/{gist_id}/commits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}/commits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_fork(
        gist_id: str,
    ) -> str:
        """POST /gists/{gist_id}/forks - Fork a gist"""
        # HTTP POST /gists/{gist_id}/forks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        try:
            return await backend.request(
                "POST",
                "/gists/{gist_id}/forks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list_forks(
        gist_id: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /gists/{gist_id}/forks - List gist forks"""
        # HTTP GET /gists/{gist_id}/forks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}/forks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_unstar(
        gist_id: str,
    ) -> str:
        """DELETE /gists/{gist_id}/star - Unstar a gist"""
        # HTTP DELETE /gists/{gist_id}/star
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        try:
            return await backend.request(
                "DELETE",
                "/gists/{gist_id}/star",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_star(
        gist_id: str,
    ) -> str:
        """PUT /gists/{gist_id}/star - Star a gist"""
        # HTTP PUT /gists/{gist_id}/star
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        try:
            return await backend.request(
                "PUT",
                "/gists/{gist_id}/star",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_check_is_starred(
        gist_id: str,
    ) -> str:
        """GET /gists/{gist_id}/star - Check if a gist is starred"""
        # HTTP GET /gists/{gist_id}/star
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}/star",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_get_revision(
        gist_id: str,
        sha: str,
    ) -> str:
        """GET /gists/{gist_id}/{sha} - Get a gist revision"""
        # HTTP GET /gists/{gist_id}/{sha}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gist_id"] = str(gist_id)
        path_params["sha"] = str(sha)
        try:
            return await backend.request(
                "GET",
                "/gists/{gist_id}/{sha}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gitignore_get_all_templates(
    ) -> str:
        """GET /gitignore/templates - Get all gitignore templates"""
        # HTTP GET /gitignore/templates
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/gitignore/templates",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gitignore_get_template(
        name: str,
    ) -> str:
        """GET /gitignore/templates/{name} - Get a gitignore template"""
        # HTTP GET /gitignore/templates/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/gitignore/templates/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_repos_accessible_to_installation(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /installation/repositories - List repositories accessible to the app installation"""
        # HTTP GET /installation/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/installation/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_revoke_installation_access_token(
    ) -> str:
        """DELETE /installation/token - Revoke an installation access token"""
        # HTTP DELETE /installation/token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "DELETE",
                "/installation/token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list(
        filter: str | None = None,
        state: str | None = None,
        labels: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        collab: bool | None = None,
        orgs: bool | None = None,
        owned: bool | None = None,
        pulls: bool | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /issues - List issues assigned to the authenticated user"""
        # HTTP GET /issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if filter is not None:
            query_params["filter"] = str(filter)
        if state is not None:
            query_params["state"] = str(state)
        if labels is not None:
            query_params["labels"] = str(labels)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if collab is not None:
            query_params["collab"] = str(collab)
        if orgs is not None:
            query_params["orgs"] = str(orgs)
        if owned is not None:
            query_params["owned"] = str(owned)
        if pulls is not None:
            query_params["pulls"] = str(pulls)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def licenses_get_all_commonly_used(
        featured: bool | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /licenses - Get all commonly used licenses"""
        # HTTP GET /licenses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if featured is not None:
            query_params["featured"] = str(featured)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/licenses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def licenses_get(
        license: str,
    ) -> str:
        """GET /licenses/{license} - Get a license"""
        # HTTP GET /licenses/{license}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["license"] = str(license)
        try:
            return await backend.request(
                "GET",
                "/licenses/{license}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def markdown_render(
        text: str,
        mode: str | None = None,
        context: str | None = None,
    ) -> str:
        """POST /markdown - Render a Markdown document"""
        # HTTP POST /markdown
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if text is not None:
            query_params["text"] = str(text)
        if mode is not None:
            query_params["mode"] = str(mode)
        if context is not None:
            query_params["context"] = str(context)
        try:
            return await backend.request(
                "POST",
                "/markdown",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def markdown_render_raw(
    ) -> str:
        """POST /markdown/raw - Render a Markdown document in raw mode"""
        # HTTP POST /markdown/raw
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "POST",
                "/markdown/raw",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_subscription_plan_for_account(
        account_id: int,
    ) -> str:
        """GET /marketplace_listing/accounts/{account_id} - Get a subscription plan for an account"""
        # HTTP GET /marketplace_listing/accounts/{account_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["account_id"] = str(account_id)
        try:
            return await backend.request(
                "GET",
                "/marketplace_listing/accounts/{account_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_plans(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /marketplace_listing/plans - List plans"""
        # HTTP GET /marketplace_listing/plans
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/marketplace_listing/plans",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_accounts_for_plan(
        plan_id: int,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /marketplace_listing/plans/{plan_id}/accounts - List accounts for a plan"""
        # HTTP GET /marketplace_listing/plans/{plan_id}/accounts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["plan_id"] = str(plan_id)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/marketplace_listing/plans/{plan_id}/accounts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_subscription_plan_for_account_stubbed(
        account_id: int,
    ) -> str:
        """GET /marketplace_listing/stubbed/accounts/{account_id} - Get a subscription plan for an account (stubbed)"""
        # HTTP GET /marketplace_listing/stubbed/accounts/{account_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["account_id"] = str(account_id)
        try:
            return await backend.request(
                "GET",
                "/marketplace_listing/stubbed/accounts/{account_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_plans_stubbed(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /marketplace_listing/stubbed/plans - List plans (stubbed)"""
        # HTTP GET /marketplace_listing/stubbed/plans
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/marketplace_listing/stubbed/plans",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_accounts_for_plan_stubbed(
        plan_id: int,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /marketplace_listing/stubbed/plans/{plan_id}/accounts - List accounts for a plan (stubbed)"""
        # HTTP GET /marketplace_listing/stubbed/plans/{plan_id}/accounts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["plan_id"] = str(plan_id)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/marketplace_listing/stubbed/plans/{plan_id}/accounts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def meta_get(
    ) -> str:
        """GET /meta - Get GitHub meta information"""
        # HTTP GET /meta
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/meta",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_public_events_for_repo_network(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /networks/{owner}/{repo}/events - List public events for a network of repositories"""
        # HTTP GET /networks/{owner}/{repo}/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/networks/{owner}/{repo}/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_mark_notifications_as_read(
        last_read_at: str | None = None,
        read: bool | None = None,
    ) -> str:
        """PUT /notifications - Mark notifications as read"""
        # HTTP PUT /notifications
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if last_read_at is not None:
            query_params["last_read_at"] = str(last_read_at)
        if read is not None:
            query_params["read"] = str(read)
        try:
            return await backend.request(
                "PUT",
                "/notifications",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_notifications(
        all: bool | None = None,
        participating: bool | None = None,
        since: str | None = None,
        before: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /notifications - List notifications for the authenticated user"""
        # HTTP GET /notifications
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if all is not None:
            query_params["all"] = str(all)
        if participating is not None:
            query_params["participating"] = str(participating)
        if since is not None:
            query_params["since"] = str(since)
        if before is not None:
            query_params["before"] = str(before)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/notifications",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_mark_thread_as_read(
        thread_id: int,
    ) -> str:
        """PATCH /notifications/threads/{thread_id} - Mark a thread as read"""
        # HTTP PATCH /notifications/threads/{thread_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["thread_id"] = str(thread_id)
        try:
            return await backend.request(
                "PATCH",
                "/notifications/threads/{thread_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_mark_thread_as_done(
        thread_id: int,
    ) -> str:
        """DELETE /notifications/threads/{thread_id} - Mark a thread as done"""
        # HTTP DELETE /notifications/threads/{thread_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["thread_id"] = str(thread_id)
        try:
            return await backend.request(
                "DELETE",
                "/notifications/threads/{thread_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_get_thread(
        thread_id: int,
    ) -> str:
        """GET /notifications/threads/{thread_id} - Get a thread"""
        # HTTP GET /notifications/threads/{thread_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["thread_id"] = str(thread_id)
        try:
            return await backend.request(
                "GET",
                "/notifications/threads/{thread_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_delete_thread_subscription(
        thread_id: int,
    ) -> str:
        """DELETE /notifications/threads/{thread_id}/subscription - Delete a thread subscription"""
        # HTTP DELETE /notifications/threads/{thread_id}/subscription
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["thread_id"] = str(thread_id)
        try:
            return await backend.request(
                "DELETE",
                "/notifications/threads/{thread_id}/subscription",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_set_thread_subscription(
        thread_id: int,
        ignored: bool | None = None,
    ) -> str:
        """PUT /notifications/threads/{thread_id}/subscription - Set a thread subscription"""
        # HTTP PUT /notifications/threads/{thread_id}/subscription
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["thread_id"] = str(thread_id)
        if ignored is not None:
            query_params["ignored"] = str(ignored)
        try:
            return await backend.request(
                "PUT",
                "/notifications/threads/{thread_id}/subscription",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_notifications_threads_by_thread_id_subscription(
        thread_id: int,
    ) -> str:
        """GET /notifications/threads/{thread_id}/subscription - Get a thread subscription for the authenticated user"""
        # HTTP GET /notifications/threads/{thread_id}/subscription
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["thread_id"] = str(thread_id)
        try:
            return await backend.request(
                "GET",
                "/notifications/threads/{thread_id}/subscription",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def meta_get_octocat(
        s: str | None = None,
    ) -> str:
        """GET /octocat - Get Octocat"""
        # HTTP GET /octocat
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if s is not None:
            query_params["s"] = str(s)
        try:
            return await backend.request(
                "GET",
                "/octocat",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list(
        since: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /organizations - List organizations"""
        # HTTP GET /organizations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/organizations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_organizations_by_org_actions_cache_retention_limit(
        org: str,
        max_cache_retention_days: int | None = None,
    ) -> str:
        """PUT /organizations/{org}/actions/cache/retention-limit - Set GitHub Actions cache retention limit for an organization"""
        # HTTP PUT /organizations/{org}/actions/cache/retention-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if max_cache_retention_days is not None:
            query_params["max_cache_retention_days"] = str(max_cache_retention_days)
        try:
            return await backend.request(
                "PUT",
                "/organizations/{org}/actions/cache/retention-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_organizations_by_org_actions_cache_retention_limit(
        org: str,
    ) -> str:
        """GET /organizations/{org}/actions/cache/retention-limit - Get GitHub Actions cache retention limit for an organization"""
        # HTTP GET /organizations/{org}/actions/cache/retention-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/actions/cache/retention-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_organizations_by_org_actions_cache_storage_limit(
        org: str,
        max_cache_size_gb: int | None = None,
    ) -> str:
        """PUT /organizations/{org}/actions/cache/storage-limit - Set GitHub Actions cache storage limit for an organization"""
        # HTTP PUT /organizations/{org}/actions/cache/storage-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if max_cache_size_gb is not None:
            query_params["max_cache_size_gb"] = str(max_cache_size_gb)
        try:
            return await backend.request(
                "PUT",
                "/organizations/{org}/actions/cache/storage-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_organizations_by_org_actions_cache_storage_limit(
        org: str,
    ) -> str:
        """GET /organizations/{org}/actions/cache/storage-limit - Get GitHub Actions cache storage limit for an organization"""
        # HTTP GET /organizations/{org}/actions/cache/storage-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/actions/cache/storage-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_update_repository_access_for_org(
        org: str,
        repository_ids_to_add: list | None = None,
        repository_ids_to_remove: list | None = None,
    ) -> str:
        """PATCH /organizations/{org}/dependabot/repository-access - Updates Dependabot's repository access list for an organization"""
        # HTTP PATCH /organizations/{org}/dependabot/repository-access
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if repository_ids_to_add is not None:
            query_params["repository_ids_to_add"] = str(repository_ids_to_add)
        if repository_ids_to_remove is not None:
            query_params["repository_ids_to_remove"] = str(repository_ids_to_remove)
        try:
            return await backend.request(
                "PATCH",
                "/organizations/{org}/dependabot/repository-access",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_repository_access_for_org(
        org: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /organizations/{org}/dependabot/repository-access - Lists the repositories Dependabot can access in an organization"""
        # HTTP GET /organizations/{org}/dependabot/repository-access
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/dependabot/repository-access",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_set_repository_access_default_level(
        org: str,
        default_level: str,
    ) -> str:
        """PUT /organizations/{org}/dependabot/repository-access/default-level - Set the default repository access level for Dependabot"""
        # HTTP PUT /organizations/{org}/dependabot/repository-access/default-level
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if default_level is not None:
            query_params["default_level"] = str(default_level)
        try:
            return await backend.request(
                "PUT",
                "/organizations/{org}/dependabot/repository-access/default-level",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def billing_get_all_budgets_org(
        org: str,
        page: int | None = None,
        per_page: int | None = None,
        scope: str | None = None,
    ) -> str:
        """GET /organizations/{org}/settings/billing/budgets - Get all budgets for an organization"""
        # HTTP GET /organizations/{org}/settings/billing/budgets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if scope is not None:
            query_params["scope"] = str(scope)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/settings/billing/budgets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def billing_update_budget_org(
        org: str,
        budget_id: str,
        budget_amount: int | None = None,
        prevent_further_usage: bool | None = None,
        budget_alerting: dict | None = None,
        budget_scope: str | None = None,
        budget_entity_name: str | None = None,
        budget_type: dict | None = None,
        budget_product_sku: str | None = None,
    ) -> str:
        """PATCH /organizations/{org}/settings/billing/budgets/{budget_id} - Update a budget for an organization"""
        # HTTP PATCH /organizations/{org}/settings/billing/budgets/{budget_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["budget_id"] = str(budget_id)
        if budget_amount is not None:
            query_params["budget_amount"] = str(budget_amount)
        if prevent_further_usage is not None:
            query_params["prevent_further_usage"] = str(prevent_further_usage)
        body = budget_alerting if isinstance(budget_alerting, dict) else {"data": budget_alerting}
        if budget_scope is not None:
            query_params["budget_scope"] = str(budget_scope)
        if budget_entity_name is not None:
            query_params["budget_entity_name"] = str(budget_entity_name)
        body = budget_type if isinstance(budget_type, dict) else {"data": budget_type}
        if budget_product_sku is not None:
            query_params["budget_product_sku"] = str(budget_product_sku)
        try:
            return await backend.request(
                "PATCH",
                "/organizations/{org}/settings/billing/budgets/{budget_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def billing_delete_budget_org(
        org: str,
        budget_id: str,
    ) -> str:
        """DELETE /organizations/{org}/settings/billing/budgets/{budget_id} - Delete a budget for an organization"""
        # HTTP DELETE /organizations/{org}/settings/billing/budgets/{budget_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["budget_id"] = str(budget_id)
        try:
            return await backend.request(
                "DELETE",
                "/organizations/{org}/settings/billing/budgets/{budget_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def billing_get_budget_org(
        org: str,
        budget_id: str,
    ) -> str:
        """GET /organizations/{org}/settings/billing/budgets/{budget_id} - Get a budget by ID for an organization"""
        # HTTP GET /organizations/{org}/settings/billing/budgets/{budget_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["budget_id"] = str(budget_id)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/settings/billing/budgets/{budget_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_organizations_by_org_settings_billing_premium_request_usage(
        org: str,
        year: int | None = None,
        month: int | None = None,
        day: int | None = None,
        user: str | None = None,
        model: str | None = None,
        product: str | None = None,
    ) -> str:
        """GET /organizations/{org}/settings/billing/premium_request/usage - Get billing premium request usage report for an organization"""
        # HTTP GET /organizations/{org}/settings/billing/premium_request/usage
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if year is not None:
            query_params["year"] = str(year)
        if month is not None:
            query_params["month"] = str(month)
        if day is not None:
            query_params["day"] = str(day)
        if user is not None:
            query_params["user"] = str(user)
        if model is not None:
            query_params["model"] = str(model)
        if product is not None:
            query_params["product"] = str(product)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/settings/billing/premium_request/usage",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def billing_get_github_billing_usage_report_org(
        org: str,
        year: int | None = None,
        month: int | None = None,
        day: int | None = None,
    ) -> str:
        """GET /organizations/{org}/settings/billing/usage - Get billing usage report for an organization"""
        # HTTP GET /organizations/{org}/settings/billing/usage
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if year is not None:
            query_params["year"] = str(year)
        if month is not None:
            query_params["month"] = str(month)
        if day is not None:
            query_params["day"] = str(day)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/settings/billing/usage",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_organizations_by_org_settings_billing_usage_summary(
        org: str,
        year: int | None = None,
        month: int | None = None,
        day: int | None = None,
        repository: str | None = None,
        product: str | None = None,
        sku: str | None = None,
    ) -> str:
        """GET /organizations/{org}/settings/billing/usage/summary - Get billing usage summary for an organization"""
        # HTTP GET /organizations/{org}/settings/billing/usage/summary
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if year is not None:
            query_params["year"] = str(year)
        if month is not None:
            query_params["month"] = str(month)
        if day is not None:
            query_params["day"] = str(day)
        if repository is not None:
            query_params["repository"] = str(repository)
        if product is not None:
            query_params["product"] = str(product)
        if sku is not None:
            query_params["sku"] = str(sku)
        try:
            return await backend.request(
                "GET",
                "/organizations/{org}/settings/billing/usage/summary",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update(
        org: str,
        billing_email: str | None = None,
        company: str | None = None,
        email: str | None = None,
        twitter_username: str | None = None,
        location: str | None = None,
        name: str | None = None,
        description: str | None = None,
        has_organization_projects: bool | None = None,
        has_repository_projects: bool | None = None,
        default_repository_permission: str | None = None,
        members_can_create_repositories: bool | None = None,
        members_can_create_internal_repositories: bool | None = None,
        members_can_create_private_repositories: bool | None = None,
        members_can_create_public_repositories: bool | None = None,
        members_allowed_repository_creation_type: str | None = None,
        members_can_create_pages: bool | None = None,
        members_can_create_public_pages: bool | None = None,
        members_can_create_private_pages: bool | None = None,
        members_can_fork_private_repositories: bool | None = None,
        web_commit_signoff_required: bool | None = None,
        blog: str | None = None,
        advanced_security_enabled_for_new_repositories: bool | None = None,
        dependabot_alerts_enabled_for_new_repositories: bool | None = None,
        dependabot_security_updates_enabled_for_new_repositories: bool | None = None,
        dependency_graph_enabled_for_new_repositories: bool | None = None,
        secret_scanning_enabled_for_new_repositories: bool | None = None,
        secret_scanning_push_protection_enabled_for_new_repositories: bool | None = None,
        secret_scanning_push_protection_custom_link_enabled: bool | None = None,
        secret_scanning_push_protection_custom_link: str | None = None,
        deploy_keys_enabled_for_repositories: bool | None = None,
    ) -> str:
        """PATCH /orgs/{org} - Update an organization"""
        # HTTP PATCH /orgs/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if billing_email is not None:
            query_params["billing_email"] = str(billing_email)
        if company is not None:
            query_params["company"] = str(company)
        if email is not None:
            query_params["email"] = str(email)
        if twitter_username is not None:
            query_params["twitter_username"] = str(twitter_username)
        if location is not None:
            query_params["location"] = str(location)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if has_organization_projects is not None:
            query_params["has_organization_projects"] = str(has_organization_projects)
        if has_repository_projects is not None:
            query_params["has_repository_projects"] = str(has_repository_projects)
        if default_repository_permission is not None:
            query_params["default_repository_permission"] = str(default_repository_permission)
        if members_can_create_repositories is not None:
            query_params["members_can_create_repositories"] = str(members_can_create_repositories)
        if members_can_create_internal_repositories is not None:
            query_params["members_can_create_internal_repositories"] = str(members_can_create_internal_repositories)
        if members_can_create_private_repositories is not None:
            query_params["members_can_create_private_repositories"] = str(members_can_create_private_repositories)
        if members_can_create_public_repositories is not None:
            query_params["members_can_create_public_repositories"] = str(members_can_create_public_repositories)
        if members_allowed_repository_creation_type is not None:
            query_params["members_allowed_repository_creation_type"] = str(members_allowed_repository_creation_type)
        if members_can_create_pages is not None:
            query_params["members_can_create_pages"] = str(members_can_create_pages)
        if members_can_create_public_pages is not None:
            query_params["members_can_create_public_pages"] = str(members_can_create_public_pages)
        if members_can_create_private_pages is not None:
            query_params["members_can_create_private_pages"] = str(members_can_create_private_pages)
        if members_can_fork_private_repositories is not None:
            query_params["members_can_fork_private_repositories"] = str(members_can_fork_private_repositories)
        if web_commit_signoff_required is not None:
            query_params["web_commit_signoff_required"] = str(web_commit_signoff_required)
        if blog is not None:
            query_params["blog"] = str(blog)
        if advanced_security_enabled_for_new_repositories is not None:
            query_params["advanced_security_enabled_for_new_repositories"] = str(advanced_security_enabled_for_new_repositories)
        if dependabot_alerts_enabled_for_new_repositories is not None:
            query_params["dependabot_alerts_enabled_for_new_repositories"] = str(dependabot_alerts_enabled_for_new_repositories)
        if dependabot_security_updates_enabled_for_new_repositories is not None:
            query_params["dependabot_security_updates_enabled_for_new_repositories"] = str(dependabot_security_updates_enabled_for_new_repositories)
        if dependency_graph_enabled_for_new_repositories is not None:
            query_params["dependency_graph_enabled_for_new_repositories"] = str(dependency_graph_enabled_for_new_repositories)
        if secret_scanning_enabled_for_new_repositories is not None:
            query_params["secret_scanning_enabled_for_new_repositories"] = str(secret_scanning_enabled_for_new_repositories)
        if secret_scanning_push_protection_enabled_for_new_repositories is not None:
            query_params["secret_scanning_push_protection_enabled_for_new_repositories"] = str(secret_scanning_push_protection_enabled_for_new_repositories)
        if secret_scanning_push_protection_custom_link_enabled is not None:
            query_params["secret_scanning_push_protection_custom_link_enabled"] = str(secret_scanning_push_protection_custom_link_enabled)
        if secret_scanning_push_protection_custom_link is not None:
            query_params["secret_scanning_push_protection_custom_link"] = str(secret_scanning_push_protection_custom_link)
        if deploy_keys_enabled_for_repositories is not None:
            query_params["deploy_keys_enabled_for_repositories"] = str(deploy_keys_enabled_for_repositories)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete(
        org: str,
    ) -> str:
        """DELETE /orgs/{org} - Delete an organization"""
        # HTTP DELETE /orgs/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get(
        org: str,
    ) -> str:
        """GET /orgs/{org} - Get an organization"""
        # HTTP GET /orgs/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_actions_cache_usage_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/cache/usage - Get GitHub Actions cache usage for an organization"""
        # HTTP GET /orgs/{org}/actions/cache/usage
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/cache/usage",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_actions_cache_usage_by_repo_for_org(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/cache/usage-by-repository - List repositories with GitHub Actions cache usage for an organization"""
        # HTTP GET /orgs/{org}/actions/cache/usage-by-repository
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/cache/usage-by-repository",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_hosted_runner_for_org(
        org: str,
        name: str,
        image: dict,
        size: str,
        runner_group_id: int,
        maximum_runners: int | None = None,
        enable_static_ip: bool | None = None,
        image_gen: bool | None = None,
    ) -> str:
        """POST /orgs/{org}/actions/hosted-runners - Create a GitHub-hosted runner for an organization"""
        # HTTP POST /orgs/{org}/actions/hosted-runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        body = image if isinstance(image, dict) else {"data": image}
        if size is not None:
            query_params["size"] = str(size)
        if runner_group_id is not None:
            query_params["runner_group_id"] = str(runner_group_id)
        if maximum_runners is not None:
            query_params["maximum_runners"] = str(maximum_runners)
        if enable_static_ip is not None:
            query_params["enable_static_ip"] = str(enable_static_ip)
        if image_gen is not None:
            query_params["image_gen"] = str(image_gen)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/hosted-runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_hosted_runners_for_org(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners - List GitHub-hosted runners for an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_custom_images_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/images/custom - List custom images for an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/images/custom
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/images/custom",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_custom_image_from_org(
        org: str,
        image_definition_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id} - Delete a custom image from the organization"""
        # HTTP DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["image_definition_id"] = str(image_definition_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_custom_image_for_org(
        org: str,
        image_definition_id: int,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id} - Get a custom image definition for GitHub Actions Hosted Runners"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["image_definition_id"] = str(image_definition_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_custom_image_versions_for_org(
        image_definition_id: int,
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions - List image versions of a custom image for an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["image_definition_id"] = str(image_definition_id)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_custom_image_version_from_org(
        org: str,
        image_definition_id: int,
        version: str,
    ) -> str:
        """DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version} - Delete an image version of custom image from the organization"""
        # HTTP DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["image_definition_id"] = str(image_definition_id)
        path_params["version"] = str(version)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_custom_image_version_for_org(
        org: str,
        image_definition_id: int,
        version: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version} - Get an image version of a custom image for GitHub Actions Hosted Runners"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["image_definition_id"] = str(image_definition_id)
        path_params["version"] = str(version)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_hosted_runners_images_github_owned(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/images/github-owned - Get GitHub-owned images for GitHub-hosted runners in an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/images/github-owned
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/images/github-owned",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_hosted_runners_partner_images_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/images/partner - Get partner images for GitHub-hosted runners in an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/images/partner
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/images/partner",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_hosted_runners_limits_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/limits - Get limits on GitHub-hosted runners for an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_hosted_runners_machine_specs_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/machine-sizes - Get GitHub-hosted runners machine specs for an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/machine-sizes
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/machine-sizes",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_hosted_runners_platforms_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/platforms - Get platforms for GitHub-hosted runners in an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/platforms
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/platforms",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_update_hosted_runner_for_org(
        org: str,
        hosted_runner_id: int,
        name: str | None = None,
        runner_group_id: int | None = None,
        maximum_runners: int | None = None,
        enable_static_ip: bool | None = None,
        size: str | None = None,
        image_id: str | None = None,
        image_version: str | None = None,
    ) -> str:
        """PATCH /orgs/{org}/actions/hosted-runners/{hosted_runner_id} - Update a GitHub-hosted runner for an organization"""
        # HTTP PATCH /orgs/{org}/actions/hosted-runners/{hosted_runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hosted_runner_id"] = str(hosted_runner_id)
        if name is not None:
            query_params["name"] = str(name)
        if runner_group_id is not None:
            query_params["runner_group_id"] = str(runner_group_id)
        if maximum_runners is not None:
            query_params["maximum_runners"] = str(maximum_runners)
        if enable_static_ip is not None:
            query_params["enable_static_ip"] = str(enable_static_ip)
        if size is not None:
            query_params["size"] = str(size)
        if image_id is not None:
            query_params["image_id"] = str(image_id)
        if image_version is not None:
            query_params["image_version"] = str(image_version)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/actions/hosted-runners/{hosted_runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_hosted_runner_for_org(
        org: str,
        hosted_runner_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/hosted-runners/{hosted_runner_id} - Delete a GitHub-hosted runner for an organization"""
        # HTTP DELETE /orgs/{org}/actions/hosted-runners/{hosted_runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hosted_runner_id"] = str(hosted_runner_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/hosted-runners/{hosted_runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_hosted_runner_for_org(
        org: str,
        hosted_runner_id: int,
    ) -> str:
        """GET /orgs/{org}/actions/hosted-runners/{hosted_runner_id} - Get a GitHub-hosted runner for an organization"""
        # HTTP GET /orgs/{org}/actions/hosted-runners/{hosted_runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hosted_runner_id"] = str(hosted_runner_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/hosted-runners/{hosted_runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_orgs_by_org_actions_oidc_customization_properties_repo(
        org: str,
        custom_property_name: str,
    ) -> str:
        """POST /orgs/{org}/actions/oidc/customization/properties/repo - Create an OIDC custom property inclusion for an organization"""
        # HTTP POST /orgs/{org}/actions/oidc/customization/properties/repo
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if custom_property_name is not None:
            query_params["custom_property_name"] = str(custom_property_name)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/oidc/customization/properties/repo",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def oidc_list_oidc_custom_property_inclusions_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/oidc/customization/properties/repo - List OIDC custom property inclusions for an organization"""
        # HTTP GET /orgs/{org}/actions/oidc/customization/properties/repo
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/oidc/customization/properties/repo",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_oidc_customization_properties_repo_by_custom_property_name(
        org: str,
        custom_property_name: str,
    ) -> str:
        """DELETE /orgs/{org}/actions/oidc/customization/properties/repo/{custom_property_name} - Delete an OIDC custom property inclusion for an organization"""
        # HTTP DELETE /orgs/{org}/actions/oidc/customization/properties/repo/{custom_property_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["custom_property_name"] = str(custom_property_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/oidc/customization/properties/repo/{custom_property_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def oidc_update_oidc_custom_sub_template_for_org(
        org: str,
        include_claim_keys: list,
    ) -> str:
        """PUT /orgs/{org}/actions/oidc/customization/sub - Set the customization template for an OIDC subject claim for an organization"""
        # HTTP PUT /orgs/{org}/actions/oidc/customization/sub
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if include_claim_keys is not None:
            query_params["include_claim_keys"] = str(include_claim_keys)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/oidc/customization/sub",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def oidc_get_oidc_custom_sub_template_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/oidc/customization/sub - Get the customization template for an OIDC subject claim for an organization"""
        # HTTP GET /orgs/{org}/actions/oidc/customization/sub
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/oidc/customization/sub",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions(
        org: str,
        enabled_repositories: str,
        allowed_actions: str | None = None,
        sha_pinning_required: bool | None = None,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions - Set GitHub Actions permissions for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if enabled_repositories is not None:
            query_params["enabled_repositories"] = str(enabled_repositories)
        if allowed_actions is not None:
            query_params["allowed_actions"] = str(allowed_actions)
        if sha_pinning_required is not None:
            query_params["sha_pinning_required"] = str(sha_pinning_required)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions - Get GitHub Actions permissions for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_artifact_and_log_retention(
        org: str,
        days: int,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/artifact-and-log-retention - Set artifact and log retention settings for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/artifact-and-log-retention
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if days is not None:
            query_params["days"] = str(days)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/artifact-and-log-retention",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_artifact_and_log_retention(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/artifact-and-log-retention - Get artifact and log retention settings for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/artifact-and-log-retention
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/artifact-and-log-retention",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_fork_pr_contributor_approval(
        org: str,
        approval_policy: str,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/fork-pr-contributor-approval - Set fork PR contributor approval permissions for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/fork-pr-contributor-approval
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if approval_policy is not None:
            query_params["approval_policy"] = str(approval_policy)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/fork-pr-contributor-approval",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_fork_pr_contributor_approval(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/fork-pr-contributor-approval - Get fork PR contributor approval permissions for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/fork-pr-contributor-approval
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/fork-pr-contributor-approval",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_fork_pr_workflows_private_repos(
        org: str,
        run_workflows_from_fork_pull_requests: bool,
        send_write_tokens_to_workflows: bool | None = None,
        send_secrets_and_variables: bool | None = None,
        require_approval_for_fork_pr_workflows: bool | None = None,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos - Set private repo fork PR workflow settings for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if run_workflows_from_fork_pull_requests is not None:
            query_params["run_workflows_from_fork_pull_requests"] = str(run_workflows_from_fork_pull_requests)
        if send_write_tokens_to_workflows is not None:
            query_params["send_write_tokens_to_workflows"] = str(send_write_tokens_to_workflows)
        if send_secrets_and_variables is not None:
            query_params["send_secrets_and_variables"] = str(send_secrets_and_variables)
        if require_approval_for_fork_pr_workflows is not None:
            query_params["require_approval_for_fork_pr_workflows"] = str(require_approval_for_fork_pr_workflows)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/fork-pr-workflows-private-repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_fork_pr_workflows_private_repos(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos - Get private repo fork PR workflow settings for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/fork-pr-workflows-private-repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_repositories(
        org: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/repositories - Set selected repositories enabled for GitHub Actions in an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_repositories(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/repositories - List selected repositories enabled for GitHub Actions in an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_permissions_repositories_by_repository_id(
        org: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/permissions/repositories/{repository_id} - Disable a selected repository for GitHub Actions in an organization"""
        # HTTP DELETE /orgs/{org}/actions/permissions/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/permissions/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_repositories_by_repository_id(
        org: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/repositories/{repository_id} - Enable a selected repository for GitHub Actions in an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_allowed_actions_organization(
        org: str,
        github_owned_allowed: bool | None = None,
        verified_allowed: bool | None = None,
        patterns_allowed: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/selected-actions - Set allowed actions and reusable workflows for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/selected-actions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if github_owned_allowed is not None:
            query_params["github_owned_allowed"] = str(github_owned_allowed)
        if verified_allowed is not None:
            query_params["verified_allowed"] = str(verified_allowed)
        if patterns_allowed is not None:
            query_params["patterns_allowed"] = str(patterns_allowed)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/selected-actions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_allowed_actions_organization(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/selected-actions - Get allowed actions and reusable workflows for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/selected-actions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/selected-actions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_self_hosted_runners(
        org: str,
        enabled_repositories: str,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/self-hosted-runners - Set self-hosted runners settings for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/self-hosted-runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if enabled_repositories is not None:
            query_params["enabled_repositories"] = str(enabled_repositories)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/self-hosted-runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_self_hosted_runners(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/self-hosted-runners - Get self-hosted runners settings for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/self-hosted-runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/self-hosted-runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_self_hosted_runners_repositories(
        org: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories - Set repositories allowed to use self-hosted runners in an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/self-hosted-runners/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_self_hosted_runners_repositories(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/self-hosted-runners/repositories - List repositories allowed to use self-hosted runners in an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/self-hosted-runners/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/self-hosted-runners/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_permissions_self_hosted_runners_repositories_by_repository_id(
        org: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id} - Remove a repository from the list of repositories allowed to use self-hosted runners in an organization"""
        # HTTP DELETE /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_self_hosted_runners_repositories_by_repository_id(
        org: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id} - Add a repository to the list of repositories allowed to use self-hosted runners in an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_permissions_workflow(
        org: str,
        default_workflow_permissions: str | None = None,
        can_approve_pull_request_reviews: bool | None = None,
    ) -> str:
        """PUT /orgs/{org}/actions/permissions/workflow - Set default workflow permissions for an organization"""
        # HTTP PUT /orgs/{org}/actions/permissions/workflow
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if default_workflow_permissions is not None:
            query_params["default_workflow_permissions"] = str(default_workflow_permissions)
        if can_approve_pull_request_reviews is not None:
            query_params["can_approve_pull_request_reviews"] = str(can_approve_pull_request_reviews)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/permissions/workflow",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_permissions_workflow(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/permissions/workflow - Get default workflow permissions for an organization"""
        # HTTP GET /orgs/{org}/actions/permissions/workflow
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/permissions/workflow",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_self_hosted_runner_group_for_org(
        org: str,
        name: str,
        visibility: str | None = None,
        selected_repository_ids: list | None = None,
        runners: list | None = None,
        allows_public_repositories: bool | None = None,
        restricted_to_workflows: bool | None = None,
        selected_workflows: list | None = None,
        network_configuration_id: str | None = None,
    ) -> str:
        """POST /orgs/{org}/actions/runner-groups - Create a self-hosted runner group for an organization"""
        # HTTP POST /orgs/{org}/actions/runner-groups
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        if runners is not None:
            query_params["runners"] = str(runners)
        if allows_public_repositories is not None:
            query_params["allows_public_repositories"] = str(allows_public_repositories)
        if restricted_to_workflows is not None:
            query_params["restricted_to_workflows"] = str(restricted_to_workflows)
        if selected_workflows is not None:
            query_params["selected_workflows"] = str(selected_workflows)
        if network_configuration_id is not None:
            query_params["network_configuration_id"] = str(network_configuration_id)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/runner-groups",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_self_hosted_runner_groups_for_org(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        visible_to_repository: str | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/runner-groups - List self-hosted runner groups for an organization"""
        # HTTP GET /orgs/{org}/actions/runner-groups
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if visible_to_repository is not None:
            query_params["visible_to_repository"] = str(visible_to_repository)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runner-groups",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_update_self_hosted_runner_group_for_org(
        org: str,
        runner_group_id: int,
        name: str,
        visibility: str | None = None,
        allows_public_repositories: bool | None = None,
        restricted_to_workflows: bool | None = None,
        selected_workflows: list | None = None,
        network_configuration_id: str | None = None,
    ) -> str:
        """PATCH /orgs/{org}/actions/runner-groups/{runner_group_id} - Update a self-hosted runner group for an organization"""
        # HTTP PATCH /orgs/{org}/actions/runner-groups/{runner_group_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        if name is not None:
            query_params["name"] = str(name)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if allows_public_repositories is not None:
            query_params["allows_public_repositories"] = str(allows_public_repositories)
        if restricted_to_workflows is not None:
            query_params["restricted_to_workflows"] = str(restricted_to_workflows)
        if selected_workflows is not None:
            query_params["selected_workflows"] = str(selected_workflows)
        if network_configuration_id is not None:
            query_params["network_configuration_id"] = str(network_configuration_id)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_self_hosted_runner_group_from_org(
        org: str,
        runner_group_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/runner-groups/{runner_group_id} - Delete a self-hosted runner group from an organization"""
        # HTTP DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_self_hosted_runner_group_for_org(
        org: str,
        runner_group_id: int,
    ) -> str:
        """GET /orgs/{org}/actions/runner-groups/{runner_group_id} - Get a self-hosted runner group for an organization"""
        # HTTP GET /orgs/{org}/actions/runner-groups/{runner_group_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_runner_groups_by_runner_group_id_hosted_runners(
        org: str,
        runner_group_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/runner-groups/{runner_group_id}/hosted-runners - List GitHub-hosted runners in a group for an organization"""
        # HTTP GET /orgs/{org}/actions/runner-groups/{runner_group_id}/hosted-runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/hosted-runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_runner_groups_by_runner_group_id_repositories(
        org: str,
        runner_group_id: int,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories - Set repository access for a self-hosted runner group in an organization"""
        # HTTP PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_runner_groups_by_runner_group_id_repositories(
        org: str,
        runner_group_id: int,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories - List repository access to a self-hosted runner group in an organization"""
        # HTTP GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_runner_groups_by_runner_group_id_repositories_by_repository_id(
        org: str,
        runner_group_id: int,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id} - Remove repository access to a self-hosted runner group in an organization"""
        # HTTP DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_runner_groups_by_runner_group_id_repositories_by_repository_id(
        org: str,
        runner_group_id: int,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id} - Add repository access to a self-hosted runner group in an organization"""
        # HTTP PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_self_hosted_runners_in_group_for_org(
        org: str,
        runner_group_id: int,
        runners: list,
    ) -> str:
        """PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners - Set self-hosted runners in a group for an organization"""
        # HTTP PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        if runners is not None:
            query_params["runners"] = str(runners)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_self_hosted_runners_in_group_for_org(
        org: str,
        runner_group_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners - List self-hosted runners in a group for an organization"""
        # HTTP GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_runner_groups_by_runner_group_id_runners_by_runner_id(
        org: str,
        runner_group_id: int,
        runner_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id} - Remove a self-hosted runner from a group for an organization"""
        # HTTP DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_add_self_hosted_runner_to_group_for_org(
        org: str,
        runner_group_id: int,
        runner_id: int,
    ) -> str:
        """PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id} - Add a self-hosted runner to a group for an organization"""
        # HTTP PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_group_id"] = str(runner_group_id)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_self_hosted_runners_for_org(
        org: str,
        name: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/runners - List self-hosted runners for an organization"""
        # HTTP GET /orgs/{org}/actions/runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if name is not None:
            query_params["name"] = str(name)
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_runner_applications_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/runners/downloads - List runner applications for an organization"""
        # HTTP GET /orgs/{org}/actions/runners/downloads
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runners/downloads",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_generate_runner_jitconfig_for_org(
        org: str,
        name: str,
        runner_group_id: int,
        labels: list,
        work_folder: str | None = None,
    ) -> str:
        """POST /orgs/{org}/actions/runners/generate-jitconfig - Create configuration for a just-in-time runner for an organization"""
        # HTTP POST /orgs/{org}/actions/runners/generate-jitconfig
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if runner_group_id is not None:
            query_params["runner_group_id"] = str(runner_group_id)
        if labels is not None:
            query_params["labels"] = str(labels)
        if work_folder is not None:
            query_params["work_folder"] = str(work_folder)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/runners/generate-jitconfig",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_registration_token_for_org(
        org: str,
    ) -> str:
        """POST /orgs/{org}/actions/runners/registration-token - Create a registration token for an organization"""
        # HTTP POST /orgs/{org}/actions/runners/registration-token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/runners/registration-token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_remove_token_for_org(
        org: str,
    ) -> str:
        """POST /orgs/{org}/actions/runners/remove-token - Create a remove token for an organization"""
        # HTTP POST /orgs/{org}/actions/runners/remove-token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/runners/remove-token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_self_hosted_runner_from_org(
        org: str,
        runner_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/runners/{runner_id} - Delete a self-hosted runner from an organization"""
        # HTTP DELETE /orgs/{org}/actions/runners/{runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/runners/{runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_self_hosted_runner_for_org(
        org: str,
        runner_id: int,
    ) -> str:
        """GET /orgs/{org}/actions/runners/{runner_id} - Get a self-hosted runner for an organization"""
        # HTTP GET /orgs/{org}/actions/runners/{runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runners/{runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_orgs_by_org_actions_runners_by_runner_id_labels(
        org: str,
        runner_id: int,
        labels: list,
    ) -> str:
        """POST /orgs/{org}/actions/runners/{runner_id}/labels - Add custom labels to a self-hosted runner for an organization"""
        # HTTP POST /orgs/{org}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        if labels is not None:
            query_params["labels"] = str(labels)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_runners_by_runner_id_labels(
        org: str,
        runner_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/runners/{runner_id}/labels - Remove all custom labels from a self-hosted runner for an organization"""
        # HTTP DELETE /orgs/{org}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_actions_runners_by_runner_id_labels(
        org: str,
        runner_id: int,
        labels: list,
    ) -> str:
        """PUT /orgs/{org}/actions/runners/{runner_id}/labels - Set custom labels for a self-hosted runner for an organization"""
        # HTTP PUT /orgs/{org}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        if labels is not None:
            query_params["labels"] = str(labels)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_actions_runners_by_runner_id_labels(
        org: str,
        runner_id: int,
    ) -> str:
        """GET /orgs/{org}/actions/runners/{runner_id}/labels - List labels for a self-hosted runner for an organization"""
        # HTTP GET /orgs/{org}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_actions_runners_by_runner_id_labels_by_name(
        org: str,
        runner_id: int,
        name: str,
    ) -> str:
        """DELETE /orgs/{org}/actions/runners/{runner_id}/labels/{name} - Remove a custom label from a self-hosted runner for an organization"""
        # HTTP DELETE /orgs/{org}/actions/runners/{runner_id}/labels/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["runner_id"] = str(runner_id)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/runners/{runner_id}/labels/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_org_secrets(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/secrets - List organization secrets"""
        # HTTP GET /orgs/{org}/actions/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_org_public_key(
        org: str,
    ) -> str:
        """GET /orgs/{org}/actions/secrets/public-key - Get an organization public key"""
        # HTTP GET /orgs/{org}/actions/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_org_secret(
        org: str,
        secret_name: str,
    ) -> str:
        """DELETE /orgs/{org}/actions/secrets/{secret_name} - Delete an organization secret"""
        # HTTP DELETE /orgs/{org}/actions/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_or_update_org_secret(
        org: str,
        secret_name: str,
        encrypted_value: str,
        key_id: str,
        visibility: str,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/actions/secrets/{secret_name} - Create or update an organization secret"""
        # HTTP PUT /orgs/{org}/actions/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_org_secret(
        org: str,
        secret_name: str,
    ) -> str:
        """GET /orgs/{org}/actions/secrets/{secret_name} - Get an organization secret"""
        # HTTP GET /orgs/{org}/actions/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_selected_repos_for_org_secret(
        org: str,
        secret_name: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/actions/secrets/{secret_name}/repositories - Set selected repositories for an organization secret"""
        # HTTP PUT /orgs/{org}/actions/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_selected_repos_for_org_secret(
        org: str,
        secret_name: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/secrets/{secret_name}/repositories - List selected repositories for an organization secret"""
        # HTTP GET /orgs/{org}/actions/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_remove_selected_repo_from_org_secret(
        org: str,
        secret_name: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id} - Remove selected repository from an organization secret"""
        # HTTP DELETE /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_add_selected_repo_to_org_secret(
        org: str,
        secret_name: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id} - Add selected repository to an organization secret"""
        # HTTP PUT /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_org_variable(
        org: str,
        name: str,
        value: str,
        visibility: str,
        selected_repository_ids: list | None = None,
    ) -> str:
        """POST /orgs/{org}/actions/variables - Create an organization variable"""
        # HTTP POST /orgs/{org}/actions/variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if value is not None:
            query_params["value"] = str(value)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/actions/variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_org_variables(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/variables - List organization variables"""
        # HTTP GET /orgs/{org}/actions/variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_update_org_variable(
        org: str,
        name: str,
        value: str | None = None,
        visibility: str | None = None,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PATCH /orgs/{org}/actions/variables/{name} - Update an organization variable"""
        # HTTP PATCH /orgs/{org}/actions/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        if value is not None:
            query_params["value"] = str(value)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/actions/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_org_variable(
        org: str,
        name: str,
    ) -> str:
        """DELETE /orgs/{org}/actions/variables/{name} - Delete an organization variable"""
        # HTTP DELETE /orgs/{org}/actions/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_org_variable(
        org: str,
        name: str,
    ) -> str:
        """GET /orgs/{org}/actions/variables/{name} - Get an organization variable"""
        # HTTP GET /orgs/{org}/actions/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_selected_repos_for_org_variable(
        org: str,
        name: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/actions/variables/{name}/repositories - Set selected repositories for an organization variable"""
        # HTTP PUT /orgs/{org}/actions/variables/{name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/variables/{name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_selected_repos_for_org_variable(
        org: str,
        name: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/actions/variables/{name}/repositories - List selected repositories for an organization variable"""
        # HTTP GET /orgs/{org}/actions/variables/{name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/actions/variables/{name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_remove_selected_repo_from_org_variable(
        org: str,
        name: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/actions/variables/{name}/repositories/{repository_id} - Remove selected repository from an organization variable"""
        # HTTP DELETE /orgs/{org}/actions/variables/{name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/actions/variables/{name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_add_selected_repo_to_org_variable(
        org: str,
        name: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/actions/variables/{name}/repositories/{repository_id} - Add selected repository to an organization variable"""
        # HTTP PUT /orgs/{org}/actions/variables/{name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["name"] = str(name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/actions/variables/{name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_create_artifact_deployment_record(
        org: str,
        name: str,
        digest: str,
        status: str,
        logical_environment: str,
        deployment_name: str,
        version: str | None = None,
        physical_environment: str | None = None,
        cluster: str | None = None,
        tags: dict | None = None,
        runtime_risks: list | None = None,
        github_repository: str | None = None,
    ) -> str:
        """POST /orgs/{org}/artifacts/metadata/deployment-record - Create an artifact deployment record"""
        # HTTP POST /orgs/{org}/artifacts/metadata/deployment-record
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if digest is not None:
            query_params["digest"] = str(digest)
        if version is not None:
            query_params["version"] = str(version)
        if status is not None:
            query_params["status"] = str(status)
        if logical_environment is not None:
            query_params["logical_environment"] = str(logical_environment)
        if physical_environment is not None:
            query_params["physical_environment"] = str(physical_environment)
        if cluster is not None:
            query_params["cluster"] = str(cluster)
        if deployment_name is not None:
            query_params["deployment_name"] = str(deployment_name)
        body = tags if isinstance(tags, dict) else {"data": tags}
        if runtime_risks is not None:
            query_params["runtime_risks"] = str(runtime_risks)
        if github_repository is not None:
            query_params["github_repository"] = str(github_repository)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/artifacts/metadata/deployment-record",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_set_cluster_deployment_records(
        org: str,
        cluster: str,
        logical_environment: str,
        deployments: list,
        physical_environment: str | None = None,
    ) -> str:
        """POST /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster} - Set cluster deployment records"""
        # HTTP POST /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["cluster"] = str(cluster)
        if logical_environment is not None:
            query_params["logical_environment"] = str(logical_environment)
        if physical_environment is not None:
            query_params["physical_environment"] = str(physical_environment)
        if deployments is not None:
            query_params["deployments"] = str(deployments)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_create_artifact_storage_record(
        org: str,
        name: str,
        digest: str,
        registry_url: str,
        version: str | None = None,
        artifact_url: str | None = None,
        path: str | None = None,
        repository: str | None = None,
        status: str | None = None,
        github_repository: str | None = None,
    ) -> str:
        """POST /orgs/{org}/artifacts/metadata/storage-record - Create artifact metadata storage record"""
        # HTTP POST /orgs/{org}/artifacts/metadata/storage-record
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if digest is not None:
            query_params["digest"] = str(digest)
        if version is not None:
            query_params["version"] = str(version)
        if artifact_url is not None:
            query_params["artifact_url"] = str(artifact_url)
        if path is not None:
            query_params["path"] = str(path)
        if registry_url is not None:
            query_params["registry_url"] = str(registry_url)
        if repository is not None:
            query_params["repository"] = str(repository)
        if status is not None:
            query_params["status"] = str(status)
        if github_repository is not None:
            query_params["github_repository"] = str(github_repository)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/artifacts/metadata/storage-record",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_artifact_deployment_records(
        org: str,
        subject_digest: str,
    ) -> str:
        """GET /orgs/{org}/artifacts/{subject_digest}/metadata/deployment-records - List artifact deployment records"""
        # HTTP GET /orgs/{org}/artifacts/{subject_digest}/metadata/deployment-records
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["subject_digest"] = str(subject_digest)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/artifacts/{subject_digest}/metadata/deployment-records",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_artifact_storage_records(
        org: str,
        subject_digest: str,
    ) -> str:
        """GET /orgs/{org}/artifacts/{subject_digest}/metadata/storage-records - List artifact storage records"""
        # HTTP GET /orgs/{org}/artifacts/{subject_digest}/metadata/storage-records
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["subject_digest"] = str(subject_digest)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/artifacts/{subject_digest}/metadata/storage-records",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_attestations_bulk(
        org: str,
        subject_digests: list,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        predicate_type: str | None = None,
    ) -> str:
        """POST /orgs/{org}/attestations/bulk-list - List attestations by bulk subject digests"""
        # HTTP POST /orgs/{org}/attestations/bulk-list
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        path_params["org"] = str(org)
        if subject_digests is not None:
            query_params["subject_digests"] = str(subject_digests)
        if predicate_type is not None:
            query_params["predicate_type"] = str(predicate_type)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/attestations/bulk-list",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete_attestations_bulk(
        org: str,
        body: dict,
    ) -> str:
        """POST /orgs/{org}/attestations/delete-request - Delete attestations in bulk"""
        # HTTP POST /orgs/{org}/attestations/delete-request
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/attestations/delete-request",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete_attestations_by_subject_digest(
        org: str,
        subject_digest: str,
    ) -> str:
        """DELETE /orgs/{org}/attestations/digest/{subject_digest} - Delete attestations by subject digest"""
        # HTTP DELETE /orgs/{org}/attestations/digest/{subject_digest}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["subject_digest"] = str(subject_digest)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/attestations/digest/{subject_digest}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_attestation_repositories(
        org: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        predicate_type: str | None = None,
    ) -> str:
        """GET /orgs/{org}/attestations/repositories - List attestation repositories"""
        # HTTP GET /orgs/{org}/attestations/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        path_params["org"] = str(org)
        if predicate_type is not None:
            query_params["predicate_type"] = str(predicate_type)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/attestations/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete_attestations_by_id(
        org: str,
        attestation_id: int,
    ) -> str:
        """DELETE /orgs/{org}/attestations/{attestation_id} - Delete attestations by ID"""
        # HTTP DELETE /orgs/{org}/attestations/{attestation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["attestation_id"] = str(attestation_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/attestations/{attestation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_attestations(
        org: str,
        subject_digest: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        predicate_type: str | None = None,
    ) -> str:
        """GET /orgs/{org}/attestations/{subject_digest} - List attestations"""
        # HTTP GET /orgs/{org}/attestations/{subject_digest}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        path_params["org"] = str(org)
        path_params["subject_digest"] = str(subject_digest)
        if predicate_type is not None:
            query_params["predicate_type"] = str(predicate_type)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/attestations/{subject_digest}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_blocked_users(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/blocks - List users blocked by an organization"""
        # HTTP GET /orgs/{org}/blocks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/blocks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_unblock_user(
        org: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/blocks/{username} - Unblock a user from an organization"""
        # HTTP DELETE /orgs/{org}/blocks/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/blocks/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_block_user(
        org: str,
        username: str,
    ) -> str:
        """PUT /orgs/{org}/blocks/{username} - Block a user from an organization"""
        # HTTP PUT /orgs/{org}/blocks/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/blocks/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_check_blocked_user(
        org: str,
        username: str,
    ) -> str:
        """GET /orgs/{org}/blocks/{username} - Check if a user is blocked by an organization"""
        # HTTP GET /orgs/{org}/blocks/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/blocks/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def campaigns_create_campaign(
        org: str,
        name: str,
        description: str,
        ends_at: str,
        managers: list | None = None,
        team_managers: list | None = None,
        contact_link: str | None = None,
        code_scanning_alerts: list | None = None,
        generate_issues: bool | None = None,
    ) -> str:
        """POST /orgs/{org}/campaigns - Create a campaign for an organization"""
        # HTTP POST /orgs/{org}/campaigns
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if managers is not None:
            query_params["managers"] = str(managers)
        if team_managers is not None:
            query_params["team_managers"] = str(team_managers)
        if ends_at is not None:
            query_params["ends_at"] = str(ends_at)
        if contact_link is not None:
            query_params["contact_link"] = str(contact_link)
        if code_scanning_alerts is not None:
            query_params["code_scanning_alerts"] = str(code_scanning_alerts)
        if generate_issues is not None:
            query_params["generate_issues"] = str(generate_issues)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/campaigns",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def campaigns_list_org_campaigns(
        org: str,
        page: int | None = None,
        per_page: int | None = None,
        direction: str | None = None,
        state: str | None = None,
        sort: str | None = None,
    ) -> str:
        """GET /orgs/{org}/campaigns - List campaigns for an organization"""
        # HTTP GET /orgs/{org}/campaigns
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if direction is not None:
            query_params["direction"] = str(direction)
        if state is not None:
            query_params["state"] = str(state)
        if sort is not None:
            query_params["sort"] = str(sort)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/campaigns",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def campaigns_update_campaign(
        org: str,
        campaign_number: int,
        name: str | None = None,
        description: str | None = None,
        managers: list | None = None,
        team_managers: list | None = None,
        ends_at: str | None = None,
        contact_link: str | None = None,
        state: str | None = None,
    ) -> str:
        """PATCH /orgs/{org}/campaigns/{campaign_number} - Update a campaign"""
        # HTTP PATCH /orgs/{org}/campaigns/{campaign_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["campaign_number"] = str(campaign_number)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if managers is not None:
            query_params["managers"] = str(managers)
        if team_managers is not None:
            query_params["team_managers"] = str(team_managers)
        if ends_at is not None:
            query_params["ends_at"] = str(ends_at)
        if contact_link is not None:
            query_params["contact_link"] = str(contact_link)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/campaigns/{campaign_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def campaigns_delete_campaign(
        org: str,
        campaign_number: int,
    ) -> str:
        """DELETE /orgs/{org}/campaigns/{campaign_number} - Delete a campaign for an organization"""
        # HTTP DELETE /orgs/{org}/campaigns/{campaign_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["campaign_number"] = str(campaign_number)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/campaigns/{campaign_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def campaigns_get_campaign_summary(
        org: str,
        campaign_number: int,
    ) -> str:
        """GET /orgs/{org}/campaigns/{campaign_number} - Get a campaign for an organization"""
        # HTTP GET /orgs/{org}/campaigns/{campaign_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["campaign_number"] = str(campaign_number)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/campaigns/{campaign_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_list_alerts_for_org(
        org: str,
        tool_name: str | None = None,
        tool_guid: str | None = None,
        before: str | None = None,
        after: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        direction: str | None = None,
        state: str | None = None,
        sort: str | None = None,
        severity: str | None = None,
        assignees: str | None = None,
    ) -> str:
        """GET /orgs/{org}/code-scanning/alerts - List code scanning alerts for an organization"""
        # HTTP GET /orgs/{org}/code-scanning/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if tool_name is not None:
            query_params["tool_name"] = str(tool_name)
        if tool_guid is not None:
            query_params["tool_guid"] = str(tool_guid)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if direction is not None:
            query_params["direction"] = str(direction)
        if state is not None:
            query_params["state"] = str(state)
        if sort is not None:
            query_params["sort"] = str(sort)
        if severity is not None:
            query_params["severity"] = str(severity)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/code-scanning/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_create_configuration(
        org: str,
        name: str,
        description: str,
        advanced_security: str | None = None,
        code_security: str | None = None,
        dependency_graph: str | None = None,
        dependency_graph_autosubmit_action: str | None = None,
        dependency_graph_autosubmit_action_options: dict | None = None,
        dependabot_alerts: str | None = None,
        dependabot_security_updates: str | None = None,
        dependabot_delegated_alert_dismissal: str | None = None,
        code_scanning_options: dict | None = None,
        code_scanning_default_setup: str | None = None,
        code_scanning_default_setup_options: dict | None = None,
        code_scanning_delegated_alert_dismissal: str | None = None,
        secret_protection: str | None = None,
        secret_scanning: str | None = None,
        secret_scanning_push_protection: str | None = None,
        secret_scanning_delegated_bypass: str | None = None,
        secret_scanning_delegated_bypass_options: dict | None = None,
        secret_scanning_validity_checks: str | None = None,
        secret_scanning_non_provider_patterns: str | None = None,
        secret_scanning_generic_secrets: str | None = None,
        secret_scanning_delegated_alert_dismissal: str | None = None,
        secret_scanning_extended_metadata: str | None = None,
        private_vulnerability_reporting: str | None = None,
        enforcement: str | None = None,
    ) -> str:
        """POST /orgs/{org}/code-security/configurations - Create a code security configuration"""
        # HTTP POST /orgs/{org}/code-security/configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if advanced_security is not None:
            query_params["advanced_security"] = str(advanced_security)
        if code_security is not None:
            query_params["code_security"] = str(code_security)
        if dependency_graph is not None:
            query_params["dependency_graph"] = str(dependency_graph)
        if dependency_graph_autosubmit_action is not None:
            query_params["dependency_graph_autosubmit_action"] = str(dependency_graph_autosubmit_action)
        body = dependency_graph_autosubmit_action_options if isinstance(dependency_graph_autosubmit_action_options, dict) else {"data": dependency_graph_autosubmit_action_options}
        if dependabot_alerts is not None:
            query_params["dependabot_alerts"] = str(dependabot_alerts)
        if dependabot_security_updates is not None:
            query_params["dependabot_security_updates"] = str(dependabot_security_updates)
        if dependabot_delegated_alert_dismissal is not None:
            query_params["dependabot_delegated_alert_dismissal"] = str(dependabot_delegated_alert_dismissal)
        body = code_scanning_options if isinstance(code_scanning_options, dict) else {"data": code_scanning_options}
        if code_scanning_default_setup is not None:
            query_params["code_scanning_default_setup"] = str(code_scanning_default_setup)
        body = code_scanning_default_setup_options if isinstance(code_scanning_default_setup_options, dict) else {"data": code_scanning_default_setup_options}
        if code_scanning_delegated_alert_dismissal is not None:
            query_params["code_scanning_delegated_alert_dismissal"] = str(code_scanning_delegated_alert_dismissal)
        if secret_protection is not None:
            query_params["secret_protection"] = str(secret_protection)
        if secret_scanning is not None:
            query_params["secret_scanning"] = str(secret_scanning)
        if secret_scanning_push_protection is not None:
            query_params["secret_scanning_push_protection"] = str(secret_scanning_push_protection)
        if secret_scanning_delegated_bypass is not None:
            query_params["secret_scanning_delegated_bypass"] = str(secret_scanning_delegated_bypass)
        body = secret_scanning_delegated_bypass_options if isinstance(secret_scanning_delegated_bypass_options, dict) else {"data": secret_scanning_delegated_bypass_options}
        if secret_scanning_validity_checks is not None:
            query_params["secret_scanning_validity_checks"] = str(secret_scanning_validity_checks)
        if secret_scanning_non_provider_patterns is not None:
            query_params["secret_scanning_non_provider_patterns"] = str(secret_scanning_non_provider_patterns)
        if secret_scanning_generic_secrets is not None:
            query_params["secret_scanning_generic_secrets"] = str(secret_scanning_generic_secrets)
        if secret_scanning_delegated_alert_dismissal is not None:
            query_params["secret_scanning_delegated_alert_dismissal"] = str(secret_scanning_delegated_alert_dismissal)
        if secret_scanning_extended_metadata is not None:
            query_params["secret_scanning_extended_metadata"] = str(secret_scanning_extended_metadata)
        if private_vulnerability_reporting is not None:
            query_params["private_vulnerability_reporting"] = str(private_vulnerability_reporting)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/code-security/configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_get_configurations_for_org(
        org: str,
        target_type: str | None = None,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
    ) -> str:
        """GET /orgs/{org}/code-security/configurations - Get code security configurations for an organization"""
        # HTTP GET /orgs/{org}/code-security/configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if target_type is not None:
            query_params["target_type"] = str(target_type)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/code-security/configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_get_default_configurations(
        org: str,
    ) -> str:
        """GET /orgs/{org}/code-security/configurations/defaults - Get default code security configurations"""
        # HTTP GET /orgs/{org}/code-security/configurations/defaults
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/code-security/configurations/defaults",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_detach_configuration(
        org: str,
        selected_repository_ids: list | None = None,
    ) -> str:
        """DELETE /orgs/{org}/code-security/configurations/detach - Detach configurations from repositories"""
        # HTTP DELETE /orgs/{org}/code-security/configurations/detach
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/code-security/configurations/detach",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_update_configuration(
        org: str,
        configuration_id: int,
        name: str | None = None,
        description: str | None = None,
        advanced_security: str | None = None,
        code_security: str | None = None,
        dependency_graph: str | None = None,
        dependency_graph_autosubmit_action: str | None = None,
        dependency_graph_autosubmit_action_options: dict | None = None,
        dependabot_alerts: str | None = None,
        dependabot_security_updates: str | None = None,
        dependabot_delegated_alert_dismissal: str | None = None,
        code_scanning_default_setup: str | None = None,
        code_scanning_default_setup_options: dict | None = None,
        code_scanning_options: dict | None = None,
        code_scanning_delegated_alert_dismissal: str | None = None,
        secret_protection: str | None = None,
        secret_scanning: str | None = None,
        secret_scanning_push_protection: str | None = None,
        secret_scanning_delegated_bypass: str | None = None,
        secret_scanning_delegated_bypass_options: dict | None = None,
        secret_scanning_validity_checks: str | None = None,
        secret_scanning_non_provider_patterns: str | None = None,
        secret_scanning_generic_secrets: str | None = None,
        secret_scanning_delegated_alert_dismissal: str | None = None,
        secret_scanning_extended_metadata: str | None = None,
        private_vulnerability_reporting: str | None = None,
        enforcement: str | None = None,
    ) -> str:
        """PATCH /orgs/{org}/code-security/configurations/{configuration_id} - Update a code security configuration"""
        # HTTP PATCH /orgs/{org}/code-security/configurations/{configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["configuration_id"] = str(configuration_id)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if advanced_security is not None:
            query_params["advanced_security"] = str(advanced_security)
        if code_security is not None:
            query_params["code_security"] = str(code_security)
        if dependency_graph is not None:
            query_params["dependency_graph"] = str(dependency_graph)
        if dependency_graph_autosubmit_action is not None:
            query_params["dependency_graph_autosubmit_action"] = str(dependency_graph_autosubmit_action)
        body = dependency_graph_autosubmit_action_options if isinstance(dependency_graph_autosubmit_action_options, dict) else {"data": dependency_graph_autosubmit_action_options}
        if dependabot_alerts is not None:
            query_params["dependabot_alerts"] = str(dependabot_alerts)
        if dependabot_security_updates is not None:
            query_params["dependabot_security_updates"] = str(dependabot_security_updates)
        if dependabot_delegated_alert_dismissal is not None:
            query_params["dependabot_delegated_alert_dismissal"] = str(dependabot_delegated_alert_dismissal)
        if code_scanning_default_setup is not None:
            query_params["code_scanning_default_setup"] = str(code_scanning_default_setup)
        body = code_scanning_default_setup_options if isinstance(code_scanning_default_setup_options, dict) else {"data": code_scanning_default_setup_options}
        body = code_scanning_options if isinstance(code_scanning_options, dict) else {"data": code_scanning_options}
        if code_scanning_delegated_alert_dismissal is not None:
            query_params["code_scanning_delegated_alert_dismissal"] = str(code_scanning_delegated_alert_dismissal)
        if secret_protection is not None:
            query_params["secret_protection"] = str(secret_protection)
        if secret_scanning is not None:
            query_params["secret_scanning"] = str(secret_scanning)
        if secret_scanning_push_protection is not None:
            query_params["secret_scanning_push_protection"] = str(secret_scanning_push_protection)
        if secret_scanning_delegated_bypass is not None:
            query_params["secret_scanning_delegated_bypass"] = str(secret_scanning_delegated_bypass)
        body = secret_scanning_delegated_bypass_options if isinstance(secret_scanning_delegated_bypass_options, dict) else {"data": secret_scanning_delegated_bypass_options}
        if secret_scanning_validity_checks is not None:
            query_params["secret_scanning_validity_checks"] = str(secret_scanning_validity_checks)
        if secret_scanning_non_provider_patterns is not None:
            query_params["secret_scanning_non_provider_patterns"] = str(secret_scanning_non_provider_patterns)
        if secret_scanning_generic_secrets is not None:
            query_params["secret_scanning_generic_secrets"] = str(secret_scanning_generic_secrets)
        if secret_scanning_delegated_alert_dismissal is not None:
            query_params["secret_scanning_delegated_alert_dismissal"] = str(secret_scanning_delegated_alert_dismissal)
        if secret_scanning_extended_metadata is not None:
            query_params["secret_scanning_extended_metadata"] = str(secret_scanning_extended_metadata)
        if private_vulnerability_reporting is not None:
            query_params["private_vulnerability_reporting"] = str(private_vulnerability_reporting)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/code-security/configurations/{configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_delete_configuration(
        org: str,
        configuration_id: int,
    ) -> str:
        """DELETE /orgs/{org}/code-security/configurations/{configuration_id} - Delete a code security configuration"""
        # HTTP DELETE /orgs/{org}/code-security/configurations/{configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["configuration_id"] = str(configuration_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/code-security/configurations/{configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_get_configuration(
        org: str,
        configuration_id: int,
    ) -> str:
        """GET /orgs/{org}/code-security/configurations/{configuration_id} - Get a code security configuration"""
        # HTTP GET /orgs/{org}/code-security/configurations/{configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["configuration_id"] = str(configuration_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/code-security/configurations/{configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_attach_configuration(
        org: str,
        configuration_id: int,
        scope: str,
        selected_repository_ids: list | None = None,
    ) -> str:
        """POST /orgs/{org}/code-security/configurations/{configuration_id}/attach - Attach a configuration to repositories"""
        # HTTP POST /orgs/{org}/code-security/configurations/{configuration_id}/attach
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["configuration_id"] = str(configuration_id)
        if scope is not None:
            query_params["scope"] = str(scope)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/code-security/configurations/{configuration_id}/attach",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_set_configuration_as_default(
        org: str,
        configuration_id: int,
        default_for_new_repos: str | None = None,
    ) -> str:
        """PUT /orgs/{org}/code-security/configurations/{configuration_id}/defaults - Set a code security configuration as a default for an organization"""
        # HTTP PUT /orgs/{org}/code-security/configurations/{configuration_id}/defaults
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["configuration_id"] = str(configuration_id)
        if default_for_new_repos is not None:
            query_params["default_for_new_repos"] = str(default_for_new_repos)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/code-security/configurations/{configuration_id}/defaults",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_get_repositories_for_configuration(
        org: str,
        configuration_id: int,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        status: str | None = None,
    ) -> str:
        """GET /orgs/{org}/code-security/configurations/{configuration_id}/repositories - Get repositories associated with a code security configuration"""
        # HTTP GET /orgs/{org}/code-security/configurations/{configuration_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["configuration_id"] = str(configuration_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if status is not None:
            query_params["status"] = str(status)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/code-security/configurations/{configuration_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_list_in_organization(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/codespaces - List codespaces for the organization"""
        # HTTP GET /orgs/{org}/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_set_codespaces_access(
        org: str,
        visibility: str,
        selected_usernames: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/codespaces/access - Manage access control for organization codespaces"""
        # HTTP PUT /orgs/{org}/codespaces/access
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_usernames is not None:
            query_params["selected_usernames"] = str(selected_usernames)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/codespaces/access",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_set_codespaces_access_users(
        org: str,
        selected_usernames: list,
    ) -> str:
        """POST /orgs/{org}/codespaces/access/selected_users - Add users to Codespaces access for an organization"""
        # HTTP POST /orgs/{org}/codespaces/access/selected_users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_usernames is not None:
            query_params["selected_usernames"] = str(selected_usernames)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/codespaces/access/selected_users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_delete_codespaces_access_users(
        org: str,
        selected_usernames: list,
    ) -> str:
        """DELETE /orgs/{org}/codespaces/access/selected_users - Remove users from Codespaces access for an organization"""
        # HTTP DELETE /orgs/{org}/codespaces/access/selected_users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_usernames is not None:
            query_params["selected_usernames"] = str(selected_usernames)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/codespaces/access/selected_users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_list_org_secrets(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/codespaces/secrets - List organization secrets"""
        # HTTP GET /orgs/{org}/codespaces/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/codespaces/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_org_public_key(
        org: str,
    ) -> str:
        """GET /orgs/{org}/codespaces/secrets/public-key - Get an organization public key"""
        # HTTP GET /orgs/{org}/codespaces/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/codespaces/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_delete_org_secret(
        org: str,
        secret_name: str,
    ) -> str:
        """DELETE /orgs/{org}/codespaces/secrets/{secret_name} - Delete an organization secret"""
        # HTTP DELETE /orgs/{org}/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_create_or_update_org_secret(
        org: str,
        secret_name: str,
        visibility: str,
        encrypted_value: str | None = None,
        key_id: str | None = None,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/codespaces/secrets/{secret_name} - Create or update an organization secret"""
        # HTTP PUT /orgs/{org}/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_org_secret(
        org: str,
        secret_name: str,
    ) -> str:
        """GET /orgs/{org}/codespaces/secrets/{secret_name} - Get an organization secret"""
        # HTTP GET /orgs/{org}/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_set_selected_repos_for_org_secret(
        org: str,
        secret_name: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories - Set selected repositories for an organization secret"""
        # HTTP PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/codespaces/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_list_selected_repos_for_org_secret(
        org: str,
        secret_name: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/codespaces/secrets/{secret_name}/repositories - List selected repositories for an organization secret"""
        # HTTP GET /orgs/{org}/codespaces/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/codespaces/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_remove_selected_repo_from_org_secret(
        org: str,
        secret_name: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id} - Remove selected repository from an organization secret"""
        # HTTP DELETE /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_add_selected_repo_to_org_secret(
        org: str,
        secret_name: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id} - Add selected repository to an organization secret"""
        # HTTP PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_get_copilot_organization_details(
        org: str,
    ) -> str:
        """GET /orgs/{org}/copilot/billing - Get Copilot seat information and settings for an organization"""
        # HTTP GET /orgs/{org}/copilot/billing
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/copilot/billing",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_list_copilot_seats(
        org: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/copilot/billing/seats - List all Copilot seat assignments for an organization"""
        # HTTP GET /orgs/{org}/copilot/billing/seats
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/copilot/billing/seats",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_add_copilot_seats_for_teams(
        org: str,
        selected_teams: list,
    ) -> str:
        """POST /orgs/{org}/copilot/billing/selected_teams - Add teams to the Copilot subscription for an organization"""
        # HTTP POST /orgs/{org}/copilot/billing/selected_teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_teams is not None:
            query_params["selected_teams"] = str(selected_teams)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/copilot/billing/selected_teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_cancel_copilot_seat_assignment_for_teams(
        org: str,
        selected_teams: list,
    ) -> str:
        """DELETE /orgs/{org}/copilot/billing/selected_teams - Remove teams from the Copilot subscription for an organization"""
        # HTTP DELETE /orgs/{org}/copilot/billing/selected_teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_teams is not None:
            query_params["selected_teams"] = str(selected_teams)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/copilot/billing/selected_teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_add_copilot_seats_for_users(
        org: str,
        selected_usernames: list,
    ) -> str:
        """POST /orgs/{org}/copilot/billing/selected_users - Add users to the Copilot subscription for an organization"""
        # HTTP POST /orgs/{org}/copilot/billing/selected_users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_usernames is not None:
            query_params["selected_usernames"] = str(selected_usernames)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/copilot/billing/selected_users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_cancel_copilot_seat_assignment_for_users(
        org: str,
        selected_usernames: list,
    ) -> str:
        """DELETE /orgs/{org}/copilot/billing/selected_users - Remove users from the Copilot subscription for an organization"""
        # HTTP DELETE /orgs/{org}/copilot/billing/selected_users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_usernames is not None:
            query_params["selected_usernames"] = str(selected_usernames)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/copilot/billing/selected_users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_copilot_content_exclusion(
        org: str,
        body: dict,
    ) -> str:
        """PUT /orgs/{org}/copilot/content_exclusion - Set Copilot content exclusion rules for an organization"""
        # HTTP PUT /orgs/{org}/copilot/content_exclusion
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/copilot/content_exclusion",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_copilot_content_exclusion(
        org: str,
    ) -> str:
        """GET /orgs/{org}/copilot/content_exclusion - Get Copilot content exclusion rules for an organization"""
        # HTTP GET /orgs/{org}/copilot/content_exclusion
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/copilot/content_exclusion",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_copilot_metrics_for_organization(
        org: str,
        since: str | None = None,
        until: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/copilot/metrics - Get Copilot metrics for an organization"""
        # HTTP GET /orgs/{org}/copilot/metrics
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if since is not None:
            query_params["since"] = str(since)
        if until is not None:
            query_params["until"] = str(until)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/copilot/metrics",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_list_alerts_for_org(
        org: str,
        state: str | None = None,
        severity: str | None = None,
        ecosystem: str | None = None,
        package: str | None = None,
        epss_percentage: str | None = None,
        artifact_registry_url: str | None = None,
        artifact_registry: str | None = None,
        has: dict | None = None,
        assignee: str | None = None,
        runtime_risk: str | None = None,
        scope: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/dependabot/alerts - List Dependabot alerts for an organization"""
        # HTTP GET /orgs/{org}/dependabot/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if state is not None:
            query_params["state"] = str(state)
        if severity is not None:
            query_params["severity"] = str(severity)
        if ecosystem is not None:
            query_params["ecosystem"] = str(ecosystem)
        if package is not None:
            query_params["package"] = str(package)
        if epss_percentage is not None:
            query_params["epss_percentage"] = str(epss_percentage)
        if artifact_registry_url is not None:
            query_params["artifact_registry_url"] = str(artifact_registry_url)
        if artifact_registry is not None:
            query_params["artifact_registry"] = str(artifact_registry)
        body = has if isinstance(has, dict) else {"data": has}
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if runtime_risk is not None:
            query_params["runtime_risk"] = str(runtime_risk)
        if scope is not None:
            query_params["scope"] = str(scope)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/dependabot/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_list_org_secrets(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/dependabot/secrets - List organization secrets"""
        # HTTP GET /orgs/{org}/dependabot/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/dependabot/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_get_org_public_key(
        org: str,
    ) -> str:
        """GET /orgs/{org}/dependabot/secrets/public-key - Get an organization public key"""
        # HTTP GET /orgs/{org}/dependabot/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/dependabot/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_delete_org_secret(
        org: str,
        secret_name: str,
    ) -> str:
        """DELETE /orgs/{org}/dependabot/secrets/{secret_name} - Delete an organization secret"""
        # HTTP DELETE /orgs/{org}/dependabot/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/dependabot/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_create_or_update_org_secret(
        org: str,
        secret_name: str,
        visibility: str,
        encrypted_value: str | None = None,
        key_id: str | None = None,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/dependabot/secrets/{secret_name} - Create or update an organization secret"""
        # HTTP PUT /orgs/{org}/dependabot/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/dependabot/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_get_org_secret(
        org: str,
        secret_name: str,
    ) -> str:
        """GET /orgs/{org}/dependabot/secrets/{secret_name} - Get an organization secret"""
        # HTTP GET /orgs/{org}/dependabot/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/dependabot/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_set_selected_repos_for_org_secret(
        org: str,
        secret_name: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories - Set selected repositories for an organization secret"""
        # HTTP PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/dependabot/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_list_selected_repos_for_org_secret(
        org: str,
        secret_name: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/dependabot/secrets/{secret_name}/repositories - List selected repositories for an organization secret"""
        # HTTP GET /orgs/{org}/dependabot/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/dependabot/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_remove_selected_repo_from_org_secret(
        org: str,
        secret_name: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id} - Remove selected repository from an organization secret"""
        # HTTP DELETE /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_add_selected_repo_to_org_secret(
        org: str,
        secret_name: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id} - Add selected repository to an organization secret"""
        # HTTP PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_docker_conflicts(
        org: str,
    ) -> str:
        """GET /orgs/{org}/docker/conflicts - Get list of conflicting packages during Docker migration for organization"""
        # HTTP GET /orgs/{org}/docker/conflicts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/docker/conflicts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_public_org_events(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/events - List public organization events"""
        # HTTP GET /orgs/{org}/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_failed_invitations(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/failed_invitations - List failed organization invitations"""
        # HTTP GET /orgs/{org}/failed_invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/failed_invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_create_webhook(
        org: str,
        name: str,
        config: dict,
        events: list | None = None,
        active: bool | None = None,
    ) -> str:
        """POST /orgs/{org}/hooks - Create an organization webhook"""
        # HTTP POST /orgs/{org}/hooks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        body = config if isinstance(config, dict) else {"data": config}
        if events is not None:
            query_params["events"] = str(events)
        if active is not None:
            query_params["active"] = str(active)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/hooks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_webhooks(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/hooks - List organization webhooks"""
        # HTTP GET /orgs/{org}/hooks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/hooks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_webhook(
        org: str,
        hook_id: int,
        config: dict | None = None,
        events: list | None = None,
        active: bool | None = None,
        name: str | None = None,
    ) -> str:
        """PATCH /orgs/{org}/hooks/{hook_id} - Update an organization webhook"""
        # HTTP PATCH /orgs/{org}/hooks/{hook_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        body = config if isinstance(config, dict) else {"data": config}
        if events is not None:
            query_params["events"] = str(events)
        if active is not None:
            query_params["active"] = str(active)
        if name is not None:
            query_params["name"] = str(name)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/hooks/{hook_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete_webhook(
        org: str,
        hook_id: int,
    ) -> str:
        """DELETE /orgs/{org}/hooks/{hook_id} - Delete an organization webhook"""
        # HTTP DELETE /orgs/{org}/hooks/{hook_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/hooks/{hook_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_webhook(
        org: str,
        hook_id: int,
    ) -> str:
        """GET /orgs/{org}/hooks/{hook_id} - Get an organization webhook"""
        # HTTP GET /orgs/{org}/hooks/{hook_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/hooks/{hook_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_webhook_config_for_org(
        org: str,
        hook_id: int,
        url: str | None = None,
        content_type: str | None = None,
        secret: str | None = None,
        insecure_ssl: dict | None = None,
    ) -> str:
        """PATCH /orgs/{org}/hooks/{hook_id}/config - Update a webhook configuration for an organization"""
        # HTTP PATCH /orgs/{org}/hooks/{hook_id}/config
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        if url is not None:
            query_params["url"] = str(url)
        if content_type is not None:
            query_params["content_type"] = str(content_type)
        if secret is not None:
            query_params["secret"] = str(secret)
        body = insecure_ssl if isinstance(insecure_ssl, dict) else {"data": insecure_ssl}
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/hooks/{hook_id}/config",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_webhook_config_for_org(
        org: str,
        hook_id: int,
    ) -> str:
        """GET /orgs/{org}/hooks/{hook_id}/config - Get a webhook configuration for an organization"""
        # HTTP GET /orgs/{org}/hooks/{hook_id}/config
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/hooks/{hook_id}/config",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_webhook_deliveries(
        org: str,
        hook_id: int,
        per_page: int | None = None,
        cursor: str | None = None,
    ) -> str:
        """GET /orgs/{org}/hooks/{hook_id}/deliveries - List deliveries for an organization webhook"""
        # HTTP GET /orgs/{org}/hooks/{hook_id}/deliveries
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if cursor is not None:
            query_params["cursor"] = str(cursor)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/hooks/{hook_id}/deliveries",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_webhook_delivery(
        org: str,
        hook_id: int,
        delivery_id: int,
    ) -> str:
        """GET /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id} - Get a webhook delivery for an organization webhook"""
        # HTTP GET /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        path_params["delivery_id"] = str(delivery_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_redeliver_webhook_delivery(
        org: str,
        hook_id: int,
        delivery_id: int,
    ) -> str:
        """POST /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts - Redeliver a delivery for an organization webhook"""
        # HTTP POST /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        path_params["delivery_id"] = str(delivery_id)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_ping_webhook(
        org: str,
        hook_id: int,
    ) -> str:
        """POST /orgs/{org}/hooks/{hook_id}/pings - Ping an organization webhook"""
        # HTTP POST /orgs/{org}/hooks/{hook_id}/pings
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/hooks/{hook_id}/pings",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_route_stats_by_actor(
        org: str,
        actor_type: str,
        actor_id: int,
        min_timestamp: str,
        max_timestamp: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        direction: str | None = None,
        sort: list | None = None,
        api_route_substring: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/route-stats/{actor_type}/{actor_id} - Get route stats by actor"""
        # HTTP GET /orgs/{org}/insights/api/route-stats/{actor_type}/{actor_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["actor_type"] = str(actor_type)
        path_params["actor_id"] = str(actor_id)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if direction is not None:
            query_params["direction"] = str(direction)
        if sort is not None:
            query_params["sort"] = str(sort)
        if api_route_substring is not None:
            query_params["api_route_substring"] = str(api_route_substring)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/route-stats/{actor_type}/{actor_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_subject_stats(
        org: str,
        min_timestamp: str,
        max_timestamp: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        direction: str | None = None,
        sort: list | None = None,
        subject_name_substring: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/subject-stats - Get subject stats"""
        # HTTP GET /orgs/{org}/insights/api/subject-stats
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if direction is not None:
            query_params["direction"] = str(direction)
        if sort is not None:
            query_params["sort"] = str(sort)
        if subject_name_substring is not None:
            query_params["subject_name_substring"] = str(subject_name_substring)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/subject-stats",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_summary_stats(
        org: str,
        min_timestamp: str,
        max_timestamp: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/summary-stats - Get summary stats"""
        # HTTP GET /orgs/{org}/insights/api/summary-stats
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/summary-stats",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_summary_stats_by_user(
        org: str,
        user_id: str,
        min_timestamp: str,
        max_timestamp: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/summary-stats/users/{user_id} - Get summary stats by user"""
        # HTTP GET /orgs/{org}/insights/api/summary-stats/users/{user_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["user_id"] = str(user_id)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/summary-stats/users/{user_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_summary_stats_by_actor(
        org: str,
        min_timestamp: str,
        actor_type: str,
        actor_id: int,
        max_timestamp: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/summary-stats/{actor_type}/{actor_id} - Get summary stats by actor"""
        # HTTP GET /orgs/{org}/insights/api/summary-stats/{actor_type}/{actor_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        path_params["actor_type"] = str(actor_type)
        path_params["actor_id"] = str(actor_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/summary-stats/{actor_type}/{actor_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_time_stats(
        org: str,
        min_timestamp: str,
        timestamp_increment: str,
        max_timestamp: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/time-stats - Get time stats"""
        # HTTP GET /orgs/{org}/insights/api/time-stats
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        if timestamp_increment is not None:
            query_params["timestamp_increment"] = str(timestamp_increment)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/time-stats",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_time_stats_by_user(
        org: str,
        user_id: str,
        min_timestamp: str,
        timestamp_increment: str,
        max_timestamp: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/time-stats/users/{user_id} - Get time stats by user"""
        # HTTP GET /orgs/{org}/insights/api/time-stats/users/{user_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["user_id"] = str(user_id)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        if timestamp_increment is not None:
            query_params["timestamp_increment"] = str(timestamp_increment)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/time-stats/users/{user_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_time_stats_by_actor(
        org: str,
        actor_type: str,
        actor_id: int,
        min_timestamp: str,
        timestamp_increment: str,
        max_timestamp: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/time-stats/{actor_type}/{actor_id} - Get time stats by actor"""
        # HTTP GET /orgs/{org}/insights/api/time-stats/{actor_type}/{actor_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["actor_type"] = str(actor_type)
        path_params["actor_id"] = str(actor_id)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        if timestamp_increment is not None:
            query_params["timestamp_increment"] = str(timestamp_increment)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/time-stats/{actor_type}/{actor_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def api_insights_get_user_stats(
        org: str,
        user_id: str,
        min_timestamp: str,
        max_timestamp: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        direction: str | None = None,
        sort: list | None = None,
        actor_name_substring: str | None = None,
    ) -> str:
        """GET /orgs/{org}/insights/api/user-stats/{user_id} - Get user stats"""
        # HTTP GET /orgs/{org}/insights/api/user-stats/{user_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["user_id"] = str(user_id)
        if min_timestamp is not None:
            query_params["min_timestamp"] = str(min_timestamp)
        if max_timestamp is not None:
            query_params["max_timestamp"] = str(max_timestamp)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if direction is not None:
            query_params["direction"] = str(direction)
        if sort is not None:
            query_params["sort"] = str(sort)
        if actor_name_substring is not None:
            query_params["actor_name_substring"] = str(actor_name_substring)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/insights/api/user-stats/{user_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_org_installation(
        org: str,
    ) -> str:
        """GET /orgs/{org}/installation - Get an organization installation for the authenticated app"""
        # HTTP GET /orgs/{org}/installation
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/installation",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_app_installations(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/installations - List app installations for an organization"""
        # HTTP GET /orgs/{org}/installations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/installations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def interactions_remove_restrictions_for_org(
        org: str,
    ) -> str:
        """DELETE /orgs/{org}/interaction-limits - Remove interaction restrictions for an organization"""
        # HTTP DELETE /orgs/{org}/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def interactions_set_restrictions_for_org(
        org: str,
        limit: str,
        expiry: str | None = None,
    ) -> str:
        """PUT /orgs/{org}/interaction-limits - Set interaction restrictions for an organization"""
        # HTTP PUT /orgs/{org}/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if limit is not None:
            query_params["limit"] = str(limit)
        if expiry is not None:
            query_params["expiry"] = str(expiry)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def interactions_get_restrictions_for_org(
        org: str,
    ) -> str:
        """GET /orgs/{org}/interaction-limits - Get interaction restrictions for an organization"""
        # HTTP GET /orgs/{org}/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_create_invitation(
        org: str,
        invitee_id: int | None = None,
        email: str | None = None,
        role: str | None = None,
        team_ids: list | None = None,
    ) -> str:
        """POST /orgs/{org}/invitations - Create an organization invitation"""
        # HTTP POST /orgs/{org}/invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if invitee_id is not None:
            query_params["invitee_id"] = str(invitee_id)
        if email is not None:
            query_params["email"] = str(email)
        if role is not None:
            query_params["role"] = str(role)
        if team_ids is not None:
            query_params["team_ids"] = str(team_ids)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_pending_invitations(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        role: str | None = None,
        invitation_source: str | None = None,
    ) -> str:
        """GET /orgs/{org}/invitations - List pending organization invitations"""
        # HTTP GET /orgs/{org}/invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if role is not None:
            query_params["role"] = str(role)
        if invitation_source is not None:
            query_params["invitation_source"] = str(invitation_source)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_cancel_invitation(
        org: str,
        invitation_id: int,
    ) -> str:
        """DELETE /orgs/{org}/invitations/{invitation_id} - Cancel an organization invitation"""
        # HTTP DELETE /orgs/{org}/invitations/{invitation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["invitation_id"] = str(invitation_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/invitations/{invitation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_invitation_teams(
        org: str,
        invitation_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/invitations/{invitation_id}/teams - List organization invitation teams"""
        # HTTP GET /orgs/{org}/invitations/{invitation_id}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["invitation_id"] = str(invitation_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/invitations/{invitation_id}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_create_issue_field(
        org: str,
        name: str,
        data_type: str,
        description: str | None = None,
        visibility: str | None = None,
        options: list | None = None,
    ) -> str:
        """POST /orgs/{org}/issue-fields - Create issue field for an organization"""
        # HTTP POST /orgs/{org}/issue-fields
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if data_type is not None:
            query_params["data_type"] = str(data_type)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if options is not None:
            query_params["options"] = str(options)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/issue-fields",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_issue_fields(
        org: str,
    ) -> str:
        """GET /orgs/{org}/issue-fields - List issue fields for an organization"""
        # HTTP GET /orgs/{org}/issue-fields
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/issue-fields",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_issue_field(
        org: str,
        issue_field_id: int,
        name: str | None = None,
        description: str | None = None,
        visibility: str | None = None,
        options: list | None = None,
    ) -> str:
        """PATCH /orgs/{org}/issue-fields/{issue_field_id} - Update issue field for an organization"""
        # HTTP PATCH /orgs/{org}/issue-fields/{issue_field_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["issue_field_id"] = str(issue_field_id)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if options is not None:
            query_params["options"] = str(options)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/issue-fields/{issue_field_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete_issue_field(
        org: str,
        issue_field_id: int,
    ) -> str:
        """DELETE /orgs/{org}/issue-fields/{issue_field_id} - Delete issue field for an organization"""
        # HTTP DELETE /orgs/{org}/issue-fields/{issue_field_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["issue_field_id"] = str(issue_field_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/issue-fields/{issue_field_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_create_issue_type(
        org: str,
        name: str,
        is_enabled: bool,
        description: str | None = None,
        color: str | None = None,
    ) -> str:
        """POST /orgs/{org}/issue-types - Create issue type for an organization"""
        # HTTP POST /orgs/{org}/issue-types
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if is_enabled is not None:
            query_params["is_enabled"] = str(is_enabled)
        if description is not None:
            query_params["description"] = str(description)
        if color is not None:
            query_params["color"] = str(color)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/issue-types",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_issue_types(
        org: str,
    ) -> str:
        """GET /orgs/{org}/issue-types - List issue types for an organization"""
        # HTTP GET /orgs/{org}/issue-types
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/issue-types",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_delete_issue_type(
        org: str,
        issue_type_id: int,
    ) -> str:
        """DELETE /orgs/{org}/issue-types/{issue_type_id} - Delete issue type for an organization"""
        # HTTP DELETE /orgs/{org}/issue-types/{issue_type_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["issue_type_id"] = str(issue_type_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/issue-types/{issue_type_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_issue_type(
        org: str,
        issue_type_id: int,
        name: str,
        is_enabled: bool,
        description: str | None = None,
        color: str | None = None,
    ) -> str:
        """PUT /orgs/{org}/issue-types/{issue_type_id} - Update issue type for an organization"""
        # HTTP PUT /orgs/{org}/issue-types/{issue_type_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["issue_type_id"] = str(issue_type_id)
        if name is not None:
            query_params["name"] = str(name)
        if is_enabled is not None:
            query_params["is_enabled"] = str(is_enabled)
        if description is not None:
            query_params["description"] = str(description)
        if color is not None:
            query_params["color"] = str(color)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/issue-types/{issue_type_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_for_org(
        org: str,
        filter: str | None = None,
        state: str | None = None,
        labels: str | None = None,
        type: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/issues - List organization issues assigned to the authenticated user"""
        # HTTP GET /orgs/{org}/issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if filter is not None:
            query_params["filter"] = str(filter)
        if state is not None:
            query_params["state"] = str(state)
        if labels is not None:
            query_params["labels"] = str(labels)
        if type is not None:
            query_params["type"] = str(type)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_members(
        org: str,
        filter: str | None = None,
        role: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/members - List organization members"""
        # HTTP GET /orgs/{org}/members
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if filter is not None:
            query_params["filter"] = str(filter)
        if role is not None:
            query_params["role"] = str(role)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/members",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_remove_member(
        org: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/members/{username} - Remove an organization member"""
        # HTTP DELETE /orgs/{org}/members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_check_membership_for_user(
        org: str,
        username: str,
    ) -> str:
        """GET /orgs/{org}/members/{username} - Check organization membership for a user"""
        # HTTP GET /orgs/{org}/members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_codespaces_for_user_in_org(
        org: str,
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/members/{username}/codespaces - List codespaces for a user in organization"""
        # HTTP GET /orgs/{org}/members/{username}/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/members/{username}/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_delete_from_organization(
        org: str,
        username: str,
        codespace_name: str,
    ) -> str:
        """DELETE /orgs/{org}/members/{username}/codespaces/{codespace_name} - Delete a codespace from the organization"""
        # HTTP DELETE /orgs/{org}/members/{username}/codespaces/{codespace_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/members/{username}/codespaces/{codespace_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_stop_in_organization(
        org: str,
        username: str,
        codespace_name: str,
    ) -> str:
        """POST /orgs/{org}/members/{username}/codespaces/{codespace_name}/stop - Stop a codespace for an organization user"""
        # HTTP POST /orgs/{org}/members/{username}/codespaces/{codespace_name}/stop
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/members/{username}/codespaces/{codespace_name}/stop",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_get_copilot_seat_details_for_user(
        org: str,
        username: str,
    ) -> str:
        """GET /orgs/{org}/members/{username}/copilot - Get Copilot seat assignment details for a user"""
        # HTTP GET /orgs/{org}/members/{username}/copilot
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/members/{username}/copilot",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_remove_membership_for_user(
        org: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/memberships/{username} - Remove organization membership for a user"""
        # HTTP DELETE /orgs/{org}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_set_membership_for_user(
        org: str,
        username: str,
        role: str | None = None,
    ) -> str:
        """PUT /orgs/{org}/memberships/{username} - Set organization membership for a user"""
        # HTTP PUT /orgs/{org}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        if role is not None:
            query_params["role"] = str(role)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_membership_for_user(
        org: str,
        username: str,
    ) -> str:
        """GET /orgs/{org}/memberships/{username} - Get organization membership for a user"""
        # HTTP GET /orgs/{org}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_start_for_org(
        org: str,
        repositories: list,
        lock_repositories: bool | None = None,
        exclude_metadata: bool | None = None,
        exclude_git_data: bool | None = None,
        exclude_attachments: bool | None = None,
        exclude_releases: bool | None = None,
        exclude_owner_projects: bool | None = None,
        org_metadata_only: bool | None = None,
        exclude: list | None = None,
    ) -> str:
        """POST /orgs/{org}/migrations - Start an organization migration"""
        # HTTP POST /orgs/{org}/migrations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if repositories is not None:
            query_params["repositories"] = str(repositories)
        if lock_repositories is not None:
            query_params["lock_repositories"] = str(lock_repositories)
        if exclude_metadata is not None:
            query_params["exclude_metadata"] = str(exclude_metadata)
        if exclude_git_data is not None:
            query_params["exclude_git_data"] = str(exclude_git_data)
        if exclude_attachments is not None:
            query_params["exclude_attachments"] = str(exclude_attachments)
        if exclude_releases is not None:
            query_params["exclude_releases"] = str(exclude_releases)
        if exclude_owner_projects is not None:
            query_params["exclude_owner_projects"] = str(exclude_owner_projects)
        if org_metadata_only is not None:
            query_params["org_metadata_only"] = str(org_metadata_only)
        if exclude is not None:
            query_params["exclude"] = str(exclude)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/migrations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_list_for_org(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        exclude: list | None = None,
    ) -> str:
        """GET /orgs/{org}/migrations - List organization migrations"""
        # HTTP GET /orgs/{org}/migrations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if exclude is not None:
            query_params["exclude"] = str(exclude)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/migrations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_get_status_for_org(
        org: str,
        migration_id: int,
        exclude: list | None = None,
    ) -> str:
        """GET /orgs/{org}/migrations/{migration_id} - Get an organization migration status"""
        # HTTP GET /orgs/{org}/migrations/{migration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["migration_id"] = str(migration_id)
        if exclude is not None:
            query_params["exclude"] = str(exclude)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/migrations/{migration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_delete_archive_for_org(
        org: str,
        migration_id: int,
    ) -> str:
        """DELETE /orgs/{org}/migrations/{migration_id}/archive - Delete an organization migration archive"""
        # HTTP DELETE /orgs/{org}/migrations/{migration_id}/archive
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["migration_id"] = str(migration_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/migrations/{migration_id}/archive",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_download_archive_for_org(
        org: str,
        migration_id: int,
    ) -> str:
        """GET /orgs/{org}/migrations/{migration_id}/archive - Download an organization migration archive"""
        # HTTP GET /orgs/{org}/migrations/{migration_id}/archive
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["migration_id"] = str(migration_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/migrations/{migration_id}/archive",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_unlock_repo_for_org(
        org: str,
        migration_id: int,
        repo_name: str,
    ) -> str:
        """DELETE /orgs/{org}/migrations/{migration_id}/repos/{repo_name}/lock - Unlock an organization repository"""
        # HTTP DELETE /orgs/{org}/migrations/{migration_id}/repos/{repo_name}/lock
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["migration_id"] = str(migration_id)
        path_params["repo_name"] = str(repo_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/migrations/{migration_id}/repos/{repo_name}/lock",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_list_repos_for_org(
        org: str,
        migration_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/migrations/{migration_id}/repositories - List repositories in an organization migration"""
        # HTTP GET /orgs/{org}/migrations/{migration_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["migration_id"] = str(migration_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/migrations/{migration_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_org_roles(
        org: str,
    ) -> str:
        """GET /orgs/{org}/organization-roles - Get all organization roles for an organization"""
        # HTTP GET /orgs/{org}/organization-roles
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/organization-roles",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_revoke_all_org_roles_team(
        org: str,
        team_slug: str,
    ) -> str:
        """DELETE /orgs/{org}/organization-roles/teams/{team_slug} - Remove all organization roles for a team"""
        # HTTP DELETE /orgs/{org}/organization-roles/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/organization-roles/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_revoke_org_role_team(
        org: str,
        team_slug: str,
        role_id: int,
    ) -> str:
        """DELETE /orgs/{org}/organization-roles/teams/{team_slug}/{role_id} - Remove an organization role from a team"""
        # HTTP DELETE /orgs/{org}/organization-roles/teams/{team_slug}/{role_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["role_id"] = str(role_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/organization-roles/teams/{team_slug}/{role_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_assign_team_to_org_role(
        org: str,
        team_slug: str,
        role_id: int,
    ) -> str:
        """PUT /orgs/{org}/organization-roles/teams/{team_slug}/{role_id} - Assign an organization role to a team"""
        # HTTP PUT /orgs/{org}/organization-roles/teams/{team_slug}/{role_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["role_id"] = str(role_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/organization-roles/teams/{team_slug}/{role_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_revoke_all_org_roles_user(
        org: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/organization-roles/users/{username} - Remove all organization roles for a user"""
        # HTTP DELETE /orgs/{org}/organization-roles/users/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/organization-roles/users/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_revoke_org_role_user(
        org: str,
        username: str,
        role_id: int,
    ) -> str:
        """DELETE /orgs/{org}/organization-roles/users/{username}/{role_id} - Remove an organization role from a user"""
        # HTTP DELETE /orgs/{org}/organization-roles/users/{username}/{role_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        path_params["role_id"] = str(role_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/organization-roles/users/{username}/{role_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_assign_user_to_org_role(
        org: str,
        username: str,
        role_id: int,
    ) -> str:
        """PUT /orgs/{org}/organization-roles/users/{username}/{role_id} - Assign an organization role to a user"""
        # HTTP PUT /orgs/{org}/organization-roles/users/{username}/{role_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        path_params["role_id"] = str(role_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/organization-roles/users/{username}/{role_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_org_role(
        org: str,
        role_id: int,
    ) -> str:
        """GET /orgs/{org}/organization-roles/{role_id} - Get an organization role"""
        # HTTP GET /orgs/{org}/organization-roles/{role_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["role_id"] = str(role_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/organization-roles/{role_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_org_role_teams(
        org: str,
        role_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/organization-roles/{role_id}/teams - List teams that are assigned to an organization role"""
        # HTTP GET /orgs/{org}/organization-roles/{role_id}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["role_id"] = str(role_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/organization-roles/{role_id}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_org_role_users(
        org: str,
        role_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/organization-roles/{role_id}/users - List users that are assigned to an organization role"""
        # HTTP GET /orgs/{org}/organization-roles/{role_id}/users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["role_id"] = str(role_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/organization-roles/{role_id}/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_outside_collaborators(
        org: str,
        filter: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/outside_collaborators - List outside collaborators for an organization"""
        # HTTP GET /orgs/{org}/outside_collaborators
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if filter is not None:
            query_params["filter"] = str(filter)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/outside_collaborators",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_remove_outside_collaborator(
        org: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/outside_collaborators/{username} - Remove outside collaborator from an organization"""
        # HTTP DELETE /orgs/{org}/outside_collaborators/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/outside_collaborators/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_convert_member_to_outside_collaborator(
        org: str,
        username: str,
        async_: bool | None = None,
    ) -> str:
        """PUT /orgs/{org}/outside_collaborators/{username} - Convert an organization member to outside collaborator"""
        # HTTP PUT /orgs/{org}/outside_collaborators/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        if async_ is not None:
            query_params["async"] = str(async_)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/outside_collaborators/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_list_packages_for_organization(
        package_type: str,
        org: str,
        visibility: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/packages - List packages for an organization"""
        # HTTP GET /orgs/{org}/packages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if package_type is not None:
            query_params["package_type"] = str(package_type)
        path_params["org"] = str(org)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/packages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_delete_package_for_org(
        package_type: str,
        package_name: str,
        org: str,
    ) -> str:
        """DELETE /orgs/{org}/packages/{package_type}/{package_name} - Delete a package for an organization"""
        # HTTP DELETE /orgs/{org}/packages/{package_type}/{package_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/packages/{package_type}/{package_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_get_package_for_organization(
        package_type: str,
        package_name: str,
        org: str,
    ) -> str:
        """GET /orgs/{org}/packages/{package_type}/{package_name} - Get a package for an organization"""
        # HTTP GET /orgs/{org}/packages/{package_type}/{package_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/packages/{package_type}/{package_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_restore_package_for_org(
        package_type: str,
        package_name: str,
        org: str,
        token: str | None = None,
    ) -> str:
        """POST /orgs/{org}/packages/{package_type}/{package_name}/restore - Restore a package for an organization"""
        # HTTP POST /orgs/{org}/packages/{package_type}/{package_name}/restore
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        if token is not None:
            query_params["token"] = str(token)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/packages/{package_type}/{package_name}/restore",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_packages_by_package_type_by_package_name_versions(
        package_type: str,
        package_name: str,
        org: str,
        page: int | None = None,
        per_page: int | None = None,
        state: str | None = None,
    ) -> str:
        """GET /orgs/{org}/packages/{package_type}/{package_name}/versions - List package versions for a package owned by an organization"""
        # HTTP GET /orgs/{org}/packages/{package_type}/{package_name}/versions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/packages/{package_type}/{package_name}/versions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_delete_package_version_for_org(
        package_type: str,
        package_name: str,
        org: str,
        package_version_id: int,
    ) -> str:
        """DELETE /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id} - Delete package version for an organization"""
        # HTTP DELETE /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_get_package_version_for_organization(
        package_type: str,
        package_name: str,
        org: str,
        package_version_id: int,
    ) -> str:
        """GET /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id} - Get a package version for an organization"""
        # HTTP GET /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_restore_package_version_for_org(
        package_type: str,
        package_name: str,
        org: str,
        package_version_id: int,
    ) -> str:
        """POST /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore - Restore package version for an organization"""
        # HTTP POST /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["org"] = str(org)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_review_pat_grant_requests_in_bulk(
        org: str,
        action: str,
        pat_request_ids: list | None = None,
        reason: str | None = None,
    ) -> str:
        """POST /orgs/{org}/personal-access-token-requests - Review requests to access organization resources with fine-grained personal access tokens"""
        # HTTP POST /orgs/{org}/personal-access-token-requests
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if pat_request_ids is not None:
            query_params["pat_request_ids"] = str(pat_request_ids)
        if action is not None:
            query_params["action"] = str(action)
        if reason is not None:
            query_params["reason"] = str(reason)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/personal-access-token-requests",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_pat_grant_requests(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        sort: str | None = None,
        direction: str | None = None,
        owner: list | None = None,
        repository: str | None = None,
        permission: str | None = None,
        last_used_before: str | None = None,
        last_used_after: str | None = None,
        token_id: list | None = None,
    ) -> str:
        """GET /orgs/{org}/personal-access-token-requests - List requests to access organization resources with fine-grained personal access tokens"""
        # HTTP GET /orgs/{org}/personal-access-token-requests
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if owner is not None:
            query_params["owner"] = str(owner)
        if repository is not None:
            query_params["repository"] = str(repository)
        if permission is not None:
            query_params["permission"] = str(permission)
        if last_used_before is not None:
            query_params["last_used_before"] = str(last_used_before)
        if last_used_after is not None:
            query_params["last_used_after"] = str(last_used_after)
        if token_id is not None:
            query_params["token_id"] = str(token_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/personal-access-token-requests",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_review_pat_grant_request(
        org: str,
        pat_request_id: int,
        action: str,
        reason: str | None = None,
    ) -> str:
        """POST /orgs/{org}/personal-access-token-requests/{pat_request_id} - Review a request to access organization resources with a fine-grained personal access token"""
        # HTTP POST /orgs/{org}/personal-access-token-requests/{pat_request_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["pat_request_id"] = str(pat_request_id)
        if action is not None:
            query_params["action"] = str(action)
        if reason is not None:
            query_params["reason"] = str(reason)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/personal-access-token-requests/{pat_request_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_pat_grant_request_repositories(
        org: str,
        pat_request_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/personal-access-token-requests/{pat_request_id}/repositories - List repositories requested to be accessed by a fine-grained personal access token"""
        # HTTP GET /orgs/{org}/personal-access-token-requests/{pat_request_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["pat_request_id"] = str(pat_request_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/personal-access-token-requests/{pat_request_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_pat_accesses(
        org: str,
        action: str,
        pat_ids: list,
    ) -> str:
        """POST /orgs/{org}/personal-access-tokens - Update the access to organization resources via fine-grained personal access tokens"""
        # HTTP POST /orgs/{org}/personal-access-tokens
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if action is not None:
            query_params["action"] = str(action)
        if pat_ids is not None:
            query_params["pat_ids"] = str(pat_ids)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/personal-access-tokens",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_pat_grants(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        sort: str | None = None,
        direction: str | None = None,
        owner: list | None = None,
        repository: str | None = None,
        permission: str | None = None,
        last_used_before: str | None = None,
        last_used_after: str | None = None,
        token_id: list | None = None,
    ) -> str:
        """GET /orgs/{org}/personal-access-tokens - List fine-grained personal access tokens with access to organization resources"""
        # HTTP GET /orgs/{org}/personal-access-tokens
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if owner is not None:
            query_params["owner"] = str(owner)
        if repository is not None:
            query_params["repository"] = str(repository)
        if permission is not None:
            query_params["permission"] = str(permission)
        if last_used_before is not None:
            query_params["last_used_before"] = str(last_used_before)
        if last_used_after is not None:
            query_params["last_used_after"] = str(last_used_after)
        if token_id is not None:
            query_params["token_id"] = str(token_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/personal-access-tokens",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_pat_access(
        org: str,
        pat_id: int,
        action: str,
    ) -> str:
        """POST /orgs/{org}/personal-access-tokens/{pat_id} - Update the access a fine-grained personal access token has to organization resources"""
        # HTTP POST /orgs/{org}/personal-access-tokens/{pat_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["pat_id"] = str(pat_id)
        if action is not None:
            query_params["action"] = str(action)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/personal-access-tokens/{pat_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_pat_grant_repositories(
        org: str,
        pat_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/personal-access-tokens/{pat_id}/repositories - List repositories a fine-grained personal access token has access to"""
        # HTTP GET /orgs/{org}/personal-access-tokens/{pat_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["pat_id"] = str(pat_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/personal-access-tokens/{pat_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def private_registries_create_org_private_registry(
        org: str,
        registry_type: str,
        url: str,
        encrypted_value: str,
        key_id: str,
        visibility: str,
        username: str | None = None,
        replaces_base: bool | None = None,
        selected_repository_ids: list | None = None,
    ) -> str:
        """POST /orgs/{org}/private-registries - Create a private registry for an organization"""
        # HTTP POST /orgs/{org}/private-registries
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if registry_type is not None:
            query_params["registry_type"] = str(registry_type)
        if url is not None:
            query_params["url"] = str(url)
        if username is not None:
            query_params["username"] = str(username)
        if replaces_base is not None:
            query_params["replaces_base"] = str(replaces_base)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/private-registries",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def private_registries_list_org_private_registries(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/private-registries - List private registries for an organization"""
        # HTTP GET /orgs/{org}/private-registries
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/private-registries",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def private_registries_get_org_public_key(
        org: str,
    ) -> str:
        """GET /orgs/{org}/private-registries/public-key - Get private registries public key for an organization"""
        # HTTP GET /orgs/{org}/private-registries/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/private-registries/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def private_registries_update_org_private_registry(
        org: str,
        secret_name: str,
        registry_type: str | None = None,
        url: str | None = None,
        username: str | None = None,
        replaces_base: bool | None = None,
        encrypted_value: str | None = None,
        key_id: str | None = None,
        visibility: str | None = None,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PATCH /orgs/{org}/private-registries/{secret_name} - Update a private registry for an organization"""
        # HTTP PATCH /orgs/{org}/private-registries/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        if registry_type is not None:
            query_params["registry_type"] = str(registry_type)
        if url is not None:
            query_params["url"] = str(url)
        if username is not None:
            query_params["username"] = str(username)
        if replaces_base is not None:
            query_params["replaces_base"] = str(replaces_base)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/private-registries/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def private_registries_delete_org_private_registry(
        org: str,
        secret_name: str,
    ) -> str:
        """DELETE /orgs/{org}/private-registries/{secret_name} - Delete a private registry for an organization"""
        # HTTP DELETE /orgs/{org}/private-registries/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/private-registries/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def private_registries_get_org_private_registry(
        org: str,
        secret_name: str,
    ) -> str:
        """GET /orgs/{org}/private-registries/{secret_name} - Get a private registry for an organization"""
        # HTTP GET /orgs/{org}/private-registries/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/private-registries/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_for_org(
        org: str,
        q: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/projectsV2 - List projects for organization"""
        # HTTP GET /orgs/{org}/projectsV2
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if q is not None:
            query_params["q"] = str(q)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_get_for_org(
        project_number: int,
        org: str,
    ) -> str:
        """GET /orgs/{org}/projectsV2/{project_number} - Get project for organization"""
        # HTTP GET /orgs/{org}/projectsV2/{project_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2/{project_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_create_draft_item_for_org(
        org: str,
        project_number: int,
        title: str,
        body: str | None = None,
    ) -> str:
        """POST /orgs/{org}/projectsV2/{project_number}/drafts - Create draft item for organization owned project"""
        # HTTP POST /orgs/{org}/projectsV2/{project_number}/drafts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["project_number"] = str(project_number)
        if title is not None:
            query_params["title"] = str(title)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/projectsV2/{project_number}/drafts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_add_field_for_org(
        project_number: int,
        org: str,
        body: dict,
    ) -> str:
        """POST /orgs/{org}/projectsV2/{project_number}/fields - Add a field to an organization-owned project."""
        # HTTP POST /orgs/{org}/projectsV2/{project_number}/fields
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/projectsV2/{project_number}/fields",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_fields_for_org(
        project_number: int,
        org: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
    ) -> str:
        """GET /orgs/{org}/projectsV2/{project_number}/fields - List project fields for organization"""
        # HTTP GET /orgs/{org}/projectsV2/{project_number}/fields
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2/{project_number}/fields",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_get_field_for_org(
        project_number: int,
        field_id: int,
        org: str,
    ) -> str:
        """GET /orgs/{org}/projectsV2/{project_number}/fields/{field_id} - Get project field for organization"""
        # HTTP GET /orgs/{org}/projectsV2/{project_number}/fields/{field_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["field_id"] = str(field_id)
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2/{project_number}/fields/{field_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_add_item_for_org(
        org: str,
        project_number: int,
        type: str,
        id: int | None = None,
        owner: str | None = None,
        repo: str | None = None,
        number: int | None = None,
    ) -> str:
        """POST /orgs/{org}/projectsV2/{project_number}/items - Add item to organization owned project"""
        # HTTP POST /orgs/{org}/projectsV2/{project_number}/items
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["project_number"] = str(project_number)
        if type is not None:
            query_params["type"] = str(type)
        if id is not None:
            query_params["id"] = str(id)
        if owner is not None:
            query_params["owner"] = str(owner)
        if repo is not None:
            query_params["repo"] = str(repo)
        if number is not None:
            query_params["number"] = str(number)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/projectsV2/{project_number}/items",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_items_for_org(
        project_number: int,
        org: str,
        q: str | None = None,
        fields: dict | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/projectsV2/{project_number}/items - List items for an organization owned project"""
        # HTTP GET /orgs/{org}/projectsV2/{project_number}/items
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        if q is not None:
            query_params["q"] = str(q)
        body = fields if isinstance(fields, dict) else {"data": fields}
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2/{project_number}/items",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_update_item_for_org(
        project_number: int,
        org: str,
        item_id: int,
        fields: list,
    ) -> str:
        """PATCH /orgs/{org}/projectsV2/{project_number}/items/{item_id} - Update project item for organization"""
        # HTTP PATCH /orgs/{org}/projectsV2/{project_number}/items/{item_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        path_params["item_id"] = str(item_id)
        if fields is not None:
            query_params["fields"] = str(fields)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/projectsV2/{project_number}/items/{item_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_delete_item_for_org(
        project_number: int,
        org: str,
        item_id: int,
    ) -> str:
        """DELETE /orgs/{org}/projectsV2/{project_number}/items/{item_id} - Delete project item for organization"""
        # HTTP DELETE /orgs/{org}/projectsV2/{project_number}/items/{item_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        path_params["item_id"] = str(item_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/projectsV2/{project_number}/items/{item_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_get_org_item(
        project_number: int,
        org: str,
        item_id: int,
        fields: dict | None = None,
    ) -> str:
        """GET /orgs/{org}/projectsV2/{project_number}/items/{item_id} - Get an item for an organization owned project"""
        # HTTP GET /orgs/{org}/projectsV2/{project_number}/items/{item_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        path_params["item_id"] = str(item_id)
        body = fields if isinstance(fields, dict) else {"data": fields}
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2/{project_number}/items/{item_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_create_view_for_org(
        org: str,
        project_number: int,
        name: str,
        layout: str,
        filter: str | None = None,
        visible_fields: list | None = None,
    ) -> str:
        """POST /orgs/{org}/projectsV2/{project_number}/views - Create a view for an organization-owned project"""
        # HTTP POST /orgs/{org}/projectsV2/{project_number}/views
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["project_number"] = str(project_number)
        if name is not None:
            query_params["name"] = str(name)
        if layout is not None:
            query_params["layout"] = str(layout)
        if filter is not None:
            query_params["filter"] = str(filter)
        if visible_fields is not None:
            query_params["visible_fields"] = str(visible_fields)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/projectsV2/{project_number}/views",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_view_items_for_org(
        project_number: int,
        org: str,
        view_number: int,
        fields: dict | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/projectsV2/{project_number}/views/{view_number}/items - List items for an organization project view"""
        # HTTP GET /orgs/{org}/projectsV2/{project_number}/views/{view_number}/items
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["org"] = str(org)
        path_params["view_number"] = str(view_number)
        body = fields if isinstance(fields, dict) else {"data": fields}
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/projectsV2/{project_number}/views/{view_number}/items",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def patch_orgs_by_org_properties_schema(
        org: str,
        properties: list,
    ) -> str:
        """PATCH /orgs/{org}/properties/schema - Create or update custom properties for an organization"""
        # HTTP PATCH /orgs/{org}/properties/schema
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if properties is not None:
            query_params["properties"] = str(properties)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/properties/schema",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_properties_schema(
        org: str,
    ) -> str:
        """GET /orgs/{org}/properties/schema - Get all custom properties for an organization"""
        # HTTP GET /orgs/{org}/properties/schema
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/properties/schema",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_properties_schema_by_custom_property_name(
        org: str,
        custom_property_name: str,
    ) -> str:
        """DELETE /orgs/{org}/properties/schema/{custom_property_name} - Remove a custom property for an organization"""
        # HTTP DELETE /orgs/{org}/properties/schema/{custom_property_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["custom_property_name"] = str(custom_property_name)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/properties/schema/{custom_property_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_properties_schema_by_custom_property_name(
        org: str,
        custom_property_name: str,
        value_type: str,
        required: bool | None = None,
        default_value: dict | None = None,
        description: str | None = None,
        allowed_values: list | None = None,
        values_editable_by: str | None = None,
        require_explicit_values: bool | None = None,
    ) -> str:
        """PUT /orgs/{org}/properties/schema/{custom_property_name} - Create or update a custom property for an organization"""
        # HTTP PUT /orgs/{org}/properties/schema/{custom_property_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["custom_property_name"] = str(custom_property_name)
        if value_type is not None:
            query_params["value_type"] = str(value_type)
        if required is not None:
            query_params["required"] = str(required)
        body = default_value if isinstance(default_value, dict) else {"data": default_value}
        if description is not None:
            query_params["description"] = str(description)
        if allowed_values is not None:
            query_params["allowed_values"] = str(allowed_values)
        if values_editable_by is not None:
            query_params["values_editable_by"] = str(values_editable_by)
        if require_explicit_values is not None:
            query_params["require_explicit_values"] = str(require_explicit_values)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/properties/schema/{custom_property_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_properties_schema_by_custom_property_name(
        org: str,
        custom_property_name: str,
    ) -> str:
        """GET /orgs/{org}/properties/schema/{custom_property_name} - Get a custom property for an organization"""
        # HTTP GET /orgs/{org}/properties/schema/{custom_property_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["custom_property_name"] = str(custom_property_name)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/properties/schema/{custom_property_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def patch_orgs_by_org_properties_values(
        org: str,
        repository_names: list,
        properties: list,
    ) -> str:
        """PATCH /orgs/{org}/properties/values - Create or update custom property values for organization repositories"""
        # HTTP PATCH /orgs/{org}/properties/values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if repository_names is not None:
            query_params["repository_names"] = str(repository_names)
        if properties is not None:
            query_params["properties"] = str(properties)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/properties/values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_properties_values(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        repository_query: str | None = None,
    ) -> str:
        """GET /orgs/{org}/properties/values - List custom property values for organization repositories"""
        # HTTP GET /orgs/{org}/properties/values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if repository_query is not None:
            query_params["repository_query"] = str(repository_query)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/properties/values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_public_members(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/public_members - List public organization members"""
        # HTTP GET /orgs/{org}/public_members
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/public_members",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_public_members_by_username(
        org: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/public_members/{username} - Remove public organization membership for the authenticated user"""
        # HTTP DELETE /orgs/{org}/public_members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/public_members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_set_public_membership_for_authenticated_user(
        org: str,
        username: str,
    ) -> str:
        """PUT /orgs/{org}/public_members/{username} - Set public organization membership for the authenticated user"""
        # HTTP PUT /orgs/{org}/public_members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/public_members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_check_public_membership_for_user(
        org: str,
        username: str,
    ) -> str:
        """GET /orgs/{org}/public_members/{username} - Check public organization membership for a user"""
        # HTTP GET /orgs/{org}/public_members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/public_members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_in_org(
        org: str,
        name: str,
        description: str | None = None,
        homepage: str | None = None,
        private: bool | None = None,
        visibility: str | None = None,
        has_issues: bool | None = None,
        has_projects: bool | None = None,
        has_wiki: bool | None = None,
        has_downloads: bool | None = None,
        is_template: bool | None = None,
        team_id: int | None = None,
        auto_init: bool | None = None,
        gitignore_template: str | None = None,
        license_template: str | None = None,
        allow_squash_merge: bool | None = None,
        allow_merge_commit: bool | None = None,
        allow_rebase_merge: bool | None = None,
        allow_auto_merge: bool | None = None,
        delete_branch_on_merge: bool | None = None,
        use_squash_pr_title_as_default: bool | None = None,
        squash_merge_commit_title: str | None = None,
        squash_merge_commit_message: str | None = None,
        merge_commit_title: str | None = None,
        merge_commit_message: str | None = None,
        custom_properties: dict | None = None,
    ) -> str:
        """POST /orgs/{org}/repos - Create an organization repository"""
        # HTTP POST /orgs/{org}/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if homepage is not None:
            query_params["homepage"] = str(homepage)
        if private is not None:
            query_params["private"] = str(private)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if has_issues is not None:
            query_params["has_issues"] = str(has_issues)
        if has_projects is not None:
            query_params["has_projects"] = str(has_projects)
        if has_wiki is not None:
            query_params["has_wiki"] = str(has_wiki)
        if has_downloads is not None:
            query_params["has_downloads"] = str(has_downloads)
        if is_template is not None:
            query_params["is_template"] = str(is_template)
        if team_id is not None:
            query_params["team_id"] = str(team_id)
        if auto_init is not None:
            query_params["auto_init"] = str(auto_init)
        if gitignore_template is not None:
            query_params["gitignore_template"] = str(gitignore_template)
        if license_template is not None:
            query_params["license_template"] = str(license_template)
        if allow_squash_merge is not None:
            query_params["allow_squash_merge"] = str(allow_squash_merge)
        if allow_merge_commit is not None:
            query_params["allow_merge_commit"] = str(allow_merge_commit)
        if allow_rebase_merge is not None:
            query_params["allow_rebase_merge"] = str(allow_rebase_merge)
        if allow_auto_merge is not None:
            query_params["allow_auto_merge"] = str(allow_auto_merge)
        if delete_branch_on_merge is not None:
            query_params["delete_branch_on_merge"] = str(delete_branch_on_merge)
        if use_squash_pr_title_as_default is not None:
            query_params["use_squash_pr_title_as_default"] = str(use_squash_pr_title_as_default)
        if squash_merge_commit_title is not None:
            query_params["squash_merge_commit_title"] = str(squash_merge_commit_title)
        if squash_merge_commit_message is not None:
            query_params["squash_merge_commit_message"] = str(squash_merge_commit_message)
        if merge_commit_title is not None:
            query_params["merge_commit_title"] = str(merge_commit_title)
        if merge_commit_message is not None:
            query_params["merge_commit_message"] = str(merge_commit_message)
        body = custom_properties if isinstance(custom_properties, dict) else {"data": custom_properties}
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_for_org(
        org: str,
        type: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/repos - List organization repositories"""
        # HTTP GET /orgs/{org}/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if type is not None:
            query_params["type"] = str(type)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_org_ruleset(
        org: str,
        name: str,
        enforcement: str,
        target: str | None = None,
        bypass_actors: list | None = None,
        conditions: dict | None = None,
        rules: list | None = None,
    ) -> str:
        """POST /orgs/{org}/rulesets - Create an organization repository ruleset"""
        # HTTP POST /orgs/{org}/rulesets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if target is not None:
            query_params["target"] = str(target)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        if bypass_actors is not None:
            query_params["bypass_actors"] = str(bypass_actors)
        body = conditions if isinstance(conditions, dict) else {"data": conditions}
        if rules is not None:
            query_params["rules"] = str(rules)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/rulesets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_org_rulesets(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        targets: str | None = None,
    ) -> str:
        """GET /orgs/{org}/rulesets - Get all organization repository rulesets"""
        # HTTP GET /orgs/{org}/rulesets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if targets is not None:
            query_params["targets"] = str(targets)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/rulesets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_org_rule_suites(
        org: str,
        ref: str | None = None,
        repository_name: str | None = None,
        time_period: str | None = None,
        actor_name: str | None = None,
        rule_suite_result: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/rulesets/rule-suites - List organization rule suites"""
        # HTTP GET /orgs/{org}/rulesets/rule-suites
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if ref is not None:
            query_params["ref"] = str(ref)
        if repository_name is not None:
            query_params["repository_name"] = str(repository_name)
        if time_period is not None:
            query_params["time_period"] = str(time_period)
        if actor_name is not None:
            query_params["actor_name"] = str(actor_name)
        if rule_suite_result is not None:
            query_params["rule_suite_result"] = str(rule_suite_result)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/rulesets/rule-suites",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_org_rule_suite(
        org: str,
        rule_suite_id: int,
    ) -> str:
        """GET /orgs/{org}/rulesets/rule-suites/{rule_suite_id} - Get an organization rule suite"""
        # HTTP GET /orgs/{org}/rulesets/rule-suites/{rule_suite_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["rule_suite_id"] = str(rule_suite_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/rulesets/rule-suites/{rule_suite_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_org_ruleset(
        org: str,
        ruleset_id: int,
    ) -> str:
        """DELETE /orgs/{org}/rulesets/{ruleset_id} - Delete an organization repository ruleset"""
        # HTTP DELETE /orgs/{org}/rulesets/{ruleset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["ruleset_id"] = str(ruleset_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/rulesets/{ruleset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_org_ruleset(
        org: str,
        ruleset_id: int,
        name: str | None = None,
        target: str | None = None,
        enforcement: str | None = None,
        bypass_actors: list | None = None,
        conditions: dict | None = None,
        rules: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/rulesets/{ruleset_id} - Update an organization repository ruleset"""
        # HTTP PUT /orgs/{org}/rulesets/{ruleset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["ruleset_id"] = str(ruleset_id)
        if name is not None:
            query_params["name"] = str(name)
        if target is not None:
            query_params["target"] = str(target)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        if bypass_actors is not None:
            query_params["bypass_actors"] = str(bypass_actors)
        body = conditions if isinstance(conditions, dict) else {"data": conditions}
        if rules is not None:
            query_params["rules"] = str(rules)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/rulesets/{ruleset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_org_ruleset(
        org: str,
        ruleset_id: int,
    ) -> str:
        """GET /orgs/{org}/rulesets/{ruleset_id} - Get an organization repository ruleset"""
        # HTTP GET /orgs/{org}/rulesets/{ruleset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["ruleset_id"] = str(ruleset_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/rulesets/{ruleset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_org_ruleset_history(
        org: str,
        ruleset_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/rulesets/{ruleset_id}/history - Get organization ruleset history"""
        # HTTP GET /orgs/{org}/rulesets/{ruleset_id}/history
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        path_params["ruleset_id"] = str(ruleset_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/rulesets/{ruleset_id}/history",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_org_ruleset_version(
        org: str,
        ruleset_id: int,
        version_id: int,
    ) -> str:
        """GET /orgs/{org}/rulesets/{ruleset_id}/history/{version_id} - Get organization ruleset version"""
        # HTTP GET /orgs/{org}/rulesets/{ruleset_id}/history/{version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["ruleset_id"] = str(ruleset_id)
        path_params["version_id"] = str(version_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/rulesets/{ruleset_id}/history/{version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_list_alerts_for_org(
        org: str,
        state: str | None = None,
        secret_type: str | None = None,
        resolution: str | None = None,
        assignee: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        validity: str | None = None,
        is_publicly_leaked: bool | None = None,
        is_multi_repo: bool | None = None,
        hide_secret: bool | None = None,
    ) -> str:
        """GET /orgs/{org}/secret-scanning/alerts - List secret scanning alerts for an organization"""
        # HTTP GET /orgs/{org}/secret-scanning/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if state is not None:
            query_params["state"] = str(state)
        if secret_type is not None:
            query_params["secret_type"] = str(secret_type)
        if resolution is not None:
            query_params["resolution"] = str(resolution)
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if validity is not None:
            query_params["validity"] = str(validity)
        if is_publicly_leaked is not None:
            query_params["is_publicly_leaked"] = str(is_publicly_leaked)
        if is_multi_repo is not None:
            query_params["is_multi_repo"] = str(is_multi_repo)
        if hide_secret is not None:
            query_params["hide_secret"] = str(hide_secret)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/secret-scanning/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_update_org_pattern_configs(
        org: str,
        pattern_config_version: str | None = None,
        provider_pattern_settings: list | None = None,
        custom_pattern_settings: list | None = None,
    ) -> str:
        """PATCH /orgs/{org}/secret-scanning/pattern-configurations - Update organization pattern configurations"""
        # HTTP PATCH /orgs/{org}/secret-scanning/pattern-configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if pattern_config_version is not None:
            query_params["pattern_config_version"] = str(pattern_config_version)
        if provider_pattern_settings is not None:
            query_params["provider_pattern_settings"] = str(provider_pattern_settings)
        if custom_pattern_settings is not None:
            query_params["custom_pattern_settings"] = str(custom_pattern_settings)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/secret-scanning/pattern-configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_list_org_pattern_configs(
        org: str,
    ) -> str:
        """GET /orgs/{org}/secret-scanning/pattern-configurations - List organization pattern configurations"""
        # HTTP GET /orgs/{org}/secret-scanning/pattern-configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/secret-scanning/pattern-configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_security_advisories(
        org: str,
        direction: str | None = None,
        sort: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
        state: str | None = None,
    ) -> str:
        """GET /orgs/{org}/security-advisories - List repository security advisories for an organization"""
        # HTTP GET /orgs/{org}/security-advisories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if direction is not None:
            query_params["direction"] = str(direction)
        if sort is not None:
            query_params["sort"] = str(sort)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/security-advisories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_security_manager_teams(
        org: str,
    ) -> str:
        """GET /orgs/{org}/security-managers - List security manager teams"""
        # HTTP GET /orgs/{org}/security-managers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/security-managers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_remove_security_manager_team(
        org: str,
        team_slug: str,
    ) -> str:
        """DELETE /orgs/{org}/security-managers/teams/{team_slug} - Remove a security manager team"""
        # HTTP DELETE /orgs/{org}/security-managers/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/security-managers/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_add_security_manager_team(
        org: str,
        team_slug: str,
    ) -> str:
        """PUT /orgs/{org}/security-managers/teams/{team_slug} - Add a security manager team"""
        # HTTP PUT /orgs/{org}/security-managers/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/security-managers/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_set_immutable_releases_settings(
        org: str,
        enforced_repositories: str,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PUT /orgs/{org}/settings/immutable-releases - Set immutable releases settings for an organization"""
        # HTTP PUT /orgs/{org}/settings/immutable-releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if enforced_repositories is not None:
            query_params["enforced_repositories"] = str(enforced_repositories)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/settings/immutable-releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_immutable_releases_settings(
        org: str,
    ) -> str:
        """GET /orgs/{org}/settings/immutable-releases - Get immutable releases settings for an organization"""
        # HTTP GET /orgs/{org}/settings/immutable-releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/settings/immutable-releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_set_immutable_releases_settings_repositories(
        org: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /orgs/{org}/settings/immutable-releases/repositories - Set selected repositories for immutable releases enforcement"""
        # HTTP PUT /orgs/{org}/settings/immutable-releases/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/settings/immutable-releases/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_immutable_releases_settings_repositories(
        org: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/settings/immutable-releases/repositories - List selected repositories for immutable releases enforcement"""
        # HTTP GET /orgs/{org}/settings/immutable-releases/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/settings/immutable-releases/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_settings_immutable_releases_repositories_by_repository_id(
        org: str,
        repository_id: int,
    ) -> str:
        """DELETE /orgs/{org}/settings/immutable-releases/repositories/{repository_id} - Disable a selected repository for immutable releases in an organization"""
        # HTTP DELETE /orgs/{org}/settings/immutable-releases/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/settings/immutable-releases/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_orgs_by_org_settings_immutable_releases_repositories_by_repository_id(
        org: str,
        repository_id: int,
    ) -> str:
        """PUT /orgs/{org}/settings/immutable-releases/repositories/{repository_id} - Enable a selected repository for immutable releases in an organization"""
        # HTTP PUT /orgs/{org}/settings/immutable-releases/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/settings/immutable-releases/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_orgs_by_org_settings_network_configurations(
        org: str,
        name: str,
        network_settings_ids: list,
        compute_service: str | None = None,
    ) -> str:
        """POST /orgs/{org}/settings/network-configurations - Create a hosted compute network configuration for an organization"""
        # HTTP POST /orgs/{org}/settings/network-configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if compute_service is not None:
            query_params["compute_service"] = str(compute_service)
        if network_settings_ids is not None:
            query_params["network_settings_ids"] = str(network_settings_ids)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/settings/network-configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_orgs_by_org_settings_network_configurations(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/settings/network-configurations - List hosted compute network configurations for an organization"""
        # HTTP GET /orgs/{org}/settings/network-configurations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/settings/network-configurations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def patch_orgs_by_org_settings_network_configurations_by_network_configuration_id(
        org: str,
        network_configuration_id: str,
        name: str | None = None,
        compute_service: str | None = None,
        network_settings_ids: list | None = None,
    ) -> str:
        """PATCH /orgs/{org}/settings/network-configurations/{network_configuration_id} - Update a hosted compute network configuration for an organization"""
        # HTTP PATCH /orgs/{org}/settings/network-configurations/{network_configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["network_configuration_id"] = str(network_configuration_id)
        if name is not None:
            query_params["name"] = str(name)
        if compute_service is not None:
            query_params["compute_service"] = str(compute_service)
        if network_settings_ids is not None:
            query_params["network_settings_ids"] = str(network_settings_ids)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/settings/network-configurations/{network_configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_orgs_by_org_settings_network_configurations_by_network_configuration_id(
        org: str,
        network_configuration_id: str,
    ) -> str:
        """DELETE /orgs/{org}/settings/network-configurations/{network_configuration_id} - Delete a hosted compute network configuration from an organization"""
        # HTTP DELETE /orgs/{org}/settings/network-configurations/{network_configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["network_configuration_id"] = str(network_configuration_id)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/settings/network-configurations/{network_configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def hosted_compute_get_network_configuration_for_org(
        org: str,
        network_configuration_id: str,
    ) -> str:
        """GET /orgs/{org}/settings/network-configurations/{network_configuration_id} - Get a hosted compute network configuration for an organization"""
        # HTTP GET /orgs/{org}/settings/network-configurations/{network_configuration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["network_configuration_id"] = str(network_configuration_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/settings/network-configurations/{network_configuration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def hosted_compute_get_network_settings_for_org(
        org: str,
        network_settings_id: str,
    ) -> str:
        """GET /orgs/{org}/settings/network-settings/{network_settings_id} - Get a hosted compute network settings resource for an organization"""
        # HTTP GET /orgs/{org}/settings/network-settings/{network_settings_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["network_settings_id"] = str(network_settings_id)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/settings/network-settings/{network_settings_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def copilot_copilot_metrics_for_team(
        org: str,
        team_slug: str,
        since: str | None = None,
        until: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/team/{team_slug}/copilot/metrics - Get Copilot metrics for a team"""
        # HTTP GET /orgs/{org}/team/{team_slug}/copilot/metrics
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        if since is not None:
            query_params["since"] = str(since)
        if until is not None:
            query_params["until"] = str(until)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/team/{team_slug}/copilot/metrics",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_create(
        org: str,
        name: str,
        description: str | None = None,
        maintainers: list | None = None,
        repo_names: list | None = None,
        privacy: str | None = None,
        notification_setting: str | None = None,
        permission: str | None = None,
        parent_team_id: int | None = None,
    ) -> str:
        """POST /orgs/{org}/teams - Create a team"""
        # HTTP POST /orgs/{org}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if maintainers is not None:
            query_params["maintainers"] = str(maintainers)
        if repo_names is not None:
            query_params["repo_names"] = str(repo_names)
        if privacy is not None:
            query_params["privacy"] = str(privacy)
        if notification_setting is not None:
            query_params["notification_setting"] = str(notification_setting)
        if permission is not None:
            query_params["permission"] = str(permission)
        if parent_team_id is not None:
            query_params["parent_team_id"] = str(parent_team_id)
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list(
        org: str,
        per_page: int | None = None,
        page: int | None = None,
        team_type: str | None = None,
    ) -> str:
        """GET /orgs/{org}/teams - List teams"""
        # HTTP GET /orgs/{org}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if team_type is not None:
            query_params["team_type"] = str(team_type)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_update_in_org(
        org: str,
        team_slug: str,
        name: str | None = None,
        description: str | None = None,
        privacy: str | None = None,
        notification_setting: str | None = None,
        permission: str | None = None,
        parent_team_id: int | None = None,
    ) -> str:
        """PATCH /orgs/{org}/teams/{team_slug} - Update a team"""
        # HTTP PATCH /orgs/{org}/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if privacy is not None:
            query_params["privacy"] = str(privacy)
        if notification_setting is not None:
            query_params["notification_setting"] = str(notification_setting)
        if permission is not None:
            query_params["permission"] = str(permission)
        if parent_team_id is not None:
            query_params["parent_team_id"] = str(parent_team_id)
        try:
            return await backend.request(
                "PATCH",
                "/orgs/{org}/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_delete_in_org(
        org: str,
        team_slug: str,
    ) -> str:
        """DELETE /orgs/{org}/teams/{team_slug} - Delete a team"""
        # HTTP DELETE /orgs/{org}/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_get_by_name(
        org: str,
        team_slug: str,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug} - Get a team by name"""
        # HTTP GET /orgs/{org}/teams/{team_slug}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_pending_invitations_in_org(
        org: str,
        team_slug: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug}/invitations - List pending team invitations"""
        # HTTP GET /orgs/{org}/teams/{team_slug}/invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}/invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_members_in_org(
        org: str,
        team_slug: str,
        role: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug}/members - List team members"""
        # HTTP GET /orgs/{org}/teams/{team_slug}/members
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        if role is not None:
            query_params["role"] = str(role)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}/members",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_remove_membership_for_user_in_org(
        org: str,
        team_slug: str,
        username: str,
    ) -> str:
        """DELETE /orgs/{org}/teams/{team_slug}/memberships/{username} - Remove team membership for a user"""
        # HTTP DELETE /orgs/{org}/teams/{team_slug}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/teams/{team_slug}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_add_or_update_membership_for_user_in_org(
        org: str,
        team_slug: str,
        username: str,
        role: str | None = None,
    ) -> str:
        """PUT /orgs/{org}/teams/{team_slug}/memberships/{username} - Add or update team membership for a user"""
        # HTTP PUT /orgs/{org}/teams/{team_slug}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["username"] = str(username)
        if role is not None:
            query_params["role"] = str(role)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/teams/{team_slug}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_get_membership_for_user_in_org(
        org: str,
        team_slug: str,
        username: str,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug}/memberships/{username} - Get team membership for a user"""
        # HTTP GET /orgs/{org}/teams/{team_slug}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_repos_in_org(
        org: str,
        team_slug: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug}/repos - List team repositories"""
        # HTTP GET /orgs/{org}/teams/{team_slug}/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_remove_repo_in_org(
        org: str,
        team_slug: str,
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo} - Remove a repository from a team"""
        # HTTP DELETE /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_add_or_update_repo_permissions_in_org(
        org: str,
        team_slug: str,
        owner: str,
        repo: str,
        permission: str | None = None,
    ) -> str:
        """PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo} - Add or update team repository permissions"""
        # HTTP PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if permission is not None:
            query_params["permission"] = str(permission)
        try:
            return await backend.request(
                "PUT",
                "/orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_check_permissions_for_repo_in_org(
        org: str,
        team_slug: str,
        owner: str,
        repo: str,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo} - Check team permissions for a repository"""
        # HTTP GET /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_child_in_org(
        org: str,
        team_slug: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /orgs/{org}/teams/{team_slug}/teams - List child teams"""
        # HTTP GET /orgs/{org}/teams/{team_slug}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["team_slug"] = str(team_slug)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/orgs/{org}/teams/{team_slug}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_orgs_by_org_by_security_product_by_enablement(
        org: str,
        security_product: str,
        enablement: str,
        body: dict | None = None,
    ) -> str:
        """POST /orgs/{org}/{security_product}/{enablement} - Enable or disable a security feature for an organization"""
        # HTTP POST /orgs/{org}/{security_product}/{enablement}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        path_params["security_product"] = str(security_product)
        path_params["enablement"] = str(enablement)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/orgs/{org}/{security_product}/{enablement}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def rate_limit_get(
    ) -> str:
        """GET /rate_limit - Get rate limit status for the authenticated user"""
        # HTTP GET /rate_limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/rate_limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update(
        owner: str,
        repo: str,
        name: str | None = None,
        description: str | None = None,
        homepage: str | None = None,
        private: bool | None = None,
        visibility: str | None = None,
        security_and_analysis: dict | None = None,
        has_issues: bool | None = None,
        has_projects: bool | None = None,
        has_wiki: bool | None = None,
        is_template: bool | None = None,
        default_branch: str | None = None,
        allow_squash_merge: bool | None = None,
        allow_merge_commit: bool | None = None,
        allow_rebase_merge: bool | None = None,
        allow_auto_merge: bool | None = None,
        delete_branch_on_merge: bool | None = None,
        allow_update_branch: bool | None = None,
        use_squash_pr_title_as_default: bool | None = None,
        squash_merge_commit_title: str | None = None,
        squash_merge_commit_message: str | None = None,
        merge_commit_title: str | None = None,
        merge_commit_message: str | None = None,
        archived: bool | None = None,
        allow_forking: bool | None = None,
        web_commit_signoff_required: bool | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo} - Update a repository"""
        # HTTP PATCH /repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if homepage is not None:
            query_params["homepage"] = str(homepage)
        if private is not None:
            query_params["private"] = str(private)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        body = security_and_analysis if isinstance(security_and_analysis, dict) else {"data": security_and_analysis}
        if has_issues is not None:
            query_params["has_issues"] = str(has_issues)
        if has_projects is not None:
            query_params["has_projects"] = str(has_projects)
        if has_wiki is not None:
            query_params["has_wiki"] = str(has_wiki)
        if is_template is not None:
            query_params["is_template"] = str(is_template)
        if default_branch is not None:
            query_params["default_branch"] = str(default_branch)
        if allow_squash_merge is not None:
            query_params["allow_squash_merge"] = str(allow_squash_merge)
        if allow_merge_commit is not None:
            query_params["allow_merge_commit"] = str(allow_merge_commit)
        if allow_rebase_merge is not None:
            query_params["allow_rebase_merge"] = str(allow_rebase_merge)
        if allow_auto_merge is not None:
            query_params["allow_auto_merge"] = str(allow_auto_merge)
        if delete_branch_on_merge is not None:
            query_params["delete_branch_on_merge"] = str(delete_branch_on_merge)
        if allow_update_branch is not None:
            query_params["allow_update_branch"] = str(allow_update_branch)
        if use_squash_pr_title_as_default is not None:
            query_params["use_squash_pr_title_as_default"] = str(use_squash_pr_title_as_default)
        if squash_merge_commit_title is not None:
            query_params["squash_merge_commit_title"] = str(squash_merge_commit_title)
        if squash_merge_commit_message is not None:
            query_params["squash_merge_commit_message"] = str(squash_merge_commit_message)
        if merge_commit_title is not None:
            query_params["merge_commit_title"] = str(merge_commit_title)
        if merge_commit_message is not None:
            query_params["merge_commit_message"] = str(merge_commit_message)
        if archived is not None:
            query_params["archived"] = str(archived)
        if allow_forking is not None:
            query_params["allow_forking"] = str(allow_forking)
        if web_commit_signoff_required is not None:
            query_params["web_commit_signoff_required"] = str(web_commit_signoff_required)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo} - Delete a repository"""
        # HTTP DELETE /repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo} - Get a repository"""
        # HTTP GET /repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_artifacts_for_repo(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
        name: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/artifacts - List artifacts for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/artifacts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if name is not None:
            query_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/artifacts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_artifact(
        owner: str,
        repo: str,
        artifact_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/artifacts/{artifact_id} - Delete an artifact"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/artifacts/{artifact_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["artifact_id"] = str(artifact_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/artifacts/{artifact_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_artifact(
        owner: str,
        repo: str,
        artifact_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id} - Get an artifact"""
        # HTTP GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["artifact_id"] = str(artifact_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/artifacts/{artifact_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_download_artifact(
        owner: str,
        repo: str,
        artifact_id: int,
        archive_format: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format} - Download an artifact"""
        # HTTP GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["artifact_id"] = str(artifact_id)
        path_params["archive_format"] = str(archive_format)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_cache_retention_limit(
        owner: str,
        repo: str,
        max_cache_retention_days: int | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/cache/retention-limit - Set GitHub Actions cache retention limit for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/cache/retention-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if max_cache_retention_days is not None:
            query_params["max_cache_retention_days"] = str(max_cache_retention_days)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/cache/retention-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_cache_retention_limit(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/cache/retention-limit - Get GitHub Actions cache retention limit for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/cache/retention-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/cache/retention-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_cache_storage_limit(
        owner: str,
        repo: str,
        max_cache_size_gb: int | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/cache/storage-limit - Set GitHub Actions cache storage limit for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/cache/storage-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if max_cache_size_gb is not None:
            query_params["max_cache_size_gb"] = str(max_cache_size_gb)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/cache/storage-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_cache_storage_limit(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/cache/storage-limit - Get GitHub Actions cache storage limit for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/cache/storage-limit
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/cache/storage-limit",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_actions_cache_usage(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/cache/usage - Get GitHub Actions cache usage for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/cache/usage
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/cache/usage",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_actions_cache_by_key(
        owner: str,
        repo: str,
        key: str,
        ref: str | None = None,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/caches - Delete GitHub Actions caches for a repository (using a cache key)"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/caches
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if key is not None:
            query_params["key"] = str(key)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/caches",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_actions_cache_list(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
        ref: str | None = None,
        key: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/caches - List GitHub Actions caches for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/caches
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if ref is not None:
            query_params["ref"] = str(ref)
        if key is not None:
            query_params["key"] = str(key)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/caches",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_actions_cache_by_id(
        owner: str,
        repo: str,
        cache_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/caches/{cache_id} - Delete a GitHub Actions cache for a repository (using a cache ID)"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/caches/{cache_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["cache_id"] = str(cache_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/caches/{cache_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_job_for_workflow_run(
        owner: str,
        repo: str,
        job_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/jobs/{job_id} - Get a job for a workflow run"""
        # HTTP GET /repos/{owner}/{repo}/actions/jobs/{job_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["job_id"] = str(job_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/jobs/{job_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_download_job_logs_for_workflow_run(
        owner: str,
        repo: str,
        job_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs - Download job logs for a workflow run"""
        # HTTP GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["job_id"] = str(job_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/jobs/{job_id}/logs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_re_run_job_for_workflow_run(
        owner: str,
        repo: str,
        job_id: int,
        enable_debug_logging: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/jobs/{job_id}/rerun - Re-run a job from a workflow run"""
        # HTTP POST /repos/{owner}/{repo}/actions/jobs/{job_id}/rerun
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["job_id"] = str(job_id)
        if enable_debug_logging is not None:
            query_params["enable_debug_logging"] = str(enable_debug_logging)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/jobs/{job_id}/rerun",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_custom_oidc_sub_claim_for_repo(
        owner: str,
        repo: str,
        use_default: bool,
        include_claim_keys: list | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/oidc/customization/sub - Set the customization template for an OIDC subject claim for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/oidc/customization/sub
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if use_default is not None:
            query_params["use_default"] = str(use_default)
        if include_claim_keys is not None:
            query_params["include_claim_keys"] = str(include_claim_keys)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/oidc/customization/sub",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_custom_oidc_sub_claim_for_repo(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/oidc/customization/sub - Get the customization template for an OIDC subject claim for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/oidc/customization/sub
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/oidc/customization/sub",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_repo_organization_secrets(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/organization-secrets - List repository organization secrets"""
        # HTTP GET /repos/{owner}/{repo}/actions/organization-secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/organization-secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_repo_organization_variables(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/organization-variables - List repository organization variables"""
        # HTTP GET /repos/{owner}/{repo}/actions/organization-variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/organization-variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_github_actions_permissions_repository(
        owner: str,
        repo: str,
        enabled: bool,
        allowed_actions: str | None = None,
        sha_pinning_required: bool | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions - Set GitHub Actions permissions for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if enabled is not None:
            query_params["enabled"] = str(enabled)
        if allowed_actions is not None:
            query_params["allowed_actions"] = str(allowed_actions)
        if sha_pinning_required is not None:
            query_params["sha_pinning_required"] = str(sha_pinning_required)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_github_actions_permissions_repository(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions - Get GitHub Actions permissions for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_workflow_access_to_repository(
        owner: str,
        repo: str,
        access_level: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions/access - Set the level of access for workflows outside of the repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions/access
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if access_level is not None:
            query_params["access_level"] = str(access_level)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions/access",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_workflow_access_to_repository(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions/access - Get the level of access for workflows outside of the repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions/access
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions/access",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_permissions_artifact_and_log_retention(
        owner: str,
        repo: str,
        days: int,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention - Set artifact and log retention settings for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if days is not None:
            query_params["days"] = str(days)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_permissions_artifact_and_log_retention(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention - Get artifact and log retention settings for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_permissions_fork_pr_contributor_approval(
        owner: str,
        repo: str,
        approval_policy: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval - Set fork PR contributor approval permissions for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if approval_policy is not None:
            query_params["approval_policy"] = str(approval_policy)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_permissions_fork_pr_contributor_approval(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval - Get fork PR contributor approval permissions for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_permissions_fork_pr_workflows_private_repos(
        owner: str,
        repo: str,
        run_workflows_from_fork_pull_requests: bool,
        send_write_tokens_to_workflows: bool | None = None,
        send_secrets_and_variables: bool | None = None,
        require_approval_for_fork_pr_workflows: bool | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos - Set private repo fork PR workflow settings for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if run_workflows_from_fork_pull_requests is not None:
            query_params["run_workflows_from_fork_pull_requests"] = str(run_workflows_from_fork_pull_requests)
        if send_write_tokens_to_workflows is not None:
            query_params["send_write_tokens_to_workflows"] = str(send_write_tokens_to_workflows)
        if send_secrets_and_variables is not None:
            query_params["send_secrets_and_variables"] = str(send_secrets_and_variables)
        if require_approval_for_fork_pr_workflows is not None:
            query_params["require_approval_for_fork_pr_workflows"] = str(require_approval_for_fork_pr_workflows)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_permissions_fork_pr_workflows_private_repos(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos - Get private repo fork PR workflow settings for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_set_allowed_actions_repository(
        owner: str,
        repo: str,
        github_owned_allowed: bool | None = None,
        verified_allowed: bool | None = None,
        patterns_allowed: list | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions/selected-actions - Set allowed actions and reusable workflows for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions/selected-actions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if github_owned_allowed is not None:
            query_params["github_owned_allowed"] = str(github_owned_allowed)
        if verified_allowed is not None:
            query_params["verified_allowed"] = str(verified_allowed)
        if patterns_allowed is not None:
            query_params["patterns_allowed"] = str(patterns_allowed)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions/selected-actions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_allowed_actions_repository(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions/selected-actions - Get allowed actions and reusable workflows for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions/selected-actions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions/selected-actions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_permissions_workflow(
        owner: str,
        repo: str,
        default_workflow_permissions: str | None = None,
        can_approve_pull_request_reviews: bool | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/permissions/workflow - Set default workflow permissions for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/permissions/workflow
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if default_workflow_permissions is not None:
            query_params["default_workflow_permissions"] = str(default_workflow_permissions)
        if can_approve_pull_request_reviews is not None:
            query_params["can_approve_pull_request_reviews"] = str(can_approve_pull_request_reviews)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/permissions/workflow",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_permissions_workflow(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/permissions/workflow - Get default workflow permissions for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/permissions/workflow
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/permissions/workflow",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_self_hosted_runners_for_repo(
        owner: str,
        repo: str,
        name: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runners - List self-hosted runners for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/runners
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if name is not None:
            query_params["name"] = str(name)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runners",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_runner_applications_for_repo(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runners/downloads - List runner applications for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/runners/downloads
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runners/downloads",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_generate_runner_jitconfig_for_repo(
        owner: str,
        repo: str,
        name: str,
        runner_group_id: int,
        labels: list,
        work_folder: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runners/generate-jitconfig - Create configuration for a just-in-time runner for a repository"""
        # HTTP POST /repos/{owner}/{repo}/actions/runners/generate-jitconfig
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        if runner_group_id is not None:
            query_params["runner_group_id"] = str(runner_group_id)
        if labels is not None:
            query_params["labels"] = str(labels)
        if work_folder is not None:
            query_params["work_folder"] = str(work_folder)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runners/generate-jitconfig",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_registration_token_for_repo(
        owner: str,
        repo: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runners/registration-token - Create a registration token for a repository"""
        # HTTP POST /repos/{owner}/{repo}/actions/runners/registration-token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runners/registration-token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_remove_token_for_repo(
        owner: str,
        repo: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runners/remove-token - Create a remove token for a repository"""
        # HTTP POST /repos/{owner}/{repo}/actions/runners/remove-token
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runners/remove-token",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_self_hosted_runner_from_repo(
        owner: str,
        repo: str,
        runner_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/runners/{runner_id} - Delete a self-hosted runner from a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_self_hosted_runner_for_repo(
        owner: str,
        repo: str,
        runner_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runners/{runner_id} - Get a self-hosted runner for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/runners/{runner_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_repos_by_owner_by_repo_actions_runners_by_runner_id_labels(
        owner: str,
        repo: str,
        runner_id: int,
        labels: list,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runners/{runner_id}/labels - Add custom labels to a self-hosted runner for a repository"""
        # HTTP POST /repos/{owner}/{repo}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        if labels is not None:
            query_params["labels"] = str(labels)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_repos_by_owner_by_repo_actions_runners_by_runner_id_labels(
        owner: str,
        repo: str,
        runner_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels - Remove all custom labels from a self-hosted runner for a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_repos_by_owner_by_repo_actions_runners_by_runner_id_labels(
        owner: str,
        repo: str,
        runner_id: int,
        labels: list,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/runners/{runner_id}/labels - Set custom labels for a self-hosted runner for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        if labels is not None:
            query_params["labels"] = str(labels)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_actions_runners_by_runner_id_labels(
        owner: str,
        repo: str,
        runner_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runners/{runner_id}/labels - List labels for a self-hosted runner for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/runners/{runner_id}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_repos_by_owner_by_repo_actions_runners_by_runner_id_labels_by_name(
        owner: str,
        repo: str,
        runner_id: int,
        name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels/{name} - Remove a custom label from a self-hosted runner for a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["runner_id"] = str(runner_id)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/runners/{runner_id}/labels/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_workflow_runs_for_repo(
        owner: str,
        repo: str,
        actor: str | None = None,
        branch: str | None = None,
        event: str | None = None,
        status: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
        created: str | None = None,
        exclude_pull_requests: bool | None = None,
        check_suite_id: int | None = None,
        head_sha: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs - List workflow runs for a repository"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if actor is not None:
            query_params["actor"] = str(actor)
        if branch is not None:
            query_params["branch"] = str(branch)
        if event is not None:
            query_params["event"] = str(event)
        if status is not None:
            query_params["status"] = str(status)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if created is not None:
            query_params["created"] = str(created)
        if exclude_pull_requests is not None:
            query_params["exclude_pull_requests"] = str(exclude_pull_requests)
        if check_suite_id is not None:
            query_params["check_suite_id"] = str(check_suite_id)
        if head_sha is not None:
            query_params["head_sha"] = str(head_sha)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_workflow_run(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/runs/{run_id} - Delete a workflow run"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/runs/{run_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/runs/{run_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_workflow_run(
        owner: str,
        repo: str,
        run_id: int,
        exclude_pull_requests: bool | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id} - Get a workflow run"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        if exclude_pull_requests is not None:
            query_params["exclude_pull_requests"] = str(exclude_pull_requests)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_reviews_for_run(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals - Get the review history for a workflow run"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/approvals",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_approve_workflow_run(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/approve - Approve a workflow run for a fork pull request"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/approve
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/approve",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_workflow_run_artifacts(
        owner: str,
        repo: str,
        run_id: int,
        per_page: int | None = None,
        page: int | None = None,
        name: str | None = None,
        direction: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts - List workflow run artifacts"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if name is not None:
            query_params["name"] = str(name)
        if direction is not None:
            query_params["direction"] = str(direction)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/artifacts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_workflow_run_attempt(
        owner: str,
        repo: str,
        run_id: int,
        attempt_number: int,
        exclude_pull_requests: bool | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number} - Get a workflow run attempt"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        path_params["attempt_number"] = str(attempt_number)
        if exclude_pull_requests is not None:
            query_params["exclude_pull_requests"] = str(exclude_pull_requests)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_jobs_for_workflow_run_attempt(
        owner: str,
        repo: str,
        run_id: int,
        attempt_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs - List jobs for a workflow run attempt"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        path_params["attempt_number"] = str(attempt_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_download_workflow_run_attempt_logs(
        owner: str,
        repo: str,
        run_id: int,
        attempt_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/logs - Download workflow run attempt logs"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/logs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        path_params["attempt_number"] = str(attempt_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/logs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_cancel_workflow_run(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel - Cancel a workflow run"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/cancel",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_review_custom_gates_for_run(
        owner: str,
        repo: str,
        run_id: int,
        body: dict,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/deployment_protection_rule - Review custom deployment protection rules for a workflow run"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/deployment_protection_rule
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/deployment_protection_rule",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_force_cancel_workflow_run(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/force-cancel - Force cancel a workflow run"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/force-cancel
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/force-cancel",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_jobs_for_workflow_run(
        owner: str,
        repo: str,
        run_id: int,
        filter: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/jobs - List jobs for a workflow run"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/jobs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        if filter is not None:
            query_params["filter"] = str(filter)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/jobs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_workflow_run_logs(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/runs/{run_id}/logs - Delete workflow run logs"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/runs/{run_id}/logs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/logs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_download_workflow_run_logs(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs - Download workflow run logs"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/logs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_review_pending_deployments_for_run(
        owner: str,
        repo: str,
        run_id: int,
        environment_ids: list,
        state: str,
        comment: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments - Review pending deployments for a workflow run"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        if environment_ids is not None:
            query_params["environment_ids"] = str(environment_ids)
        if state is not None:
            query_params["state"] = str(state)
        if comment is not None:
            query_params["comment"] = str(comment)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_pending_deployments_for_run(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments - Get pending deployments for a workflow run"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_re_run_workflow(
        owner: str,
        repo: str,
        run_id: int,
        enable_debug_logging: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun - Re-run a workflow"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        if enable_debug_logging is not None:
            query_params["enable_debug_logging"] = str(enable_debug_logging)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/rerun",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_re_run_workflow_failed_jobs(
        owner: str,
        repo: str,
        run_id: int,
        enable_debug_logging: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs - Re-run failed jobs from a workflow run"""
        # HTTP POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        if enable_debug_logging is not None:
            query_params["enable_debug_logging"] = str(enable_debug_logging)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_workflow_run_usage(
        owner: str,
        repo: str,
        run_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/runs/{run_id}/timing - Get workflow run usage"""
        # HTTP GET /repos/{owner}/{repo}/actions/runs/{run_id}/timing
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["run_id"] = str(run_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/runs/{run_id}/timing",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_repo_secrets(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/secrets - List repository secrets"""
        # HTTP GET /repos/{owner}/{repo}/actions/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_repo_public_key(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/secrets/public-key - Get a repository public key"""
        # HTTP GET /repos/{owner}/{repo}/actions/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/secrets/{secret_name} - Delete a repository secret"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_or_update_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
        encrypted_value: str,
        key_id: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/secrets/{secret_name} - Create or update a repository secret"""
        # HTTP PUT /repos/{owner}/{repo}/actions/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/secrets/{secret_name} - Get a repository secret"""
        # HTTP GET /repos/{owner}/{repo}/actions/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_repo_variable(
        owner: str,
        repo: str,
        name: str,
        value: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/variables - Create a repository variable"""
        # HTTP POST /repos/{owner}/{repo}/actions/variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        if value is not None:
            query_params["value"] = str(value)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_repo_variables(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/variables - List repository variables"""
        # HTTP GET /repos/{owner}/{repo}/actions/variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_update_repo_variable(
        owner: str,
        repo: str,
        name: str,
        value: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/actions/variables/{name} - Update a repository variable"""
        # HTTP PATCH /repos/{owner}/{repo}/actions/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        if value is not None:
            query_params["value"] = str(value)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/actions/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_repo_variable(
        owner: str,
        repo: str,
        name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/actions/variables/{name} - Delete a repository variable"""
        # HTTP DELETE /repos/{owner}/{repo}/actions/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/actions/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_repo_variable(
        owner: str,
        repo: str,
        name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/variables/{name} - Get a repository variable"""
        # HTTP GET /repos/{owner}/{repo}/actions/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_repo_workflows(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/workflows - List repository workflows"""
        # HTTP GET /repos/{owner}/{repo}/actions/workflows
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/workflows",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_workflow(
        owner: str,
        repo: str,
        workflow_id: dict,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/workflows/{workflow_id} - Get a workflow"""
        # HTTP GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["workflow_id"] = str(workflow_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/workflows/{workflow_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_disable_workflow(
        owner: str,
        repo: str,
        workflow_id: dict,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable - Disable a workflow"""
        # HTTP PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["workflow_id"] = str(workflow_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_workflow_dispatch(
        owner: str,
        repo: str,
        workflow_id: dict,
        ref: str,
        inputs: dict | None = None,
        return_run_details: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches - Create a workflow dispatch event"""
        # HTTP POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["workflow_id"] = str(workflow_id)
        if ref is not None:
            query_params["ref"] = str(ref)
        body = inputs if isinstance(inputs, dict) else {"data": inputs}
        if return_run_details is not None:
            query_params["return_run_details"] = str(return_run_details)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_enable_workflow(
        owner: str,
        repo: str,
        workflow_id: dict,
    ) -> str:
        """PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable - Enable a workflow"""
        # HTTP PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["workflow_id"] = str(workflow_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_workflow_runs(
        owner: str,
        repo: str,
        workflow_id: dict,
        actor: str | None = None,
        branch: str | None = None,
        event: str | None = None,
        status: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
        created: str | None = None,
        exclude_pull_requests: bool | None = None,
        check_suite_id: int | None = None,
        head_sha: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs - List workflow runs for a workflow"""
        # HTTP GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["workflow_id"] = str(workflow_id)
        if actor is not None:
            query_params["actor"] = str(actor)
        if branch is not None:
            query_params["branch"] = str(branch)
        if event is not None:
            query_params["event"] = str(event)
        if status is not None:
            query_params["status"] = str(status)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if created is not None:
            query_params["created"] = str(created)
        if exclude_pull_requests is not None:
            query_params["exclude_pull_requests"] = str(exclude_pull_requests)
        if check_suite_id is not None:
            query_params["check_suite_id"] = str(check_suite_id)
        if head_sha is not None:
            query_params["head_sha"] = str(head_sha)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_workflow_usage(
        owner: str,
        repo: str,
        workflow_id: dict,
    ) -> str:
        """GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/timing - Get workflow usage"""
        # HTTP GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/timing
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["workflow_id"] = str(workflow_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/timing",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_activities(
        owner: str,
        repo: str,
        direction: str | None = None,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        ref: str | None = None,
        actor: str | None = None,
        time_period: str | None = None,
        activity_type: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/activity - List repository activities"""
        # HTTP GET /repos/{owner}/{repo}/activity
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if ref is not None:
            query_params["ref"] = str(ref)
        if actor is not None:
            query_params["actor"] = str(actor)
        if time_period is not None:
            query_params["time_period"] = str(time_period)
        if activity_type is not None:
            query_params["activity_type"] = str(activity_type)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/activity",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_assignees(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/assignees - List assignees"""
        # HTTP GET /repos/{owner}/{repo}/assignees
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/assignees",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_check_user_can_be_assigned(
        owner: str,
        repo: str,
        assignee: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/assignees/{assignee} - Check if a user can be assigned"""
        # HTTP GET /repos/{owner}/{repo}/assignees/{assignee}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["assignee"] = str(assignee)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/assignees/{assignee}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_attestation(
        owner: str,
        repo: str,
        bundle: dict,
    ) -> str:
        """POST /repos/{owner}/{repo}/attestations - Create an attestation"""
        # HTTP POST /repos/{owner}/{repo}/attestations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        body = bundle if isinstance(bundle, dict) else {"data": bundle}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/attestations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_attestations(
        owner: str,
        repo: str,
        subject_digest: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        predicate_type: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/attestations/{subject_digest} - List attestations"""
        # HTTP GET /repos/{owner}/{repo}/attestations/{subject_digest}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        path_params["subject_digest"] = str(subject_digest)
        if predicate_type is not None:
            query_params["predicate_type"] = str(predicate_type)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/attestations/{subject_digest}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_autolink(
        owner: str,
        repo: str,
        key_prefix: str,
        url_template: str,
        is_alphanumeric: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/autolinks - Create an autolink reference for a repository"""
        # HTTP POST /repos/{owner}/{repo}/autolinks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if key_prefix is not None:
            query_params["key_prefix"] = str(key_prefix)
        if url_template is not None:
            query_params["url_template"] = str(url_template)
        if is_alphanumeric is not None:
            query_params["is_alphanumeric"] = str(is_alphanumeric)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/autolinks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_autolinks(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/autolinks - Get all autolinks of a repository"""
        # HTTP GET /repos/{owner}/{repo}/autolinks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/autolinks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_autolink(
        owner: str,
        repo: str,
        autolink_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/autolinks/{autolink_id} - Delete an autolink reference from a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/autolinks/{autolink_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["autolink_id"] = str(autolink_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/autolinks/{autolink_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_autolink(
        owner: str,
        repo: str,
        autolink_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/autolinks/{autolink_id} - Get an autolink reference of a repository"""
        # HTTP GET /repos/{owner}/{repo}/autolinks/{autolink_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["autolink_id"] = str(autolink_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/autolinks/{autolink_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_disable_automated_security_fixes(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/automated-security-fixes - Disable Dependabot security updates"""
        # HTTP DELETE /repos/{owner}/{repo}/automated-security-fixes
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/automated-security-fixes",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_enable_automated_security_fixes(
        owner: str,
        repo: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/automated-security-fixes - Enable Dependabot security updates"""
        # HTTP PUT /repos/{owner}/{repo}/automated-security-fixes
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/automated-security-fixes",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_check_automated_security_fixes(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/automated-security-fixes - Check if Dependabot security updates are enabled for a repository"""
        # HTTP GET /repos/{owner}/{repo}/automated-security-fixes
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/automated-security-fixes",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_branches(
        owner: str,
        repo: str,
        protected: bool | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches - List branches"""
        # HTTP GET /repos/{owner}/{repo}/branches
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if protected is not None:
            query_params["protected"] = str(protected)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_branch(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch} - Get a branch"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_branch_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection - Delete branch protection"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_branch_protection(
        owner: str,
        repo: str,
        branch: str,
        required_status_checks: dict,
        enforce_admins: bool,
        required_pull_request_reviews: dict,
        restrictions: dict,
        required_linear_history: bool | None = None,
        allow_force_pushes: bool | None = None,
        allow_deletions: bool | None = None,
        block_creations: bool | None = None,
        required_conversation_resolution: bool | None = None,
        lock_branch: bool | None = None,
        allow_fork_syncing: bool | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/branches/{branch}/protection - Update branch protection"""
        # HTTP PUT /repos/{owner}/{repo}/branches/{branch}/protection
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = required_status_checks if isinstance(required_status_checks, dict) else {"data": required_status_checks}
        if enforce_admins is not None:
            query_params["enforce_admins"] = str(enforce_admins)
        body = required_pull_request_reviews if isinstance(required_pull_request_reviews, dict) else {"data": required_pull_request_reviews}
        body = restrictions if isinstance(restrictions, dict) else {"data": restrictions}
        if required_linear_history is not None:
            query_params["required_linear_history"] = str(required_linear_history)
        if allow_force_pushes is not None:
            query_params["allow_force_pushes"] = str(allow_force_pushes)
        if allow_deletions is not None:
            query_params["allow_deletions"] = str(allow_deletions)
        if block_creations is not None:
            query_params["block_creations"] = str(block_creations)
        if required_conversation_resolution is not None:
            query_params["required_conversation_resolution"] = str(required_conversation_resolution)
        if lock_branch is not None:
            query_params["lock_branch"] = str(lock_branch)
        if allow_fork_syncing is not None:
            query_params["allow_fork_syncing"] = str(allow_fork_syncing)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/branches/{branch}/protection",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_branch_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection - Get branch protection"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_set_admin_branch_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins - Set admin branch protection"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_admin_branch_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins - Delete admin branch protection"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_admin_branch_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins - Get admin branch protection"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_pull_request_review_protection(
        owner: str,
        repo: str,
        branch: str,
        dismissal_restrictions: dict | None = None,
        dismiss_stale_reviews: bool | None = None,
        require_code_owner_reviews: bool | None = None,
        required_approving_review_count: int | None = None,
        require_last_push_approval: bool | None = None,
        bypass_pull_request_allowances: dict | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews - Update pull request review protection"""
        # HTTP PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = dismissal_restrictions if isinstance(dismissal_restrictions, dict) else {"data": dismissal_restrictions}
        if dismiss_stale_reviews is not None:
            query_params["dismiss_stale_reviews"] = str(dismiss_stale_reviews)
        if require_code_owner_reviews is not None:
            query_params["require_code_owner_reviews"] = str(require_code_owner_reviews)
        if required_approving_review_count is not None:
            query_params["required_approving_review_count"] = str(required_approving_review_count)
        if require_last_push_approval is not None:
            query_params["require_last_push_approval"] = str(require_last_push_approval)
        body = bypass_pull_request_allowances if isinstance(bypass_pull_request_allowances, dict) else {"data": bypass_pull_request_allowances}
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_pull_request_review_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews - Delete pull request review protection"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_pull_request_review_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews - Get pull request review protection"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_commit_signature_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures - Create commit signature protection"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_signatures",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_commit_signature_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures - Delete commit signature protection"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_signatures",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_commit_signature_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures - Get commit signature protection"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_signatures",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_status_check_protection(
        owner: str,
        repo: str,
        branch: str,
        strict: bool | None = None,
        contexts: list | None = None,
        checks: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks - Update status check protection"""
        # HTTP PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if strict is not None:
            query_params["strict"] = str(strict)
        if contexts is not None:
            query_params["contexts"] = str(contexts)
        if checks is not None:
            query_params["checks"] = str(checks)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_remove_status_check_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks - Remove status check protection"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_status_checks_protection(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks - Get status checks protection"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_add_status_check_contexts(
        owner: str,
        repo: str,
        branch: str,
        body: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts - Add status check contexts"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_remove_status_check_contexts(
        owner: str,
        repo: str,
        branch: str,
        body: dict,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts - Remove status check contexts"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_set_status_check_contexts(
        owner: str,
        repo: str,
        branch: str,
        body: dict | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts - Set status check contexts"""
        # HTTP PUT /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_all_status_check_contexts(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts - Get all status check contexts"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions - Delete access restrictions"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions - Get access restrictions"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_add_app_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        apps: list,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps - Add app access restrictions"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if apps is not None:
            query_params["apps"] = str(apps)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_remove_app_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        apps: list,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps - Remove app access restrictions"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if apps is not None:
            query_params["apps"] = str(apps)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_set_app_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        apps: list,
    ) -> str:
        """PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps - Set app access restrictions"""
        # HTTP PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if apps is not None:
            query_params["apps"] = str(apps)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_apps_with_access_to_protected_branch(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps - Get apps with access to the protected branch"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_add_team_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        body: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams - Add team access restrictions"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_remove_team_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        body: dict,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams - Remove team access restrictions"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_set_team_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        body: dict | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams - Set team access restrictions"""
        # HTTP PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_teams_with_access_to_protected_branch(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams - Get teams with access to the protected branch"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_add_user_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        users: list,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users - Add user access restrictions"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if users is not None:
            query_params["users"] = str(users)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_remove_user_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        users: list,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users - Remove user access restrictions"""
        # HTTP DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if users is not None:
            query_params["users"] = str(users)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_set_user_access_restrictions(
        owner: str,
        repo: str,
        branch: str,
        users: list,
    ) -> str:
        """PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users - Set user access restrictions"""
        # HTTP PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if users is not None:
            query_params["users"] = str(users)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_users_with_access_to_protected_branch(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users - Get users with access to the protected branch"""
        # HTTP GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_rename_branch(
        owner: str,
        repo: str,
        branch: str,
        new_name: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/branches/{branch}/rename - Rename a branch"""
        # HTTP POST /repos/{owner}/{repo}/branches/{branch}/rename
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if new_name is not None:
            query_params["new_name"] = str(new_name)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/branches/{branch}/rename",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_create(
        owner: str,
        repo: str,
        name: str,
        head_sha: str,
        details_url: str | None = None,
        external_id: str | None = None,
        status: str | None = None,
        started_at: str | None = None,
        conclusion: str | None = None,
        completed_at: str | None = None,
        output: dict | None = None,
        actions: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/check-runs - Create a check run"""
        # HTTP POST /repos/{owner}/{repo}/check-runs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        if head_sha is not None:
            query_params["head_sha"] = str(head_sha)
        if details_url is not None:
            query_params["details_url"] = str(details_url)
        if external_id is not None:
            query_params["external_id"] = str(external_id)
        if status is not None:
            query_params["status"] = str(status)
        if started_at is not None:
            query_params["started_at"] = str(started_at)
        if conclusion is not None:
            query_params["conclusion"] = str(conclusion)
        if completed_at is not None:
            query_params["completed_at"] = str(completed_at)
        body = output if isinstance(output, dict) else {"data": output}
        if actions is not None:
            query_params["actions"] = str(actions)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/check-runs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_update(
        owner: str,
        repo: str,
        check_run_id: int,
        name: str | None = None,
        details_url: str | None = None,
        external_id: str | None = None,
        started_at: str | None = None,
        status: str | None = None,
        conclusion: str | None = None,
        completed_at: str | None = None,
        output: dict | None = None,
        actions: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/check-runs/{check_run_id} - Update a check run"""
        # HTTP PATCH /repos/{owner}/{repo}/check-runs/{check_run_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_run_id"] = str(check_run_id)
        if name is not None:
            query_params["name"] = str(name)
        if details_url is not None:
            query_params["details_url"] = str(details_url)
        if external_id is not None:
            query_params["external_id"] = str(external_id)
        if started_at is not None:
            query_params["started_at"] = str(started_at)
        if status is not None:
            query_params["status"] = str(status)
        if conclusion is not None:
            query_params["conclusion"] = str(conclusion)
        if completed_at is not None:
            query_params["completed_at"] = str(completed_at)
        body = output if isinstance(output, dict) else {"data": output}
        if actions is not None:
            query_params["actions"] = str(actions)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/check-runs/{check_run_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_get(
        owner: str,
        repo: str,
        check_run_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/check-runs/{check_run_id} - Get a check run"""
        # HTTP GET /repos/{owner}/{repo}/check-runs/{check_run_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_run_id"] = str(check_run_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/check-runs/{check_run_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_list_annotations(
        owner: str,
        repo: str,
        check_run_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/check-runs/{check_run_id}/annotations - List check run annotations"""
        # HTTP GET /repos/{owner}/{repo}/check-runs/{check_run_id}/annotations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_run_id"] = str(check_run_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/check-runs/{check_run_id}/annotations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_rerequest_run(
        owner: str,
        repo: str,
        check_run_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/check-runs/{check_run_id}/rerequest - Rerequest a check run"""
        # HTTP POST /repos/{owner}/{repo}/check-runs/{check_run_id}/rerequest
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_run_id"] = str(check_run_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/check-runs/{check_run_id}/rerequest",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_create_suite(
        owner: str,
        repo: str,
        head_sha: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/check-suites - Create a check suite"""
        # HTTP POST /repos/{owner}/{repo}/check-suites
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if head_sha is not None:
            query_params["head_sha"] = str(head_sha)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/check-suites",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_set_suites_preferences(
        owner: str,
        repo: str,
        auto_trigger_checks: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/check-suites/preferences - Update repository preferences for check suites"""
        # HTTP PATCH /repos/{owner}/{repo}/check-suites/preferences
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if auto_trigger_checks is not None:
            query_params["auto_trigger_checks"] = str(auto_trigger_checks)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/check-suites/preferences",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_get_suite(
        owner: str,
        repo: str,
        check_suite_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/check-suites/{check_suite_id} - Get a check suite"""
        # HTTP GET /repos/{owner}/{repo}/check-suites/{check_suite_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_suite_id"] = str(check_suite_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/check-suites/{check_suite_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_list_for_suite(
        owner: str,
        repo: str,
        check_suite_id: int,
        check_name: str | None = None,
        status: str | None = None,
        filter: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs - List check runs in a check suite"""
        # HTTP GET /repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_suite_id"] = str(check_suite_id)
        if check_name is not None:
            query_params["check_name"] = str(check_name)
        if status is not None:
            query_params["status"] = str(status)
        if filter is not None:
            query_params["filter"] = str(filter)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_rerequest_suite(
        owner: str,
        repo: str,
        check_suite_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest - Rerequest a check suite"""
        # HTTP POST /repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["check_suite_id"] = str(check_suite_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_list_alerts_for_repo(
        owner: str,
        repo: str,
        tool_name: str | None = None,
        tool_guid: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        ref: str | None = None,
        pr: int | None = None,
        direction: str | None = None,
        before: str | None = None,
        after: str | None = None,
        sort: str | None = None,
        state: str | None = None,
        severity: str | None = None,
        assignees: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/alerts - List code scanning alerts for a repository"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if tool_name is not None:
            query_params["tool_name"] = str(tool_name)
        if tool_guid is not None:
            query_params["tool_guid"] = str(tool_guid)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if ref is not None:
            query_params["ref"] = str(ref)
        if pr is not None:
            query_params["pr"] = str(pr)
        if direction is not None:
            query_params["direction"] = str(direction)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if sort is not None:
            query_params["sort"] = str(sort)
        if state is not None:
            query_params["state"] = str(state)
        if severity is not None:
            query_params["severity"] = str(severity)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_update_alert(
        owner: str,
        repo: str,
        alert_number: int,
        state: str | None = None,
        dismissed_reason: str | None = None,
        dismissed_comment: str | None = None,
        create_request: bool | None = None,
        assignees: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/code-scanning/alerts/{alert_number} - Update a code scanning alert"""
        # HTTP PATCH /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if state is not None:
            query_params["state"] = str(state)
        if dismissed_reason is not None:
            query_params["dismissed_reason"] = str(dismissed_reason)
        if dismissed_comment is not None:
            query_params["dismissed_comment"] = str(dismissed_comment)
        if create_request is not None:
            query_params["create_request"] = str(create_request)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_alert(
        owner: str,
        repo: str,
        alert_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number} - Get a code scanning alert"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_create_autofix(
        owner: str,
        repo: str,
        alert_number: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix - Create an autofix for a code scanning alert"""
        # HTTP POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_autofix(
        owner: str,
        repo: str,
        alert_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix - Get the status of an autofix for a code scanning alert"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_commit_autofix(
        owner: str,
        repo: str,
        alert_number: int,
        target_ref: str | None = None,
        message: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix/commits - Commit an autofix for a code scanning alert"""
        # HTTP POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix/commits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if target_ref is not None:
            query_params["target_ref"] = str(target_ref)
        if message is not None:
            query_params["message"] = str(message)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix/commits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_list_alert_instances(
        owner: str,
        repo: str,
        alert_number: int,
        page: int | None = None,
        per_page: int | None = None,
        ref: str | None = None,
        pr: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances - List instances of a code scanning alert"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if ref is not None:
            query_params["ref"] = str(ref)
        if pr is not None:
            query_params["pr"] = str(pr)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_list_recent_analyses(
        owner: str,
        repo: str,
        tool_name: str | None = None,
        tool_guid: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        pr: int | None = None,
        ref: str | None = None,
        sarif_id: str | None = None,
        direction: str | None = None,
        sort: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/analyses - List code scanning analyses for a repository"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/analyses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if tool_name is not None:
            query_params["tool_name"] = str(tool_name)
        if tool_guid is not None:
            query_params["tool_guid"] = str(tool_guid)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if pr is not None:
            query_params["pr"] = str(pr)
        if ref is not None:
            query_params["ref"] = str(ref)
        if sarif_id is not None:
            query_params["sarif_id"] = str(sarif_id)
        if direction is not None:
            query_params["direction"] = str(direction)
        if sort is not None:
            query_params["sort"] = str(sort)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/analyses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_delete_analysis(
        owner: str,
        repo: str,
        analysis_id: int,
        confirm_delete: str | None = None,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id} - Delete a code scanning analysis from a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["analysis_id"] = str(analysis_id)
        if confirm_delete is not None:
            query_params["confirm_delete"] = str(confirm_delete)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_analysis(
        owner: str,
        repo: str,
        analysis_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id} - Get a code scanning analysis for a repository"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["analysis_id"] = str(analysis_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_list_codeql_databases(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/codeql/databases - List CodeQL databases for a repository"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/codeql/databases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/codeql/databases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_delete_codeql_database(
        owner: str,
        repo: str,
        language: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/code-scanning/codeql/databases/{language} - Delete a CodeQL database"""
        # HTTP DELETE /repos/{owner}/{repo}/code-scanning/codeql/databases/{language}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["language"] = str(language)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/code-scanning/codeql/databases/{language}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_codeql_database(
        owner: str,
        repo: str,
        language: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/codeql/databases/{language} - Get a CodeQL database for a repository"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/codeql/databases/{language}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["language"] = str(language)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/codeql/databases/{language}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_create_variant_analysis(
        owner: str,
        repo: str,
        language: str,
        query_pack: str,
        repositories: list | None = None,
        repository_lists: list | None = None,
        repository_owners: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses - Create a CodeQL variant analysis"""
        # HTTP POST /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if language is not None:
            query_params["language"] = str(language)
        if query_pack is not None:
            query_params["query_pack"] = str(query_pack)
        if repositories is not None:
            query_params["repositories"] = str(repositories)
        if repository_lists is not None:
            query_params["repository_lists"] = str(repository_lists)
        if repository_owners is not None:
            query_params["repository_owners"] = str(repository_owners)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/code-scanning/codeql/variant-analyses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_variant_analysis(
        owner: str,
        repo: str,
        codeql_variant_analysis_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id} - Get the summary of a CodeQL variant analysis"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["codeql_variant_analysis_id"] = str(codeql_variant_analysis_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_variant_analysis_repo_task(
        owner: str,
        repo: str,
        codeql_variant_analysis_id: int,
        repo_owner: str,
        repo_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}/repos/{repo_owner}/{repo_name} - Get the analysis status of a repository in a CodeQL variant analysis"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}/repos/{repo_owner}/{repo_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["codeql_variant_analysis_id"] = str(codeql_variant_analysis_id)
        path_params["repo_owner"] = str(repo_owner)
        path_params["repo_name"] = str(repo_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}/repos/{repo_owner}/{repo_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_update_default_setup(
        owner: str,
        repo: str,
        state: str | None = None,
        runner_type: str | None = None,
        runner_label: str | None = None,
        query_suite: str | None = None,
        threat_model: str | None = None,
        languages: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/code-scanning/default-setup - Update a code scanning default setup configuration"""
        # HTTP PATCH /repos/{owner}/{repo}/code-scanning/default-setup
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if state is not None:
            query_params["state"] = str(state)
        if runner_type is not None:
            query_params["runner_type"] = str(runner_type)
        if runner_label is not None:
            query_params["runner_label"] = str(runner_label)
        if query_suite is not None:
            query_params["query_suite"] = str(query_suite)
        if threat_model is not None:
            query_params["threat_model"] = str(threat_model)
        if languages is not None:
            query_params["languages"] = str(languages)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/code-scanning/default-setup",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_default_setup(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/default-setup - Get a code scanning default setup configuration"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/default-setup
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/default-setup",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_upload_sarif(
        owner: str,
        repo: str,
        commit_sha: str,
        ref: str,
        sarif: str,
        checkout_uri: str | None = None,
        started_at: str | None = None,
        tool_name: str | None = None,
        validate: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/code-scanning/sarifs - Upload an analysis as SARIF data"""
        # HTTP POST /repos/{owner}/{repo}/code-scanning/sarifs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if commit_sha is not None:
            query_params["commit_sha"] = str(commit_sha)
        if ref is not None:
            query_params["ref"] = str(ref)
        if sarif is not None:
            query_params["sarif"] = str(sarif)
        if checkout_uri is not None:
            query_params["checkout_uri"] = str(checkout_uri)
        if started_at is not None:
            query_params["started_at"] = str(started_at)
        if tool_name is not None:
            query_params["tool_name"] = str(tool_name)
        if validate is not None:
            query_params["validate"] = str(validate)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/code-scanning/sarifs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_scanning_get_sarif(
        owner: str,
        repo: str,
        sarif_id: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-scanning/sarifs/{sarif_id} - Get information about a SARIF upload"""
        # HTTP GET /repos/{owner}/{repo}/code-scanning/sarifs/{sarif_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["sarif_id"] = str(sarif_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-scanning/sarifs/{sarif_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def code_security_get_configuration_for_repository(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/code-security-configuration - Get the code security configuration associated with a repository"""
        # HTTP GET /repos/{owner}/{repo}/code-security-configuration
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/code-security-configuration",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_codeowners_errors(
        owner: str,
        repo: str,
        ref: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/codeowners/errors - List CODEOWNERS errors"""
        # HTTP GET /repos/{owner}/{repo}/codeowners/errors
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codeowners/errors",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_repos_by_owner_by_repo_codespaces(
        owner: str,
        repo: str,
        ref: str | None = None,
        location: str | None = None,
        geo: str | None = None,
        client_ip: str | None = None,
        machine: str | None = None,
        devcontainer_path: str | None = None,
        multi_repo_permissions_opt_out: bool | None = None,
        working_directory: str | None = None,
        idle_timeout_minutes: int | None = None,
        display_name: str | None = None,
        retention_period_minutes: int | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/codespaces - Create a codespace in a repository"""
        # HTTP POST /repos/{owner}/{repo}/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        if location is not None:
            query_params["location"] = str(location)
        if geo is not None:
            query_params["geo"] = str(geo)
        if client_ip is not None:
            query_params["client_ip"] = str(client_ip)
        if machine is not None:
            query_params["machine"] = str(machine)
        if devcontainer_path is not None:
            query_params["devcontainer_path"] = str(devcontainer_path)
        if multi_repo_permissions_opt_out is not None:
            query_params["multi_repo_permissions_opt_out"] = str(multi_repo_permissions_opt_out)
        if working_directory is not None:
            query_params["working_directory"] = str(working_directory)
        if idle_timeout_minutes is not None:
            query_params["idle_timeout_minutes"] = str(idle_timeout_minutes)
        if display_name is not None:
            query_params["display_name"] = str(display_name)
        if retention_period_minutes is not None:
            query_params["retention_period_minutes"] = str(retention_period_minutes)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_codespaces(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces - List codespaces in a repository for the authenticated user"""
        # HTTP GET /repos/{owner}/{repo}/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_codespaces_devcontainers(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/devcontainers - List devcontainer configurations in a repository for the authenticated user"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/devcontainers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/devcontainers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_repo_machines_for_authenticated_user(
        owner: str,
        repo: str,
        location: str | None = None,
        client_ip: str | None = None,
        ref: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/machines - List available machine types for a repository"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/machines
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if location is not None:
            query_params["location"] = str(location)
        if client_ip is not None:
            query_params["client_ip"] = str(client_ip)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/machines",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_codespaces_new(
        owner: str,
        repo: str,
        ref: str | None = None,
        client_ip: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/new - Get default attributes for a codespace"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/new
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        if client_ip is not None:
            query_params["client_ip"] = str(client_ip)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/new",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_check_permissions_for_devcontainer(
        owner: str,
        repo: str,
        ref: str,
        devcontainer_path: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/permissions_check - Check if permissions defined by a devcontainer have been accepted by the authenticated user"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/permissions_check
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        if devcontainer_path is not None:
            query_params["devcontainer_path"] = str(devcontainer_path)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/permissions_check",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_list_repo_secrets(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/secrets - List repository secrets"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_repo_public_key(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/secrets/public-key - Get a repository public key"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_delete_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/codespaces/secrets/{secret_name} - Delete a repository secret"""
        # HTTP DELETE /repos/{owner}/{repo}/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_create_or_update_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
        encrypted_value: str | None = None,
        key_id: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/codespaces/secrets/{secret_name} - Create or update a repository secret"""
        # HTTP PUT /repos/{owner}/{repo}/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/codespaces/secrets/{secret_name} - Get a repository secret"""
        # HTTP GET /repos/{owner}/{repo}/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_collaborators(
        owner: str,
        repo: str,
        affiliation: str | None = None,
        permission: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/collaborators - List repository collaborators"""
        # HTTP GET /repos/{owner}/{repo}/collaborators
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if affiliation is not None:
            query_params["affiliation"] = str(affiliation)
        if permission is not None:
            query_params["permission"] = str(permission)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/collaborators",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_remove_collaborator(
        owner: str,
        repo: str,
        username: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/collaborators/{username} - Remove a repository collaborator"""
        # HTTP DELETE /repos/{owner}/{repo}/collaborators/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/collaborators/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_add_collaborator(
        owner: str,
        repo: str,
        username: str,
        permission: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/collaborators/{username} - Add a repository collaborator"""
        # HTTP PUT /repos/{owner}/{repo}/collaborators/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["username"] = str(username)
        if permission is not None:
            query_params["permission"] = str(permission)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/collaborators/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_check_collaborator(
        owner: str,
        repo: str,
        username: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/collaborators/{username} - Check if a user is a repository collaborator"""
        # HTTP GET /repos/{owner}/{repo}/collaborators/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/collaborators/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_collaborator_permission_level(
        owner: str,
        repo: str,
        username: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/collaborators/{username}/permission - Get repository permissions for a user"""
        # HTTP GET /repos/{owner}/{repo}/collaborators/{username}/permission
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/collaborators/{username}/permission",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_commit_comments_for_repo(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/comments - List commit comments for a repository"""
        # HTTP GET /repos/{owner}/{repo}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_commit_comment(
        owner: str,
        repo: str,
        comment_id: int,
        body: str,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/comments/{comment_id} - Update a commit comment"""
        # HTTP PATCH /repos/{owner}/{repo}/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_commit_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/comments/{comment_id} - Delete a commit comment"""
        # HTTP DELETE /repos/{owner}/{repo}/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_commit_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/comments/{comment_id} - Get a commit comment"""
        # HTTP GET /repos/{owner}/{repo}/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_create_for_commit_comment(
        owner: str,
        repo: str,
        comment_id: int,
        content: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/comments/{comment_id}/reactions - Create reaction for a commit comment"""
        # HTTP POST /repos/{owner}/{repo}/comments/{comment_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if content is not None:
            query_params["content"] = str(content)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/comments/{comment_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_list_for_commit_comment(
        owner: str,
        repo: str,
        comment_id: int,
        content: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/comments/{comment_id}/reactions - List reactions for a commit comment"""
        # HTTP GET /repos/{owner}/{repo}/comments/{comment_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if content is not None:
            query_params["content"] = str(content)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/comments/{comment_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_delete_for_commit_comment(
        owner: str,
        repo: str,
        comment_id: int,
        reaction_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id} - Delete a commit comment reaction"""
        # HTTP DELETE /repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        path_params["reaction_id"] = str(reaction_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_commits(
        owner: str,
        repo: str,
        sha: str | None = None,
        path: str | None = None,
        author: str | None = None,
        committer: str | None = None,
        since: str | None = None,
        until: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits - List commits"""
        # HTTP GET /repos/{owner}/{repo}/commits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if sha is not None:
            query_params["sha"] = str(sha)
        if path is not None:
            query_params["path"] = str(path)
        if author is not None:
            query_params["author"] = str(author)
        if committer is not None:
            query_params["committer"] = str(committer)
        if since is not None:
            query_params["since"] = str(since)
        if until is not None:
            query_params["until"] = str(until)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_branches_for_head_commit(
        owner: str,
        repo: str,
        commit_sha: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head - List branches for HEAD commit"""
        # HTTP GET /repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["commit_sha"] = str(commit_sha)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_commit_comment(
        owner: str,
        repo: str,
        commit_sha: str,
        body: str,
        path: str | None = None,
        position: int | None = None,
        line: int | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/commits/{commit_sha}/comments - Create a commit comment"""
        # HTTP POST /repos/{owner}/{repo}/commits/{commit_sha}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["commit_sha"] = str(commit_sha)
        if body is not None:
            query_params["body"] = str(body)
        if path is not None:
            query_params["path"] = str(path)
        if position is not None:
            query_params["position"] = str(position)
        if line is not None:
            query_params["line"] = str(line)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/commits/{commit_sha}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_comments_for_commit(
        owner: str,
        repo: str,
        commit_sha: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{commit_sha}/comments - List commit comments"""
        # HTTP GET /repos/{owner}/{repo}/commits/{commit_sha}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["commit_sha"] = str(commit_sha)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{commit_sha}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_pull_requests_associated_with_commit(
        owner: str,
        repo: str,
        commit_sha: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls - List pull requests associated with a commit"""
        # HTTP GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["commit_sha"] = str(commit_sha)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{commit_sha}/pulls",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_commit(
        owner: str,
        repo: str,
        ref: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{ref} - Get a commit"""
        # HTTP GET /repos/{owner}/{repo}/commits/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        path_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_list_for_ref(
        owner: str,
        repo: str,
        ref: str,
        check_name: str | None = None,
        status: str | None = None,
        filter: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
        app_id: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{ref}/check-runs - List check runs for a Git reference"""
        # HTTP GET /repos/{owner}/{repo}/commits/{ref}/check-runs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        if check_name is not None:
            query_params["check_name"] = str(check_name)
        if status is not None:
            query_params["status"] = str(status)
        if filter is not None:
            query_params["filter"] = str(filter)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if app_id is not None:
            query_params["app_id"] = str(app_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{ref}/check-runs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def checks_list_suites_for_ref(
        owner: str,
        repo: str,
        ref: str,
        app_id: int | None = None,
        check_name: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{ref}/check-suites - List check suites for a Git reference"""
        # HTTP GET /repos/{owner}/{repo}/commits/{ref}/check-suites
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        if app_id is not None:
            query_params["app_id"] = str(app_id)
        if check_name is not None:
            query_params["check_name"] = str(check_name)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{ref}/check-suites",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_combined_status_for_ref(
        owner: str,
        repo: str,
        ref: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{ref}/status - Get the combined status for a specific reference"""
        # HTTP GET /repos/{owner}/{repo}/commits/{ref}/status
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{ref}/status",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_commit_statuses_for_ref(
        owner: str,
        repo: str,
        ref: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/commits/{ref}/statuses - List commit statuses for a reference"""
        # HTTP GET /repos/{owner}/{repo}/commits/{ref}/statuses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/commits/{ref}/statuses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_community_profile_metrics(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/community/profile - Get community profile metrics"""
        # HTTP GET /repos/{owner}/{repo}/community/profile
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/community/profile",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_compare_commits(
        owner: str,
        repo: str,
        basehead: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/compare/{basehead} - Compare two commits"""
        # HTTP GET /repos/{owner}/{repo}/compare/{basehead}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        path_params["basehead"] = str(basehead)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/compare/{basehead}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_file(
        owner: str,
        repo: str,
        path: str,
        message: str,
        sha: str,
        branch: str | None = None,
        committer: dict | None = None,
        author: dict | None = None,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/contents/{path} - Delete a file"""
        # HTTP DELETE /repos/{owner}/{repo}/contents/{path}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["path"] = str(path)
        if message is not None:
            query_params["message"] = str(message)
        if sha is not None:
            query_params["sha"] = str(sha)
        if branch is not None:
            query_params["branch"] = str(branch)
        body = committer if isinstance(committer, dict) else {"data": committer}
        body = author if isinstance(author, dict) else {"data": author}
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/contents/{path}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_or_update_file_contents(
        owner: str,
        repo: str,
        path: str,
        message: str,
        content: str,
        sha: str | None = None,
        branch: str | None = None,
        committer: dict | None = None,
        author: dict | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/contents/{path} - Create or update file contents"""
        # HTTP PUT /repos/{owner}/{repo}/contents/{path}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["path"] = str(path)
        if message is not None:
            query_params["message"] = str(message)
        if content is not None:
            query_params["content"] = str(content)
        if sha is not None:
            query_params["sha"] = str(sha)
        if branch is not None:
            query_params["branch"] = str(branch)
        body = committer if isinstance(committer, dict) else {"data": committer}
        body = author if isinstance(author, dict) else {"data": author}
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/contents/{path}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_content(
        owner: str,
        repo: str,
        path: str,
        ref: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/contents/{path} - Get repository content"""
        # HTTP GET /repos/{owner}/{repo}/contents/{path}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["path"] = str(path)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/contents/{path}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_contributors(
        owner: str,
        repo: str,
        anon: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/contributors - List repository contributors"""
        # HTTP GET /repos/{owner}/{repo}/contributors
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if anon is not None:
            query_params["anon"] = str(anon)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/contributors",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_list_alerts_for_repo(
        owner: str,
        repo: str,
        state: str | None = None,
        severity: str | None = None,
        ecosystem: str | None = None,
        package: str | None = None,
        manifest: str | None = None,
        epss_percentage: str | None = None,
        has: dict | None = None,
        assignee: str | None = None,
        scope: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependabot/alerts - List Dependabot alerts for a repository"""
        # HTTP GET /repos/{owner}/{repo}/dependabot/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if state is not None:
            query_params["state"] = str(state)
        if severity is not None:
            query_params["severity"] = str(severity)
        if ecosystem is not None:
            query_params["ecosystem"] = str(ecosystem)
        if package is not None:
            query_params["package"] = str(package)
        if manifest is not None:
            query_params["manifest"] = str(manifest)
        if epss_percentage is not None:
            query_params["epss_percentage"] = str(epss_percentage)
        body = has if isinstance(has, dict) else {"data": has}
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if scope is not None:
            query_params["scope"] = str(scope)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependabot/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_update_alert(
        owner: str,
        repo: str,
        alert_number: int,
        state: str | None = None,
        dismissed_reason: str | None = None,
        dismissed_comment: str | None = None,
        assignees: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/dependabot/alerts/{alert_number} - Update a Dependabot alert"""
        # HTTP PATCH /repos/{owner}/{repo}/dependabot/alerts/{alert_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if state is not None:
            query_params["state"] = str(state)
        if dismissed_reason is not None:
            query_params["dismissed_reason"] = str(dismissed_reason)
        if dismissed_comment is not None:
            query_params["dismissed_comment"] = str(dismissed_comment)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/dependabot/alerts/{alert_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_get_alert(
        owner: str,
        repo: str,
        alert_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependabot/alerts/{alert_number} - Get a Dependabot alert"""
        # HTTP GET /repos/{owner}/{repo}/dependabot/alerts/{alert_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependabot/alerts/{alert_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_list_repo_secrets(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependabot/secrets - List repository secrets"""
        # HTTP GET /repos/{owner}/{repo}/dependabot/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependabot/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_get_repo_public_key(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependabot/secrets/public-key - Get a repository public key"""
        # HTTP GET /repos/{owner}/{repo}/dependabot/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependabot/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_delete_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/dependabot/secrets/{secret_name} - Delete a repository secret"""
        # HTTP DELETE /repos/{owner}/{repo}/dependabot/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/dependabot/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_create_or_update_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
        encrypted_value: str | None = None,
        key_id: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/dependabot/secrets/{secret_name} - Create or update a repository secret"""
        # HTTP PUT /repos/{owner}/{repo}/dependabot/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/dependabot/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependabot_get_repo_secret(
        owner: str,
        repo: str,
        secret_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependabot/secrets/{secret_name} - Get a repository secret"""
        # HTTP GET /repos/{owner}/{repo}/dependabot/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependabot/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependency_graph_diff_range(
        owner: str,
        repo: str,
        basehead: str,
        name: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependency-graph/compare/{basehead} - Get a diff of the dependencies between commits"""
        # HTTP GET /repos/{owner}/{repo}/dependency-graph/compare/{basehead}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["basehead"] = str(basehead)
        if name is not None:
            query_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependency-graph/compare/{basehead}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependency_graph_export_sbom(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/dependency-graph/sbom - Export a software bill of materials (SBOM) for a repository."""
        # HTTP GET /repos/{owner}/{repo}/dependency-graph/sbom
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/dependency-graph/sbom",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def dependency_graph_create_repository_snapshot(
        owner: str,
        repo: str,
        version: int,
        job: dict,
        sha: str,
        ref: str,
        detector: dict,
        scanned: str,
        metadata: dict | None = None,
        manifests: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/dependency-graph/snapshots - Create a snapshot of dependencies for a repository"""
        # HTTP POST /repos/{owner}/{repo}/dependency-graph/snapshots
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if version is not None:
            query_params["version"] = str(version)
        body = job if isinstance(job, dict) else {"data": job}
        if sha is not None:
            query_params["sha"] = str(sha)
        if ref is not None:
            query_params["ref"] = str(ref)
        body = detector if isinstance(detector, dict) else {"data": detector}
        body = metadata if isinstance(metadata, dict) else {"data": metadata}
        body = manifests if isinstance(manifests, dict) else {"data": manifests}
        if scanned is not None:
            query_params["scanned"] = str(scanned)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/dependency-graph/snapshots",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_deployment(
        owner: str,
        repo: str,
        ref: str,
        task: str | None = None,
        auto_merge: bool | None = None,
        required_contexts: list | None = None,
        payload: dict | None = None,
        environment: str | None = None,
        description: str | None = None,
        transient_environment: bool | None = None,
        production_environment: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/deployments - Create a deployment"""
        # HTTP POST /repos/{owner}/{repo}/deployments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        if task is not None:
            query_params["task"] = str(task)
        if auto_merge is not None:
            query_params["auto_merge"] = str(auto_merge)
        if required_contexts is not None:
            query_params["required_contexts"] = str(required_contexts)
        body = payload if isinstance(payload, dict) else {"data": payload}
        if environment is not None:
            query_params["environment"] = str(environment)
        if description is not None:
            query_params["description"] = str(description)
        if transient_environment is not None:
            query_params["transient_environment"] = str(transient_environment)
        if production_environment is not None:
            query_params["production_environment"] = str(production_environment)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/deployments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_deployments(
        owner: str,
        repo: str,
        sha: str | None = None,
        ref: str | None = None,
        task: str | None = None,
        environment: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/deployments - List deployments"""
        # HTTP GET /repos/{owner}/{repo}/deployments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if sha is not None:
            query_params["sha"] = str(sha)
        if ref is not None:
            query_params["ref"] = str(ref)
        if task is not None:
            query_params["task"] = str(task)
        if environment is not None:
            query_params["environment"] = str(environment)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/deployments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_deployment(
        owner: str,
        repo: str,
        deployment_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/deployments/{deployment_id} - Delete a deployment"""
        # HTTP DELETE /repos/{owner}/{repo}/deployments/{deployment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["deployment_id"] = str(deployment_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/deployments/{deployment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_deployment(
        owner: str,
        repo: str,
        deployment_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/deployments/{deployment_id} - Get a deployment"""
        # HTTP GET /repos/{owner}/{repo}/deployments/{deployment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["deployment_id"] = str(deployment_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/deployments/{deployment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_deployment_status(
        owner: str,
        repo: str,
        deployment_id: int,
        state: str,
        target_url: str | None = None,
        log_url: str | None = None,
        description: str | None = None,
        environment: str | None = None,
        environment_url: str | None = None,
        auto_inactive: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/deployments/{deployment_id}/statuses - Create a deployment status"""
        # HTTP POST /repos/{owner}/{repo}/deployments/{deployment_id}/statuses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["deployment_id"] = str(deployment_id)
        if state is not None:
            query_params["state"] = str(state)
        if target_url is not None:
            query_params["target_url"] = str(target_url)
        if log_url is not None:
            query_params["log_url"] = str(log_url)
        if description is not None:
            query_params["description"] = str(description)
        if environment is not None:
            query_params["environment"] = str(environment)
        if environment_url is not None:
            query_params["environment_url"] = str(environment_url)
        if auto_inactive is not None:
            query_params["auto_inactive"] = str(auto_inactive)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/deployments/{deployment_id}/statuses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_deployment_statuses(
        owner: str,
        repo: str,
        deployment_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses - List deployment statuses"""
        # HTTP GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["deployment_id"] = str(deployment_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/deployments/{deployment_id}/statuses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_deployment_status(
        owner: str,
        repo: str,
        deployment_id: int,
        status_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id} - Get a deployment status"""
        # HTTP GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["deployment_id"] = str(deployment_id)
        path_params["status_id"] = str(status_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_dispatch_event(
        owner: str,
        repo: str,
        event_type: str,
        client_payload: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/dispatches - Create a repository dispatch event"""
        # HTTP POST /repos/{owner}/{repo}/dispatches
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if event_type is not None:
            query_params["event_type"] = str(event_type)
        body = client_payload if isinstance(client_payload, dict) else {"data": client_payload}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/dispatches",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_all_environments(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments - List environments"""
        # HTTP GET /repos/{owner}/{repo}/environments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_an_environment(
        owner: str,
        repo: str,
        environment_name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/environments/{environment_name} - Delete an environment"""
        # HTTP DELETE /repos/{owner}/{repo}/environments/{environment_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/environments/{environment_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_or_update_environment(
        owner: str,
        repo: str,
        environment_name: str,
        wait_timer: int | None = None,
        prevent_self_review: bool | None = None,
        reviewers: list | None = None,
        deployment_branch_policy: dict | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/environments/{environment_name} - Create or update an environment"""
        # HTTP PUT /repos/{owner}/{repo}/environments/{environment_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        if wait_timer is not None:
            query_params["wait_timer"] = str(wait_timer)
        if prevent_self_review is not None:
            query_params["prevent_self_review"] = str(prevent_self_review)
        if reviewers is not None:
            query_params["reviewers"] = str(reviewers)
        body = deployment_branch_policy if isinstance(deployment_branch_policy, dict) else {"data": deployment_branch_policy}
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/environments/{environment_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_environment(
        owner: str,
        repo: str,
        environment_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name} - Get an environment"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_deployment_branch_policy(
        owner: str,
        repo: str,
        environment_name: str,
        name: str,
        type: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies - Create a deployment branch policy"""
        # HTTP POST /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        if name is not None:
            query_params["name"] = str(name)
        if type is not None:
            query_params["type"] = str(type)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_deployment_branch_policies(
        owner: str,
        repo: str,
        environment_name: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies - List deployment branch policies"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_deployment_branch_policy(
        owner: str,
        repo: str,
        environment_name: str,
        branch_policy_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id} - Delete a deployment branch policy"""
        # HTTP DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["branch_policy_id"] = str(branch_policy_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_deployment_branch_policy(
        owner: str,
        repo: str,
        environment_name: str,
        branch_policy_id: int,
        name: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id} - Update a deployment branch policy"""
        # HTTP PUT /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["branch_policy_id"] = str(branch_policy_id)
        if name is not None:
            query_params["name"] = str(name)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_deployment_branch_policy(
        owner: str,
        repo: str,
        environment_name: str,
        branch_policy_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id} - Get a deployment branch policy"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["branch_policy_id"] = str(branch_policy_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_deployment_protection_rule(
        environment_name: str,
        repo: str,
        owner: str,
        integration_id: int | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules - Create a custom deployment protection rule on an environment"""
        # HTTP POST /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["environment_name"] = str(environment_name)
        path_params["repo"] = str(repo)
        path_params["owner"] = str(owner)
        if integration_id is not None:
            query_params["integration_id"] = str(integration_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_all_deployment_protection_rules(
        environment_name: str,
        repo: str,
        owner: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules - Get all deployment protection rules for an environment"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["environment_name"] = str(environment_name)
        path_params["repo"] = str(repo)
        path_params["owner"] = str(owner)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_custom_deployment_rule_integrations(
        environment_name: str,
        repo: str,
        owner: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/apps - List custom deployment rule integrations available for an environment"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/apps
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["environment_name"] = str(environment_name)
        path_params["repo"] = str(repo)
        path_params["owner"] = str(owner)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/apps",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_disable_deployment_protection_rule(
        environment_name: str,
        repo: str,
        owner: str,
        protection_rule_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id} - Disable a custom protection rule for an environment"""
        # HTTP DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["environment_name"] = str(environment_name)
        path_params["repo"] = str(repo)
        path_params["owner"] = str(owner)
        path_params["protection_rule_id"] = str(protection_rule_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_custom_deployment_protection_rule(
        owner: str,
        repo: str,
        environment_name: str,
        protection_rule_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id} - Get a custom deployment protection rule"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["protection_rule_id"] = str(protection_rule_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_environment_secrets(
        owner: str,
        repo: str,
        environment_name: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/secrets - List environment secrets"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_environment_public_key(
        owner: str,
        repo: str,
        environment_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/public-key - Get an environment public key"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_environment_secret(
        owner: str,
        repo: str,
        environment_name: str,
        secret_name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name} - Delete an environment secret"""
        # HTTP DELETE /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_or_update_environment_secret(
        owner: str,
        repo: str,
        environment_name: str,
        secret_name: str,
        encrypted_value: str,
        key_id: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name} - Create or update an environment secret"""
        # HTTP PUT /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_environment_secret(
        owner: str,
        repo: str,
        environment_name: str,
        secret_name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name} - Get an environment secret"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_create_environment_variable(
        owner: str,
        repo: str,
        environment_name: str,
        name: str,
        value: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/environments/{environment_name}/variables - Create an environment variable"""
        # HTTP POST /repos/{owner}/{repo}/environments/{environment_name}/variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        if name is not None:
            query_params["name"] = str(name)
        if value is not None:
            query_params["value"] = str(value)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/environments/{environment_name}/variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_list_environment_variables(
        owner: str,
        repo: str,
        environment_name: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/variables - List environment variables"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/variables
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/variables",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_update_environment_variable(
        owner: str,
        repo: str,
        name: str,
        environment_name: str,
        value: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/environments/{environment_name}/variables/{name} - Update an environment variable"""
        # HTTP PATCH /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        path_params["environment_name"] = str(environment_name)
        if value is not None:
            query_params["value"] = str(value)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/environments/{environment_name}/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_delete_environment_variable(
        owner: str,
        repo: str,
        name: str,
        environment_name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/environments/{environment_name}/variables/{name} - Delete an environment variable"""
        # HTTP DELETE /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        path_params["environment_name"] = str(environment_name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/environments/{environment_name}/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def actions_get_environment_variable(
        owner: str,
        repo: str,
        environment_name: str,
        name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/environments/{environment_name}/variables/{name} - Get an environment variable"""
        # HTTP GET /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["environment_name"] = str(environment_name)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/environments/{environment_name}/variables/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_repo_events(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/events - List repository events"""
        # HTTP GET /repos/{owner}/{repo}/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_fork(
        owner: str,
        repo: str,
        organization: str | None = None,
        name: str | None = None,
        default_branch_only: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/forks - Create a fork"""
        # HTTP POST /repos/{owner}/{repo}/forks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if organization is not None:
            query_params["organization"] = str(organization)
        if name is not None:
            query_params["name"] = str(name)
        if default_branch_only is not None:
            query_params["default_branch_only"] = str(default_branch_only)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/forks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_forks(
        owner: str,
        repo: str,
        sort: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/forks - List forks"""
        # HTTP GET /repos/{owner}/{repo}/forks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if sort is not None:
            query_params["sort"] = str(sort)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/forks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_create_blob(
        owner: str,
        repo: str,
        content: str,
        encoding: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/git/blobs - Create a blob"""
        # HTTP POST /repos/{owner}/{repo}/git/blobs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if content is not None:
            query_params["content"] = str(content)
        if encoding is not None:
            query_params["encoding"] = str(encoding)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/git/blobs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_get_blob(
        owner: str,
        repo: str,
        file_sha: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/git/blobs/{file_sha} - Get a blob"""
        # HTTP GET /repos/{owner}/{repo}/git/blobs/{file_sha}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["file_sha"] = str(file_sha)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/git/blobs/{file_sha}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_create_commit(
        owner: str,
        repo: str,
        message: str,
        tree: str,
        parents: list | None = None,
        author: dict | None = None,
        committer: dict | None = None,
        signature: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/git/commits - Create a commit"""
        # HTTP POST /repos/{owner}/{repo}/git/commits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if message is not None:
            query_params["message"] = str(message)
        if tree is not None:
            query_params["tree"] = str(tree)
        if parents is not None:
            query_params["parents"] = str(parents)
        body = author if isinstance(author, dict) else {"data": author}
        body = committer if isinstance(committer, dict) else {"data": committer}
        if signature is not None:
            query_params["signature"] = str(signature)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/git/commits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_get_commit(
        owner: str,
        repo: str,
        commit_sha: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/git/commits/{commit_sha} - Get a commit object"""
        # HTTP GET /repos/{owner}/{repo}/git/commits/{commit_sha}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["commit_sha"] = str(commit_sha)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/git/commits/{commit_sha}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_list_matching_refs(
        owner: str,
        repo: str,
        ref: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/git/matching-refs/{ref} - List matching references"""
        # HTTP GET /repos/{owner}/{repo}/git/matching-refs/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/git/matching-refs/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_get_ref(
        owner: str,
        repo: str,
        ref: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/git/ref/{ref} - Get a reference"""
        # HTTP GET /repos/{owner}/{repo}/git/ref/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/git/ref/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_create_ref(
        owner: str,
        repo: str,
        ref: str,
        sha: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/git/refs - Create a reference"""
        # HTTP POST /repos/{owner}/{repo}/git/refs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        if sha is not None:
            query_params["sha"] = str(sha)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/git/refs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_update_ref(
        owner: str,
        repo: str,
        ref: str,
        sha: str,
        force: bool | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/git/refs/{ref} - Update a reference"""
        # HTTP PATCH /repos/{owner}/{repo}/git/refs/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        if sha is not None:
            query_params["sha"] = str(sha)
        if force is not None:
            query_params["force"] = str(force)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/git/refs/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_delete_ref(
        owner: str,
        repo: str,
        ref: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/git/refs/{ref} - Delete a reference"""
        # HTTP DELETE /repos/{owner}/{repo}/git/refs/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/git/refs/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_create_tag(
        owner: str,
        repo: str,
        tag: str,
        message: str,
        object: str,
        type: str,
        tagger: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/git/tags - Create a tag object"""
        # HTTP POST /repos/{owner}/{repo}/git/tags
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if tag is not None:
            query_params["tag"] = str(tag)
        if message is not None:
            query_params["message"] = str(message)
        if object is not None:
            query_params["object"] = str(object)
        if type is not None:
            query_params["type"] = str(type)
        body = tagger if isinstance(tagger, dict) else {"data": tagger}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/git/tags",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_get_tag(
        owner: str,
        repo: str,
        tag_sha: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/git/tags/{tag_sha} - Get a tag"""
        # HTTP GET /repos/{owner}/{repo}/git/tags/{tag_sha}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["tag_sha"] = str(tag_sha)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/git/tags/{tag_sha}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_create_tree(
        owner: str,
        repo: str,
        tree: list,
        base_tree: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/git/trees - Create a tree"""
        # HTTP POST /repos/{owner}/{repo}/git/trees
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if tree is not None:
            query_params["tree"] = str(tree)
        if base_tree is not None:
            query_params["base_tree"] = str(base_tree)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/git/trees",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def git_get_tree(
        owner: str,
        repo: str,
        tree_sha: str,
        recursive: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/git/trees/{tree_sha} - Get a tree"""
        # HTTP GET /repos/{owner}/{repo}/git/trees/{tree_sha}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["tree_sha"] = str(tree_sha)
        if recursive is not None:
            query_params["recursive"] = str(recursive)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/git/trees/{tree_sha}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_webhook(
        owner: str,
        repo: str,
        name: str | None = None,
        config: dict | None = None,
        events: list | None = None,
        active: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/hooks - Create a repository webhook"""
        # HTTP POST /repos/{owner}/{repo}/hooks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        body = config if isinstance(config, dict) else {"data": config}
        if events is not None:
            query_params["events"] = str(events)
        if active is not None:
            query_params["active"] = str(active)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/hooks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_webhooks(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/hooks - List repository webhooks"""
        # HTTP GET /repos/{owner}/{repo}/hooks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/hooks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_webhook(
        owner: str,
        repo: str,
        hook_id: int,
        config: dict | None = None,
        events: list | None = None,
        add_events: list | None = None,
        remove_events: list | None = None,
        active: bool | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/hooks/{hook_id} - Update a repository webhook"""
        # HTTP PATCH /repos/{owner}/{repo}/hooks/{hook_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        body = config if isinstance(config, dict) else {"data": config}
        if events is not None:
            query_params["events"] = str(events)
        if add_events is not None:
            query_params["add_events"] = str(add_events)
        if remove_events is not None:
            query_params["remove_events"] = str(remove_events)
        if active is not None:
            query_params["active"] = str(active)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/hooks/{hook_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_webhook(
        owner: str,
        repo: str,
        hook_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/hooks/{hook_id} - Delete a repository webhook"""
        # HTTP DELETE /repos/{owner}/{repo}/hooks/{hook_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/hooks/{hook_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_webhook(
        owner: str,
        repo: str,
        hook_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/hooks/{hook_id} - Get a repository webhook"""
        # HTTP GET /repos/{owner}/{repo}/hooks/{hook_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/hooks/{hook_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_webhook_config_for_repo(
        owner: str,
        repo: str,
        hook_id: int,
        url: str | None = None,
        content_type: str | None = None,
        secret: str | None = None,
        insecure_ssl: dict | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/hooks/{hook_id}/config - Update a webhook configuration for a repository"""
        # HTTP PATCH /repos/{owner}/{repo}/hooks/{hook_id}/config
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        if url is not None:
            query_params["url"] = str(url)
        if content_type is not None:
            query_params["content_type"] = str(content_type)
        if secret is not None:
            query_params["secret"] = str(secret)
        body = insecure_ssl if isinstance(insecure_ssl, dict) else {"data": insecure_ssl}
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/hooks/{hook_id}/config",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_webhook_config_for_repo(
        owner: str,
        repo: str,
        hook_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/hooks/{hook_id}/config - Get a webhook configuration for a repository"""
        # HTTP GET /repos/{owner}/{repo}/hooks/{hook_id}/config
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/hooks/{hook_id}/config",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_webhook_deliveries(
        owner: str,
        repo: str,
        hook_id: int,
        per_page: int | None = None,
        cursor: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries - List deliveries for a repository webhook"""
        # HTTP GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if cursor is not None:
            query_params["cursor"] = str(cursor)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/hooks/{hook_id}/deliveries",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_webhook_delivery(
        owner: str,
        repo: str,
        hook_id: int,
        delivery_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id} - Get a delivery for a repository webhook"""
        # HTTP GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        path_params["delivery_id"] = str(delivery_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_redeliver_webhook_delivery(
        owner: str,
        repo: str,
        hook_id: int,
        delivery_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts - Redeliver a delivery for a repository webhook"""
        # HTTP POST /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        path_params["delivery_id"] = str(delivery_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_ping_webhook(
        owner: str,
        repo: str,
        hook_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/hooks/{hook_id}/pings - Ping a repository webhook"""
        # HTTP POST /repos/{owner}/{repo}/hooks/{hook_id}/pings
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/hooks/{hook_id}/pings",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_test_push_webhook(
        owner: str,
        repo: str,
        hook_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/hooks/{hook_id}/tests - Test the push repository webhook"""
        # HTTP POST /repos/{owner}/{repo}/hooks/{hook_id}/tests
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["hook_id"] = str(hook_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/hooks/{hook_id}/tests",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_disable_immutable_releases(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/immutable-releases - Disable immutable releases"""
        # HTTP DELETE /repos/{owner}/{repo}/immutable-releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/immutable-releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_enable_immutable_releases(
        owner: str,
        repo: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/immutable-releases - Enable immutable releases"""
        # HTTP PUT /repos/{owner}/{repo}/immutable-releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/immutable-releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_check_immutable_releases(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/immutable-releases - Check if immutable releases are enabled for a repository"""
        # HTTP GET /repos/{owner}/{repo}/immutable-releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/immutable-releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_update_import(
        owner: str,
        repo: str,
        vcs_username: str | None = None,
        vcs_password: str | None = None,
        vcs: str | None = None,
        tfvc_project: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/import - Update an import"""
        # HTTP PATCH /repos/{owner}/{repo}/import
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if vcs_username is not None:
            query_params["vcs_username"] = str(vcs_username)
        if vcs_password is not None:
            query_params["vcs_password"] = str(vcs_password)
        if vcs is not None:
            query_params["vcs"] = str(vcs)
        if tfvc_project is not None:
            query_params["tfvc_project"] = str(tfvc_project)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/import",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_cancel_import(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/import - Cancel an import"""
        # HTTP DELETE /repos/{owner}/{repo}/import
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/import",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_start_import(
        owner: str,
        repo: str,
        vcs_url: str,
        vcs: str | None = None,
        vcs_username: str | None = None,
        vcs_password: str | None = None,
        tfvc_project: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/import - Start an import"""
        # HTTP PUT /repos/{owner}/{repo}/import
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if vcs_url is not None:
            query_params["vcs_url"] = str(vcs_url)
        if vcs is not None:
            query_params["vcs"] = str(vcs)
        if vcs_username is not None:
            query_params["vcs_username"] = str(vcs_username)
        if vcs_password is not None:
            query_params["vcs_password"] = str(vcs_password)
        if tfvc_project is not None:
            query_params["tfvc_project"] = str(tfvc_project)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/import",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_get_import_status(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/import - Get an import status"""
        # HTTP GET /repos/{owner}/{repo}/import
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/import",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_get_commit_authors(
        owner: str,
        repo: str,
        since: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/import/authors - Get commit authors"""
        # HTTP GET /repos/{owner}/{repo}/import/authors
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if since is not None:
            query_params["since"] = str(since)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/import/authors",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_map_commit_author(
        owner: str,
        repo: str,
        author_id: int,
        email: str | None = None,
        name: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/import/authors/{author_id} - Map a commit author"""
        # HTTP PATCH /repos/{owner}/{repo}/import/authors/{author_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["author_id"] = str(author_id)
        if email is not None:
            query_params["email"] = str(email)
        if name is not None:
            query_params["name"] = str(name)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/import/authors/{author_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_get_large_files(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/import/large_files - Get large files"""
        # HTTP GET /repos/{owner}/{repo}/import/large_files
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/import/large_files",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_set_lfs_preference(
        owner: str,
        repo: str,
        use_lfs: str,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/import/lfs - Update Git LFS preference"""
        # HTTP PATCH /repos/{owner}/{repo}/import/lfs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if use_lfs is not None:
            query_params["use_lfs"] = str(use_lfs)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/import/lfs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_repo_installation(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/installation - Get a repository installation for the authenticated app"""
        # HTTP GET /repos/{owner}/{repo}/installation
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/installation",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def interactions_remove_restrictions_for_repo(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/interaction-limits - Remove interaction restrictions for a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def interactions_set_restrictions_for_repo(
        owner: str,
        repo: str,
        limit: str,
        expiry: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/interaction-limits - Set interaction restrictions for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if limit is not None:
            query_params["limit"] = str(limit)
        if expiry is not None:
            query_params["expiry"] = str(expiry)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def interactions_get_restrictions_for_repo(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/interaction-limits - Get interaction restrictions for a repository"""
        # HTTP GET /repos/{owner}/{repo}/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_invitations(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/invitations - List repository invitations"""
        # HTTP GET /repos/{owner}/{repo}/invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_invitation(
        owner: str,
        repo: str,
        invitation_id: int,
        permissions: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/invitations/{invitation_id} - Update a repository invitation"""
        # HTTP PATCH /repos/{owner}/{repo}/invitations/{invitation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["invitation_id"] = str(invitation_id)
        if permissions is not None:
            query_params["permissions"] = str(permissions)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/invitations/{invitation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_invitation(
        owner: str,
        repo: str,
        invitation_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/invitations/{invitation_id} - Delete a repository invitation"""
        # HTTP DELETE /repos/{owner}/{repo}/invitations/{invitation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["invitation_id"] = str(invitation_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/invitations/{invitation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_create(
        owner: str,
        repo: str,
        title: dict,
        body: str | None = None,
        assignee: str | None = None,
        milestone: dict | None = None,
        labels: list | None = None,
        assignees: list | None = None,
        type: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues - Create an issue"""
        # HTTP POST /repos/{owner}/{repo}/issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        body = title if isinstance(title, dict) else {"data": title}
        if body is not None:
            query_params["body"] = str(body)
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        body = milestone if isinstance(milestone, dict) else {"data": milestone}
        if labels is not None:
            query_params["labels"] = str(labels)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        if type is not None:
            query_params["type"] = str(type)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_for_repo(
        owner: str,
        repo: str,
        milestone: str | None = None,
        state: str | None = None,
        assignee: str | None = None,
        type: str | None = None,
        creator: str | None = None,
        mentioned: str | None = None,
        labels: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues - List repository issues"""
        # HTTP GET /repos/{owner}/{repo}/issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if milestone is not None:
            query_params["milestone"] = str(milestone)
        if state is not None:
            query_params["state"] = str(state)
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if type is not None:
            query_params["type"] = str(type)
        if creator is not None:
            query_params["creator"] = str(creator)
        if mentioned is not None:
            query_params["mentioned"] = str(mentioned)
        if labels is not None:
            query_params["labels"] = str(labels)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_comments_for_repo(
        owner: str,
        repo: str,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/comments - List issue comments for a repository"""
        # HTTP GET /repos/{owner}/{repo}/issues/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_update_comment(
        owner: str,
        repo: str,
        comment_id: int,
        body: str,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/issues/comments/{comment_id} - Update an issue comment"""
        # HTTP PATCH /repos/{owner}/{repo}/issues/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_delete_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/comments/{comment_id} - Delete an issue comment"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_get_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/comments/{comment_id} - Get an issue comment"""
        # HTTP GET /repos/{owner}/{repo}/issues/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_unpin_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/pin - Unpin an issue comment"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/pin
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}/pin",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_pin_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """PUT /repos/{owner}/{repo}/issues/comments/{comment_id}/pin - Pin an issue comment"""
        # HTTP PUT /repos/{owner}/{repo}/issues/comments/{comment_id}/pin
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}/pin",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_create_for_issue_comment(
        owner: str,
        repo: str,
        comment_id: int,
        content: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions - Create reaction for an issue comment"""
        # HTTP POST /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if content is not None:
            query_params["content"] = str(content)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_list_for_issue_comment(
        owner: str,
        repo: str,
        comment_id: int,
        content: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions - List reactions for an issue comment"""
        # HTTP GET /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if content is not None:
            query_params["content"] = str(content)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_delete_for_issue_comment(
        owner: str,
        repo: str,
        comment_id: int,
        reaction_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id} - Delete an issue comment reaction"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        path_params["reaction_id"] = str(reaction_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_events_for_repo(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/events - List issue events for a repository"""
        # HTTP GET /repos/{owner}/{repo}/issues/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_get_event(
        owner: str,
        repo: str,
        event_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/events/{event_id} - Get an issue event"""
        # HTTP GET /repos/{owner}/{repo}/issues/events/{event_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["event_id"] = str(event_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/events/{event_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_update(
        owner: str,
        repo: str,
        issue_number: int,
        title: dict | None = None,
        body: str | None = None,
        assignee: str | None = None,
        state: str | None = None,
        state_reason: str | None = None,
        milestone: dict | None = None,
        labels: list | None = None,
        assignees: list | None = None,
        issue_field_values: list | None = None,
        type: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/issues/{issue_number} - Update an issue"""
        # HTTP PATCH /repos/{owner}/{repo}/issues/{issue_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        body = title if isinstance(title, dict) else {"data": title}
        if body is not None:
            query_params["body"] = str(body)
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if state is not None:
            query_params["state"] = str(state)
        if state_reason is not None:
            query_params["state_reason"] = str(state_reason)
        body = milestone if isinstance(milestone, dict) else {"data": milestone}
        if labels is not None:
            query_params["labels"] = str(labels)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        if issue_field_values is not None:
            query_params["issue_field_values"] = str(issue_field_values)
        if type is not None:
            query_params["type"] = str(type)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/issues/{issue_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_get(
        owner: str,
        repo: str,
        issue_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number} - Get an issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_add_assignees(
        owner: str,
        repo: str,
        issue_number: int,
        assignees: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/{issue_number}/assignees - Add assignees to an issue"""
        # HTTP POST /repos/{owner}/{repo}/issues/{issue_number}/assignees
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/{issue_number}/assignees",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_remove_assignees(
        owner: str,
        repo: str,
        issue_number: int,
        assignees: list | None = None,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/assignees - Remove assignees from an issue"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/assignees
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if assignees is not None:
            query_params["assignees"] = str(assignees)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/assignees",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_check_user_can_be_assigned_to_issue(
        owner: str,
        repo: str,
        issue_number: int,
        assignee: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/assignees/{assignee} - Check if a user can be assigned to a issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/assignees/{assignee}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        path_params["assignee"] = str(assignee)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/assignees/{assignee}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_create_comment(
        owner: str,
        repo: str,
        issue_number: int,
        body: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/{issue_number}/comments - Create an issue comment"""
        # HTTP POST /repos/{owner}/{repo}/issues/{issue_number}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/{issue_number}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_comments(
        owner: str,
        repo: str,
        issue_number: int,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/comments - List issue comments"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_add_blocked_by_dependency(
        owner: str,
        repo: str,
        issue_number: int,
        issue_id: int,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by - Add a dependency an issue is blocked by"""
        # HTTP POST /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if issue_id is not None:
            query_params["issue_id"] = str(issue_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_dependencies_blocked_by(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by - List dependencies an issue is blocked by"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_remove_dependency_blocked_by(
        owner: str,
        repo: str,
        issue_number: int,
        issue_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by/{issue_id} - Remove dependency an issue is blocked by"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by/{issue_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        path_params["issue_id"] = str(issue_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by/{issue_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_dependencies_blocking(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocking - List dependencies an issue is blocking"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocking
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocking",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_events(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/events - List issue events"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_issue_field_values_for_issue(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values - List issue field values for an issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/issue-field-values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_add_labels(
        owner: str,
        repo: str,
        issue_number: int,
        body: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/{issue_number}/labels - Add labels to an issue"""
        # HTTP POST /repos/{owner}/{repo}/issues/{issue_number}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/{issue_number}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_remove_all_labels(
        owner: str,
        repo: str,
        issue_number: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels - Remove all labels from an issue"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_set_labels(
        owner: str,
        repo: str,
        issue_number: int,
        body: dict | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/issues/{issue_number}/labels - Set labels for an issue"""
        # HTTP PUT /repos/{owner}/{repo}/issues/{issue_number}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/issues/{issue_number}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_labels_on_issue(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/labels - List labels for an issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_remove_label(
        owner: str,
        repo: str,
        issue_number: int,
        name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels/{name} - Remove a label from an issue"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/labels/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_unlock(
        owner: str,
        repo: str,
        issue_number: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/lock - Unlock an issue"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/lock
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/lock",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_lock(
        owner: str,
        repo: str,
        issue_number: int,
        lock_reason: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/issues/{issue_number}/lock - Lock an issue"""
        # HTTP PUT /repos/{owner}/{repo}/issues/{issue_number}/lock
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if lock_reason is not None:
            query_params["lock_reason"] = str(lock_reason)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/issues/{issue_number}/lock",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_get_parent(
        owner: str,
        repo: str,
        issue_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/parent - Get parent issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/parent
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/parent",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_create_for_issue(
        owner: str,
        repo: str,
        issue_number: int,
        content: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/{issue_number}/reactions - Create reaction for an issue"""
        # HTTP POST /repos/{owner}/{repo}/issues/{issue_number}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if content is not None:
            query_params["content"] = str(content)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/{issue_number}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_list_for_issue(
        owner: str,
        repo: str,
        issue_number: int,
        content: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/reactions - List reactions for an issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if content is not None:
            query_params["content"] = str(content)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_delete_for_issue(
        owner: str,
        repo: str,
        issue_number: int,
        reaction_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id} - Delete an issue reaction"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        path_params["reaction_id"] = str(reaction_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_remove_sub_issue(
        owner: str,
        repo: str,
        issue_number: int,
        sub_issue_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/issues/{issue_number}/sub_issue - Remove sub-issue"""
        # HTTP DELETE /repos/{owner}/{repo}/issues/{issue_number}/sub_issue
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if sub_issue_id is not None:
            query_params["sub_issue_id"] = str(sub_issue_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/issues/{issue_number}/sub_issue",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_add_sub_issue(
        owner: str,
        repo: str,
        issue_number: int,
        sub_issue_id: int,
        replace_parent: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/issues/{issue_number}/sub_issues - Add sub-issue"""
        # HTTP POST /repos/{owner}/{repo}/issues/{issue_number}/sub_issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if sub_issue_id is not None:
            query_params["sub_issue_id"] = str(sub_issue_id)
        if replace_parent is not None:
            query_params["replace_parent"] = str(replace_parent)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/issues/{issue_number}/sub_issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_sub_issues(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/sub_issues - List sub-issues"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/sub_issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/sub_issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_reprioritize_sub_issue(
        owner: str,
        repo: str,
        issue_number: int,
        sub_issue_id: int,
        after_id: int | None = None,
        before_id: int | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/issues/{issue_number}/sub_issues/priority - Reprioritize sub-issue"""
        # HTTP PATCH /repos/{owner}/{repo}/issues/{issue_number}/sub_issues/priority
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if sub_issue_id is not None:
            query_params["sub_issue_id"] = str(sub_issue_id)
        if after_id is not None:
            query_params["after_id"] = str(after_id)
        if before_id is not None:
            query_params["before_id"] = str(before_id)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/issues/{issue_number}/sub_issues/priority",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_events_for_timeline(
        owner: str,
        repo: str,
        issue_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/issues/{issue_number}/timeline - List timeline events for an issue"""
        # HTTP GET /repos/{owner}/{repo}/issues/{issue_number}/timeline
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["issue_number"] = str(issue_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/issues/{issue_number}/timeline",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_deploy_key(
        owner: str,
        repo: str,
        key: str,
        title: str | None = None,
        read_only: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/keys - Create a deploy key"""
        # HTTP POST /repos/{owner}/{repo}/keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if title is not None:
            query_params["title"] = str(title)
        if key is not None:
            query_params["key"] = str(key)
        if read_only is not None:
            query_params["read_only"] = str(read_only)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_deploy_keys(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/keys - List deploy keys"""
        # HTTP GET /repos/{owner}/{repo}/keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_deploy_key(
        owner: str,
        repo: str,
        key_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/keys/{key_id} - Delete a deploy key"""
        # HTTP DELETE /repos/{owner}/{repo}/keys/{key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/keys/{key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_deploy_key(
        owner: str,
        repo: str,
        key_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/keys/{key_id} - Get a deploy key"""
        # HTTP GET /repos/{owner}/{repo}/keys/{key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/keys/{key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_create_label(
        owner: str,
        repo: str,
        name: str,
        color: str | None = None,
        description: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/labels - Create a label"""
        # HTTP POST /repos/{owner}/{repo}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        if color is not None:
            query_params["color"] = str(color)
        if description is not None:
            query_params["description"] = str(description)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_labels_for_repo(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/labels - List labels for a repository"""
        # HTTP GET /repos/{owner}/{repo}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_update_label(
        owner: str,
        repo: str,
        name: str,
        new_name: str | None = None,
        color: str | None = None,
        description: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/labels/{name} - Update a label"""
        # HTTP PATCH /repos/{owner}/{repo}/labels/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        if new_name is not None:
            query_params["new_name"] = str(new_name)
        if color is not None:
            query_params["color"] = str(color)
        if description is not None:
            query_params["description"] = str(description)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/labels/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_delete_label(
        owner: str,
        repo: str,
        name: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/labels/{name} - Delete a label"""
        # HTTP DELETE /repos/{owner}/{repo}/labels/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/labels/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_get_label(
        owner: str,
        repo: str,
        name: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/labels/{name} - Get a label"""
        # HTTP GET /repos/{owner}/{repo}/labels/{name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["name"] = str(name)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/labels/{name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_languages(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/languages - List repository languages"""
        # HTTP GET /repos/{owner}/{repo}/languages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/languages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def licenses_get_for_repo(
        owner: str,
        repo: str,
        ref: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/license - Get the license for a repository"""
        # HTTP GET /repos/{owner}/{repo}/license
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/license",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_merge_upstream(
        owner: str,
        repo: str,
        branch: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/merge-upstream - Sync a fork branch with the upstream repository"""
        # HTTP POST /repos/{owner}/{repo}/merge-upstream
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if branch is not None:
            query_params["branch"] = str(branch)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/merge-upstream",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_merge(
        owner: str,
        repo: str,
        base: str,
        head: str,
        commit_message: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/merges - Merge a branch"""
        # HTTP POST /repos/{owner}/{repo}/merges
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if base is not None:
            query_params["base"] = str(base)
        if head is not None:
            query_params["head"] = str(head)
        if commit_message is not None:
            query_params["commit_message"] = str(commit_message)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/merges",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_create_milestone(
        owner: str,
        repo: str,
        title: str,
        state: str | None = None,
        description: str | None = None,
        due_on: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/milestones - Create a milestone"""
        # HTTP POST /repos/{owner}/{repo}/milestones
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if title is not None:
            query_params["title"] = str(title)
        if state is not None:
            query_params["state"] = str(state)
        if description is not None:
            query_params["description"] = str(description)
        if due_on is not None:
            query_params["due_on"] = str(due_on)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/milestones",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_milestones(
        owner: str,
        repo: str,
        state: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/milestones - List milestones"""
        # HTTP GET /repos/{owner}/{repo}/milestones
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if state is not None:
            query_params["state"] = str(state)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/milestones",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_update_milestone(
        owner: str,
        repo: str,
        milestone_number: int,
        title: str | None = None,
        state: str | None = None,
        description: str | None = None,
        due_on: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/milestones/{milestone_number} - Update a milestone"""
        # HTTP PATCH /repos/{owner}/{repo}/milestones/{milestone_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["milestone_number"] = str(milestone_number)
        if title is not None:
            query_params["title"] = str(title)
        if state is not None:
            query_params["state"] = str(state)
        if description is not None:
            query_params["description"] = str(description)
        if due_on is not None:
            query_params["due_on"] = str(due_on)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/milestones/{milestone_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_delete_milestone(
        owner: str,
        repo: str,
        milestone_number: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/milestones/{milestone_number} - Delete a milestone"""
        # HTTP DELETE /repos/{owner}/{repo}/milestones/{milestone_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["milestone_number"] = str(milestone_number)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/milestones/{milestone_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_get_milestone(
        owner: str,
        repo: str,
        milestone_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/milestones/{milestone_number} - Get a milestone"""
        # HTTP GET /repos/{owner}/{repo}/milestones/{milestone_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["milestone_number"] = str(milestone_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/milestones/{milestone_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_labels_for_milestone(
        owner: str,
        repo: str,
        milestone_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/milestones/{milestone_number}/labels - List labels for issues in a milestone"""
        # HTTP GET /repos/{owner}/{repo}/milestones/{milestone_number}/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["milestone_number"] = str(milestone_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/milestones/{milestone_number}/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_mark_repo_notifications_as_read(
        owner: str,
        repo: str,
        last_read_at: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/notifications - Mark repository notifications as read"""
        # HTTP PUT /repos/{owner}/{repo}/notifications
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if last_read_at is not None:
            query_params["last_read_at"] = str(last_read_at)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/notifications",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_notifications(
        owner: str,
        repo: str,
        all: bool | None = None,
        participating: bool | None = None,
        since: str | None = None,
        before: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/notifications - List repository notifications for the authenticated user"""
        # HTTP GET /repos/{owner}/{repo}/notifications
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if all is not None:
            query_params["all"] = str(all)
        if participating is not None:
            query_params["participating"] = str(participating)
        if since is not None:
            query_params["since"] = str(since)
        if before is not None:
            query_params["before"] = str(before)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/notifications",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_pages_site(
        owner: str,
        repo: str,
        build_type: str | None = None,
        source: dict | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pages - Create a GitHub Pages site"""
        # HTTP POST /repos/{owner}/{repo}/pages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if build_type is not None:
            query_params["build_type"] = str(build_type)
        body = source if isinstance(source, dict) else {"data": source}
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_pages_site(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/pages - Delete a GitHub Pages site"""
        # HTTP DELETE /repos/{owner}/{repo}/pages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/pages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_information_about_pages_site(
        owner: str,
        repo: str,
        cname: str | None = None,
        https_enforced: bool | None = None,
        build_type: str | None = None,
        source: dict | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/pages - Update information about a GitHub Pages site"""
        # HTTP PUT /repos/{owner}/{repo}/pages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if cname is not None:
            query_params["cname"] = str(cname)
        if https_enforced is not None:
            query_params["https_enforced"] = str(https_enforced)
        if build_type is not None:
            query_params["build_type"] = str(build_type)
        body = source if isinstance(source, dict) else {"data": source}
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/pages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_pages(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/pages - Get a GitHub Pages site"""
        # HTTP GET /repos/{owner}/{repo}/pages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_request_pages_build(
        owner: str,
        repo: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/pages/builds - Request a GitHub Pages build"""
        # HTTP POST /repos/{owner}/{repo}/pages/builds
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pages/builds",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_pages_builds(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pages/builds - List GitHub Pages builds"""
        # HTTP GET /repos/{owner}/{repo}/pages/builds
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pages/builds",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_latest_pages_build(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/pages/builds/latest - Get latest Pages build"""
        # HTTP GET /repos/{owner}/{repo}/pages/builds/latest
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pages/builds/latest",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_pages_build(
        owner: str,
        repo: str,
        build_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/pages/builds/{build_id} - Get GitHub Pages build"""
        # HTTP GET /repos/{owner}/{repo}/pages/builds/{build_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["build_id"] = str(build_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pages/builds/{build_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_pages_deployment(
        owner: str,
        repo: str,
        pages_build_version: str,
        oidc_token: str,
        artifact_id: float | None = None,
        artifact_url: str | None = None,
        environment: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pages/deployments - Create a GitHub Pages deployment"""
        # HTTP POST /repos/{owner}/{repo}/pages/deployments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if artifact_id is not None:
            query_params["artifact_id"] = str(artifact_id)
        if artifact_url is not None:
            query_params["artifact_url"] = str(artifact_url)
        if environment is not None:
            query_params["environment"] = str(environment)
        if pages_build_version is not None:
            query_params["pages_build_version"] = str(pages_build_version)
        if oidc_token is not None:
            query_params["oidc_token"] = str(oidc_token)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pages/deployments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_pages_deployment(
        owner: str,
        repo: str,
        pages_deployment_id: dict,
    ) -> str:
        """GET /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id} - Get the status of a GitHub Pages deployment"""
        # HTTP GET /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pages_deployment_id"] = str(pages_deployment_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_cancel_pages_deployment(
        owner: str,
        repo: str,
        pages_deployment_id: dict,
    ) -> str:
        """POST /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}/cancel - Cancel a GitHub Pages deployment"""
        # HTTP POST /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}/cancel
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pages_deployment_id"] = str(pages_deployment_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}/cancel",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_pages_health_check(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/pages/health - Get a DNS health check for GitHub Pages"""
        # HTTP GET /repos/{owner}/{repo}/pages/health
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pages/health",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_disable_private_vulnerability_reporting(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/private-vulnerability-reporting - Disable private vulnerability reporting for a repository"""
        # HTTP DELETE /repos/{owner}/{repo}/private-vulnerability-reporting
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/private-vulnerability-reporting",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_enable_private_vulnerability_reporting(
        owner: str,
        repo: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/private-vulnerability-reporting - Enable private vulnerability reporting for a repository"""
        # HTTP PUT /repos/{owner}/{repo}/private-vulnerability-reporting
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/private-vulnerability-reporting",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_check_private_vulnerability_reporting(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/private-vulnerability-reporting - Check if private vulnerability reporting is enabled for a repository"""
        # HTTP GET /repos/{owner}/{repo}/private-vulnerability-reporting
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/private-vulnerability-reporting",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def patch_repos_by_owner_by_repo_properties_values(
        owner: str,
        repo: str,
        properties: list,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/properties/values - Create or update custom property values for a repository"""
        # HTTP PATCH /repos/{owner}/{repo}/properties/values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if properties is not None:
            query_params["properties"] = str(properties)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/properties/values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_repos_by_owner_by_repo_properties_values(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/properties/values - Get all custom property values for a repository"""
        # HTTP GET /repos/{owner}/{repo}/properties/values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/properties/values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_create(
        owner: str,
        repo: str,
        head: str,
        base: str,
        title: str | None = None,
        head_repo: str | None = None,
        body: str | None = None,
        maintainer_can_modify: bool | None = None,
        draft: bool | None = None,
        issue: int | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls - Create a pull request"""
        # HTTP POST /repos/{owner}/{repo}/pulls
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if title is not None:
            query_params["title"] = str(title)
        if head is not None:
            query_params["head"] = str(head)
        if head_repo is not None:
            query_params["head_repo"] = str(head_repo)
        if base is not None:
            query_params["base"] = str(base)
        if body is not None:
            query_params["body"] = str(body)
        if maintainer_can_modify is not None:
            query_params["maintainer_can_modify"] = str(maintainer_can_modify)
        if draft is not None:
            query_params["draft"] = str(draft)
        if issue is not None:
            query_params["issue"] = str(issue)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list(
        owner: str,
        repo: str,
        state: str | None = None,
        head: str | None = None,
        base: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls - List pull requests"""
        # HTTP GET /repos/{owner}/{repo}/pulls
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if state is not None:
            query_params["state"] = str(state)
        if head is not None:
            query_params["head"] = str(head)
        if base is not None:
            query_params["base"] = str(base)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_review_comments_for_repo(
        owner: str,
        repo: str,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/comments - List review comments in a repository"""
        # HTTP GET /repos/{owner}/{repo}/pulls/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_update_review_comment(
        owner: str,
        repo: str,
        comment_id: int,
        body: str,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/pulls/comments/{comment_id} - Update a review comment for a pull request"""
        # HTTP PATCH /repos/{owner}/{repo}/pulls/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/pulls/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_delete_review_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id} - Delete a review comment for a pull request"""
        # HTTP DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/pulls/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_get_review_comment(
        owner: str,
        repo: str,
        comment_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/comments/{comment_id} - Get a review comment for a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/comments/{comment_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/comments/{comment_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_create_for_pull_request_review_comment(
        owner: str,
        repo: str,
        comment_id: int,
        content: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions - Create reaction for a pull request review comment"""
        # HTTP POST /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if content is not None:
            query_params["content"] = str(content)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_list_for_pull_request_review_comment(
        owner: str,
        repo: str,
        comment_id: int,
        content: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions - List reactions for a pull request review comment"""
        # HTTP GET /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        if content is not None:
            query_params["content"] = str(content)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_delete_for_pull_request_comment(
        owner: str,
        repo: str,
        comment_id: int,
        reaction_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id} - Delete a pull request comment reaction"""
        # HTTP DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["comment_id"] = str(comment_id)
        path_params["reaction_id"] = str(reaction_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_update(
        owner: str,
        repo: str,
        pull_number: int,
        title: str | None = None,
        body: str | None = None,
        state: str | None = None,
        base: str | None = None,
        maintainer_can_modify: bool | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/pulls/{pull_number} - Update a pull request"""
        # HTTP PATCH /repos/{owner}/{repo}/pulls/{pull_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if title is not None:
            query_params["title"] = str(title)
        if body is not None:
            query_params["body"] = str(body)
        if state is not None:
            query_params["state"] = str(state)
        if base is not None:
            query_params["base"] = str(base)
        if maintainer_can_modify is not None:
            query_params["maintainer_can_modify"] = str(maintainer_can_modify)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/pulls/{pull_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_get(
        owner: str,
        repo: str,
        pull_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number} - Get a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_create_with_pr_for_authenticated_user(
        owner: str,
        repo: str,
        pull_number: int,
        location: str | None = None,
        geo: str | None = None,
        client_ip: str | None = None,
        machine: str | None = None,
        devcontainer_path: str | None = None,
        multi_repo_permissions_opt_out: bool | None = None,
        working_directory: str | None = None,
        idle_timeout_minutes: int | None = None,
        display_name: str | None = None,
        retention_period_minutes: int | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/{pull_number}/codespaces - Create a codespace from a pull request"""
        # HTTP POST /repos/{owner}/{repo}/pulls/{pull_number}/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if location is not None:
            query_params["location"] = str(location)
        if geo is not None:
            query_params["geo"] = str(geo)
        if client_ip is not None:
            query_params["client_ip"] = str(client_ip)
        if machine is not None:
            query_params["machine"] = str(machine)
        if devcontainer_path is not None:
            query_params["devcontainer_path"] = str(devcontainer_path)
        if multi_repo_permissions_opt_out is not None:
            query_params["multi_repo_permissions_opt_out"] = str(multi_repo_permissions_opt_out)
        if working_directory is not None:
            query_params["working_directory"] = str(working_directory)
        if idle_timeout_minutes is not None:
            query_params["idle_timeout_minutes"] = str(idle_timeout_minutes)
        if display_name is not None:
            query_params["display_name"] = str(display_name)
        if retention_period_minutes is not None:
            query_params["retention_period_minutes"] = str(retention_period_minutes)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/{pull_number}/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_create_review_comment(
        owner: str,
        repo: str,
        pull_number: int,
        body: str,
        commit_id: str,
        path: str,
        position: int | None = None,
        side: str | None = None,
        line: int | None = None,
        start_line: int | None = None,
        start_side: str | None = None,
        in_reply_to: int | None = None,
        subject_type: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/{pull_number}/comments - Create a review comment for a pull request"""
        # HTTP POST /repos/{owner}/{repo}/pulls/{pull_number}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if body is not None:
            query_params["body"] = str(body)
        if commit_id is not None:
            query_params["commit_id"] = str(commit_id)
        if path is not None:
            query_params["path"] = str(path)
        if position is not None:
            query_params["position"] = str(position)
        if side is not None:
            query_params["side"] = str(side)
        if line is not None:
            query_params["line"] = str(line)
        if start_line is not None:
            query_params["start_line"] = str(start_line)
        if start_side is not None:
            query_params["start_side"] = str(start_side)
        if in_reply_to is not None:
            query_params["in_reply_to"] = str(in_reply_to)
        if subject_type is not None:
            query_params["subject_type"] = str(subject_type)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/{pull_number}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_review_comments(
        owner: str,
        repo: str,
        pull_number: int,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/comments - List review comments on a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_create_reply_for_review_comment(
        owner: str,
        repo: str,
        pull_number: int,
        comment_id: int,
        body: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies - Create a reply for a review comment"""
        # HTTP POST /repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["comment_id"] = str(comment_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_commits(
        owner: str,
        repo: str,
        pull_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/commits - List commits on a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/commits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/commits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_files(
        owner: str,
        repo: str,
        pull_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/files - List pull requests files"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/files
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/files",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_merge(
        owner: str,
        repo: str,
        pull_number: int,
        commit_title: str | None = None,
        commit_message: str | None = None,
        sha: str | None = None,
        merge_method: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge - Merge a pull request"""
        # HTTP PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if commit_title is not None:
            query_params["commit_title"] = str(commit_title)
        if commit_message is not None:
            query_params["commit_message"] = str(commit_message)
        if sha is not None:
            query_params["sha"] = str(sha)
        if merge_method is not None:
            query_params["merge_method"] = str(merge_method)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/pulls/{pull_number}/merge",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_check_if_merged(
        owner: str,
        repo: str,
        pull_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/merge - Check if a pull request has been merged"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/merge
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/merge",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_request_reviewers(
        owner: str,
        repo: str,
        pull_number: int,
        reviewers: list | None = None,
        team_reviewers: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers - Request reviewers for a pull request"""
        # HTTP POST /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if reviewers is not None:
            query_params["reviewers"] = str(reviewers)
        if team_reviewers is not None:
            query_params["team_reviewers"] = str(team_reviewers)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_remove_requested_reviewers(
        owner: str,
        repo: str,
        pull_number: int,
        reviewers: list,
        team_reviewers: list | None = None,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers - Remove requested reviewers from a pull request"""
        # HTTP DELETE /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if reviewers is not None:
            query_params["reviewers"] = str(reviewers)
        if team_reviewers is not None:
            query_params["team_reviewers"] = str(team_reviewers)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_requested_reviewers(
        owner: str,
        repo: str,
        pull_number: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers - Get all requested reviewers for a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_create_review(
        owner: str,
        repo: str,
        pull_number: int,
        commit_id: str | None = None,
        body: str | None = None,
        event: str | None = None,
        comments: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews - Create a review for a pull request"""
        # HTTP POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if commit_id is not None:
            query_params["commit_id"] = str(commit_id)
        if body is not None:
            query_params["body"] = str(body)
        if event is not None:
            query_params["event"] = str(event)
        if comments is not None:
            query_params["comments"] = str(comments)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_reviews(
        owner: str,
        repo: str,
        pull_number: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews - List reviews for a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_delete_pending_review(
        owner: str,
        repo: str,
        pull_number: int,
        review_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id} - Delete a pending review for a pull request"""
        # HTTP DELETE /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["review_id"] = str(review_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_update_review(
        owner: str,
        repo: str,
        pull_number: int,
        review_id: int,
        body: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id} - Update a review for a pull request"""
        # HTTP PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["review_id"] = str(review_id)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_get_review(
        owner: str,
        repo: str,
        pull_number: int,
        review_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id} - Get a review for a pull request"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["review_id"] = str(review_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_list_comments_for_review(
        owner: str,
        repo: str,
        pull_number: int,
        review_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments - List comments for a pull request review"""
        # HTTP GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["review_id"] = str(review_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_dismiss_review(
        owner: str,
        repo: str,
        pull_number: int,
        review_id: int,
        message: str,
        event: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals - Dismiss a review for a pull request"""
        # HTTP PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["review_id"] = str(review_id)
        if message is not None:
            query_params["message"] = str(message)
        if event is not None:
            query_params["event"] = str(event)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_submit_review(
        owner: str,
        repo: str,
        pull_number: int,
        review_id: int,
        event: str,
        body: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events - Submit a review for a pull request"""
        # HTTP POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        path_params["review_id"] = str(review_id)
        if body is not None:
            query_params["body"] = str(body)
        if event is not None:
            query_params["event"] = str(event)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def pulls_update_branch(
        owner: str,
        repo: str,
        pull_number: int,
        expected_head_sha: str | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/pulls/{pull_number}/update-branch - Update a pull request branch"""
        # HTTP PUT /repos/{owner}/{repo}/pulls/{pull_number}/update-branch
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["pull_number"] = str(pull_number)
        if expected_head_sha is not None:
            query_params["expected_head_sha"] = str(expected_head_sha)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/pulls/{pull_number}/update-branch",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_readme(
        owner: str,
        repo: str,
        ref: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/readme - Get a repository README"""
        # HTTP GET /repos/{owner}/{repo}/readme
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/readme",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_readme_in_directory(
        owner: str,
        repo: str,
        dir: str,
        ref: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/readme/{dir} - Get a repository README for a directory"""
        # HTTP GET /repos/{owner}/{repo}/readme/{dir}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["dir"] = str(dir)
        if ref is not None:
            query_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/readme/{dir}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_release(
        owner: str,
        repo: str,
        tag_name: str,
        target_commitish: str | None = None,
        name: str | None = None,
        body: str | None = None,
        draft: bool | None = None,
        prerelease: bool | None = None,
        discussion_category_name: str | None = None,
        generate_release_notes: bool | None = None,
        make_latest: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/releases - Create a release"""
        # HTTP POST /repos/{owner}/{repo}/releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if tag_name is not None:
            query_params["tag_name"] = str(tag_name)
        if target_commitish is not None:
            query_params["target_commitish"] = str(target_commitish)
        if name is not None:
            query_params["name"] = str(name)
        if body is not None:
            query_params["body"] = str(body)
        if draft is not None:
            query_params["draft"] = str(draft)
        if prerelease is not None:
            query_params["prerelease"] = str(prerelease)
        if discussion_category_name is not None:
            query_params["discussion_category_name"] = str(discussion_category_name)
        if generate_release_notes is not None:
            query_params["generate_release_notes"] = str(generate_release_notes)
        if make_latest is not None:
            query_params["make_latest"] = str(make_latest)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_releases(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases - List releases"""
        # HTTP GET /repos/{owner}/{repo}/releases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_release_asset(
        owner: str,
        repo: str,
        asset_id: int,
        name: str | None = None,
        label: str | None = None,
        state: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/releases/assets/{asset_id} - Update a release asset"""
        # HTTP PATCH /repos/{owner}/{repo}/releases/assets/{asset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["asset_id"] = str(asset_id)
        if name is not None:
            query_params["name"] = str(name)
        if label is not None:
            query_params["label"] = str(label)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/releases/assets/{asset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_release_asset(
        owner: str,
        repo: str,
        asset_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/releases/assets/{asset_id} - Delete a release asset"""
        # HTTP DELETE /repos/{owner}/{repo}/releases/assets/{asset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["asset_id"] = str(asset_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/releases/assets/{asset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_release_asset(
        owner: str,
        repo: str,
        asset_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases/assets/{asset_id} - Get a release asset"""
        # HTTP GET /repos/{owner}/{repo}/releases/assets/{asset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["asset_id"] = str(asset_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases/assets/{asset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_generate_release_notes(
        owner: str,
        repo: str,
        tag_name: str,
        target_commitish: str | None = None,
        previous_tag_name: str | None = None,
        configuration_file_path: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/releases/generate-notes - Generate release notes content for a release"""
        # HTTP POST /repos/{owner}/{repo}/releases/generate-notes
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if tag_name is not None:
            query_params["tag_name"] = str(tag_name)
        if target_commitish is not None:
            query_params["target_commitish"] = str(target_commitish)
        if previous_tag_name is not None:
            query_params["previous_tag_name"] = str(previous_tag_name)
        if configuration_file_path is not None:
            query_params["configuration_file_path"] = str(configuration_file_path)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/releases/generate-notes",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_latest_release(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases/latest - Get the latest release"""
        # HTTP GET /repos/{owner}/{repo}/releases/latest
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases/latest",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_release_by_tag(
        owner: str,
        repo: str,
        tag: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases/tags/{tag} - Get a release by tag name"""
        # HTTP GET /repos/{owner}/{repo}/releases/tags/{tag}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["tag"] = str(tag)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases/tags/{tag}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_release(
        owner: str,
        repo: str,
        release_id: int,
        tag_name: str | None = None,
        target_commitish: str | None = None,
        name: str | None = None,
        body: str | None = None,
        draft: bool | None = None,
        prerelease: bool | None = None,
        make_latest: str | None = None,
        discussion_category_name: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/releases/{release_id} - Update a release"""
        # HTTP PATCH /repos/{owner}/{repo}/releases/{release_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        if tag_name is not None:
            query_params["tag_name"] = str(tag_name)
        if target_commitish is not None:
            query_params["target_commitish"] = str(target_commitish)
        if name is not None:
            query_params["name"] = str(name)
        if body is not None:
            query_params["body"] = str(body)
        if draft is not None:
            query_params["draft"] = str(draft)
        if prerelease is not None:
            query_params["prerelease"] = str(prerelease)
        if make_latest is not None:
            query_params["make_latest"] = str(make_latest)
        if discussion_category_name is not None:
            query_params["discussion_category_name"] = str(discussion_category_name)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/releases/{release_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_release(
        owner: str,
        repo: str,
        release_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/releases/{release_id} - Delete a release"""
        # HTTP DELETE /repos/{owner}/{repo}/releases/{release_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/releases/{release_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_release(
        owner: str,
        repo: str,
        release_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases/{release_id} - Get a release"""
        # HTTP GET /repos/{owner}/{repo}/releases/{release_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases/{release_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_upload_release_asset(
        owner: str,
        repo: str,
        release_id: int,
        name: str,
        label: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/releases/{release_id}/assets - Upload a release asset"""
        # HTTP POST /repos/{owner}/{repo}/releases/{release_id}/assets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        if name is not None:
            query_params["name"] = str(name)
        if label is not None:
            query_params["label"] = str(label)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/releases/{release_id}/assets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_release_assets(
        owner: str,
        repo: str,
        release_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases/{release_id}/assets - List release assets"""
        # HTTP GET /repos/{owner}/{repo}/releases/{release_id}/assets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases/{release_id}/assets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_create_for_release(
        owner: str,
        repo: str,
        release_id: int,
        content: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/releases/{release_id}/reactions - Create reaction for a release"""
        # HTTP POST /repos/{owner}/{repo}/releases/{release_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        if content is not None:
            query_params["content"] = str(content)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/releases/{release_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_list_for_release(
        owner: str,
        repo: str,
        release_id: int,
        content: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/releases/{release_id}/reactions - List reactions for a release"""
        # HTTP GET /repos/{owner}/{repo}/releases/{release_id}/reactions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        if content is not None:
            query_params["content"] = str(content)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/releases/{release_id}/reactions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def reactions_delete_for_release(
        owner: str,
        repo: str,
        release_id: int,
        reaction_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/releases/{release_id}/reactions/{reaction_id} - Delete a release reaction"""
        # HTTP DELETE /repos/{owner}/{repo}/releases/{release_id}/reactions/{reaction_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["release_id"] = str(release_id)
        path_params["reaction_id"] = str(reaction_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/releases/{release_id}/reactions/{reaction_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_branch_rules(
        owner: str,
        repo: str,
        branch: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/rules/branches/{branch} - Get rules for a branch"""
        # HTTP GET /repos/{owner}/{repo}/rules/branches/{branch}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["branch"] = str(branch)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rules/branches/{branch}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_repo_ruleset(
        owner: str,
        repo: str,
        name: str,
        enforcement: str,
        target: str | None = None,
        bypass_actors: list | None = None,
        conditions: dict | None = None,
        rules: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/rulesets - Create a repository ruleset"""
        # HTTP POST /repos/{owner}/{repo}/rulesets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if name is not None:
            query_params["name"] = str(name)
        if target is not None:
            query_params["target"] = str(target)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        if bypass_actors is not None:
            query_params["bypass_actors"] = str(bypass_actors)
        body = conditions if isinstance(conditions, dict) else {"data": conditions}
        if rules is not None:
            query_params["rules"] = str(rules)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/rulesets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_repo_rulesets(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
        includes_parents: bool | None = None,
        targets: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/rulesets - Get all repository rulesets"""
        # HTTP GET /repos/{owner}/{repo}/rulesets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if includes_parents is not None:
            query_params["includes_parents"] = str(includes_parents)
        if targets is not None:
            query_params["targets"] = str(targets)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rulesets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_repo_rule_suites(
        owner: str,
        repo: str,
        ref: str | None = None,
        time_period: str | None = None,
        actor_name: str | None = None,
        rule_suite_result: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/rulesets/rule-suites - List repository rule suites"""
        # HTTP GET /repos/{owner}/{repo}/rulesets/rule-suites
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if ref is not None:
            query_params["ref"] = str(ref)
        if time_period is not None:
            query_params["time_period"] = str(time_period)
        if actor_name is not None:
            query_params["actor_name"] = str(actor_name)
        if rule_suite_result is not None:
            query_params["rule_suite_result"] = str(rule_suite_result)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rulesets/rule-suites",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_repo_rule_suite(
        owner: str,
        repo: str,
        rule_suite_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/rulesets/rule-suites/{rule_suite_id} - Get a repository rule suite"""
        # HTTP GET /repos/{owner}/{repo}/rulesets/rule-suites/{rule_suite_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["rule_suite_id"] = str(rule_suite_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rulesets/rule-suites/{rule_suite_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_delete_repo_ruleset(
        owner: str,
        repo: str,
        ruleset_id: int,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/rulesets/{ruleset_id} - Delete a repository ruleset"""
        # HTTP DELETE /repos/{owner}/{repo}/rulesets/{ruleset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ruleset_id"] = str(ruleset_id)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/rulesets/{ruleset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_update_repo_ruleset(
        owner: str,
        repo: str,
        ruleset_id: int,
        name: str | None = None,
        target: str | None = None,
        enforcement: str | None = None,
        bypass_actors: list | None = None,
        conditions: dict | None = None,
        rules: list | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/rulesets/{ruleset_id} - Update a repository ruleset"""
        # HTTP PUT /repos/{owner}/{repo}/rulesets/{ruleset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ruleset_id"] = str(ruleset_id)
        if name is not None:
            query_params["name"] = str(name)
        if target is not None:
            query_params["target"] = str(target)
        if enforcement is not None:
            query_params["enforcement"] = str(enforcement)
        if bypass_actors is not None:
            query_params["bypass_actors"] = str(bypass_actors)
        body = conditions if isinstance(conditions, dict) else {"data": conditions}
        if rules is not None:
            query_params["rules"] = str(rules)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/rulesets/{ruleset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_repo_ruleset(
        owner: str,
        repo: str,
        ruleset_id: int,
        includes_parents: bool | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/rulesets/{ruleset_id} - Get a repository ruleset"""
        # HTTP GET /repos/{owner}/{repo}/rulesets/{ruleset_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ruleset_id"] = str(ruleset_id)
        if includes_parents is not None:
            query_params["includes_parents"] = str(includes_parents)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rulesets/{ruleset_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_repo_ruleset_history(
        owner: str,
        repo: str,
        ruleset_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history - Get repository ruleset history"""
        # HTTP GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        path_params["ruleset_id"] = str(ruleset_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rulesets/{ruleset_id}/history",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_repo_ruleset_version(
        owner: str,
        repo: str,
        ruleset_id: int,
        version_id: int,
    ) -> str:
        """GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history/{version_id} - Get repository ruleset version"""
        # HTTP GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history/{version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ruleset_id"] = str(ruleset_id)
        path_params["version_id"] = str(version_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/rulesets/{ruleset_id}/history/{version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_list_alerts_for_repo(
        owner: str,
        repo: str,
        state: str | None = None,
        secret_type: str | None = None,
        resolution: str | None = None,
        assignee: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        validity: str | None = None,
        is_publicly_leaked: bool | None = None,
        is_multi_repo: bool | None = None,
        hide_secret: bool | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/secret-scanning/alerts - List secret scanning alerts for a repository"""
        # HTTP GET /repos/{owner}/{repo}/secret-scanning/alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if state is not None:
            query_params["state"] = str(state)
        if secret_type is not None:
            query_params["secret_type"] = str(secret_type)
        if resolution is not None:
            query_params["resolution"] = str(resolution)
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if validity is not None:
            query_params["validity"] = str(validity)
        if is_publicly_leaked is not None:
            query_params["is_publicly_leaked"] = str(is_publicly_leaked)
        if is_multi_repo is not None:
            query_params["is_multi_repo"] = str(is_multi_repo)
        if hide_secret is not None:
            query_params["hide_secret"] = str(hide_secret)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/secret-scanning/alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_update_alert(
        owner: str,
        repo: str,
        alert_number: int,
        state: str | None = None,
        resolution: str | None = None,
        resolution_comment: str | None = None,
        assignee: str | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number} - Update a secret scanning alert"""
        # HTTP PATCH /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if state is not None:
            query_params["state"] = str(state)
        if resolution is not None:
            query_params["resolution"] = str(resolution)
        if resolution_comment is not None:
            query_params["resolution_comment"] = str(resolution_comment)
        if assignee is not None:
            query_params["assignee"] = str(assignee)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_get_alert(
        owner: str,
        repo: str,
        alert_number: int,
        hide_secret: bool | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number} - Get a secret scanning alert"""
        # HTTP GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if hide_secret is not None:
            query_params["hide_secret"] = str(hide_secret)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_list_locations_for_alert(
        owner: str,
        repo: str,
        alert_number: int,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations - List locations for a secret scanning alert"""
        # HTTP GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["alert_number"] = str(alert_number)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_create_push_protection_bypass(
        owner: str,
        repo: str,
        reason: str,
        placeholder_id: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/secret-scanning/push-protection-bypasses - Create a push protection bypass"""
        # HTTP POST /repos/{owner}/{repo}/secret-scanning/push-protection-bypasses
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if reason is not None:
            query_params["reason"] = str(reason)
        if placeholder_id is not None:
            query_params["placeholder_id"] = str(placeholder_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/secret-scanning/push-protection-bypasses",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def secret_scanning_get_scan_history(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/secret-scanning/scan-history - Get secret scanning scan history for a repository"""
        # HTTP GET /repos/{owner}/{repo}/secret-scanning/scan-history
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/secret-scanning/scan-history",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_create_repository_advisory(
        owner: str,
        repo: str,
        summary: str,
        description: str,
        vulnerabilities: list,
        cve_id: str | None = None,
        cwe_ids: list | None = None,
        credits: list | None = None,
        severity: str | None = None,
        cvss_vector_string: str | None = None,
        start_private_fork: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/security-advisories - Create a repository security advisory"""
        # HTTP POST /repos/{owner}/{repo}/security-advisories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if summary is not None:
            query_params["summary"] = str(summary)
        if description is not None:
            query_params["description"] = str(description)
        if cve_id is not None:
            query_params["cve_id"] = str(cve_id)
        if vulnerabilities is not None:
            query_params["vulnerabilities"] = str(vulnerabilities)
        if cwe_ids is not None:
            query_params["cwe_ids"] = str(cwe_ids)
        if credits is not None:
            query_params["credits"] = str(credits)
        if severity is not None:
            query_params["severity"] = str(severity)
        if cvss_vector_string is not None:
            query_params["cvss_vector_string"] = str(cvss_vector_string)
        if start_private_fork is not None:
            query_params["start_private_fork"] = str(start_private_fork)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/security-advisories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_list_repository_advisories(
        owner: str,
        repo: str,
        direction: str | None = None,
        sort: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
        state: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/security-advisories - List repository security advisories"""
        # HTTP GET /repos/{owner}/{repo}/security-advisories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if direction is not None:
            query_params["direction"] = str(direction)
        if sort is not None:
            query_params["sort"] = str(sort)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/security-advisories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_repos_by_owner_by_repo_security_advisories_reports(
        owner: str,
        repo: str,
        summary: str,
        description: str,
        vulnerabilities: list | None = None,
        cwe_ids: list | None = None,
        severity: str | None = None,
        cvss_vector_string: str | None = None,
        start_private_fork: bool | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/security-advisories/reports - Privately report a security vulnerability"""
        # HTTP POST /repos/{owner}/{repo}/security-advisories/reports
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if summary is not None:
            query_params["summary"] = str(summary)
        if description is not None:
            query_params["description"] = str(description)
        if vulnerabilities is not None:
            query_params["vulnerabilities"] = str(vulnerabilities)
        if cwe_ids is not None:
            query_params["cwe_ids"] = str(cwe_ids)
        if severity is not None:
            query_params["severity"] = str(severity)
        if cvss_vector_string is not None:
            query_params["cvss_vector_string"] = str(cvss_vector_string)
        if start_private_fork is not None:
            query_params["start_private_fork"] = str(start_private_fork)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/security-advisories/reports",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_update_repository_advisory(
        owner: str,
        repo: str,
        ghsa_id: str,
        summary: str | None = None,
        description: str | None = None,
        cve_id: str | None = None,
        vulnerabilities: list | None = None,
        cwe_ids: list | None = None,
        credits: list | None = None,
        severity: str | None = None,
        cvss_vector_string: str | None = None,
        state: str | None = None,
        collaborating_users: list | None = None,
        collaborating_teams: list | None = None,
    ) -> str:
        """PATCH /repos/{owner}/{repo}/security-advisories/{ghsa_id} - Update a repository security advisory"""
        # HTTP PATCH /repos/{owner}/{repo}/security-advisories/{ghsa_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ghsa_id"] = str(ghsa_id)
        if summary is not None:
            query_params["summary"] = str(summary)
        if description is not None:
            query_params["description"] = str(description)
        if cve_id is not None:
            query_params["cve_id"] = str(cve_id)
        if vulnerabilities is not None:
            query_params["vulnerabilities"] = str(vulnerabilities)
        if cwe_ids is not None:
            query_params["cwe_ids"] = str(cwe_ids)
        if credits is not None:
            query_params["credits"] = str(credits)
        if severity is not None:
            query_params["severity"] = str(severity)
        if cvss_vector_string is not None:
            query_params["cvss_vector_string"] = str(cvss_vector_string)
        if state is not None:
            query_params["state"] = str(state)
        if collaborating_users is not None:
            query_params["collaborating_users"] = str(collaborating_users)
        if collaborating_teams is not None:
            query_params["collaborating_teams"] = str(collaborating_teams)
        try:
            return await backend.request(
                "PATCH",
                "/repos/{owner}/{repo}/security-advisories/{ghsa_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_get_repository_advisory(
        owner: str,
        repo: str,
        ghsa_id: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/security-advisories/{ghsa_id} - Get a repository security advisory"""
        # HTTP GET /repos/{owner}/{repo}/security-advisories/{ghsa_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ghsa_id"] = str(ghsa_id)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/security-advisories/{ghsa_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_repos_by_owner_by_repo_security_advisories_by_ghsa_id_cve(
        owner: str,
        repo: str,
        ghsa_id: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/cve - Request a CVE for a repository security advisory"""
        # HTTP POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/cve
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ghsa_id"] = str(ghsa_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/security-advisories/{ghsa_id}/cve",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def security_advisories_create_fork(
        owner: str,
        repo: str,
        ghsa_id: str,
    ) -> str:
        """POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/forks - Create a temporary private fork"""
        # HTTP POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/forks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ghsa_id"] = str(ghsa_id)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/security-advisories/{ghsa_id}/forks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_stargazers_for_repo(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/stargazers - List stargazers"""
        # HTTP GET /repos/{owner}/{repo}/stargazers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/stargazers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_code_frequency_stats(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/stats/code_frequency - Get the weekly commit activity"""
        # HTTP GET /repos/{owner}/{repo}/stats/code_frequency
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/stats/code_frequency",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_commit_activity_stats(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/stats/commit_activity - Get the last year of commit activity"""
        # HTTP GET /repos/{owner}/{repo}/stats/commit_activity
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/stats/commit_activity",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_contributors_stats(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/stats/contributors - Get all contributor commit activity"""
        # HTTP GET /repos/{owner}/{repo}/stats/contributors
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/stats/contributors",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_participation_stats(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/stats/participation - Get the weekly commit count"""
        # HTTP GET /repos/{owner}/{repo}/stats/participation
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/stats/participation",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_punch_card_stats(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/stats/punch_card - Get the hourly commit count for each day"""
        # HTTP GET /repos/{owner}/{repo}/stats/punch_card
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/stats/punch_card",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_commit_status(
        owner: str,
        repo: str,
        sha: str,
        state: str,
        target_url: str | None = None,
        description: str | None = None,
        context: str | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/statuses/{sha} - Create a commit status"""
        # HTTP POST /repos/{owner}/{repo}/statuses/{sha}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["sha"] = str(sha)
        if state is not None:
            query_params["state"] = str(state)
        if target_url is not None:
            query_params["target_url"] = str(target_url)
        if description is not None:
            query_params["description"] = str(description)
        if context is not None:
            query_params["context"] = str(context)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/statuses/{sha}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_watchers_for_repo(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/subscribers - List watchers"""
        # HTTP GET /repos/{owner}/{repo}/subscribers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/subscribers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_delete_repo_subscription(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/subscription - Delete a repository subscription"""
        # HTTP DELETE /repos/{owner}/{repo}/subscription
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/subscription",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_set_repo_subscription(
        owner: str,
        repo: str,
        subscribed: bool | None = None,
        ignored: bool | None = None,
    ) -> str:
        """PUT /repos/{owner}/{repo}/subscription - Set a repository subscription"""
        # HTTP PUT /repos/{owner}/{repo}/subscription
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if subscribed is not None:
            query_params["subscribed"] = str(subscribed)
        if ignored is not None:
            query_params["ignored"] = str(ignored)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/subscription",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_get_repo_subscription(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/subscription - Get a repository subscription"""
        # HTTP GET /repos/{owner}/{repo}/subscription
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/subscription",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_tags(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/tags - List repository tags"""
        # HTTP GET /repos/{owner}/{repo}/tags
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/tags",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_download_tarball_archive(
        owner: str,
        repo: str,
        ref: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/tarball/{ref} - Download a repository archive (tar)"""
        # HTTP GET /repos/{owner}/{repo}/tarball/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/tarball/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_teams(
        owner: str,
        repo: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/teams - List repository teams"""
        # HTTP GET /repos/{owner}/{repo}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_replace_all_topics(
        owner: str,
        repo: str,
        names: list,
    ) -> str:
        """PUT /repos/{owner}/{repo}/topics - Replace all repository topics"""
        # HTTP PUT /repos/{owner}/{repo}/topics
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if names is not None:
            query_params["names"] = str(names)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/topics",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_all_topics(
        owner: str,
        repo: str,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/topics - Get all repository topics"""
        # HTTP GET /repos/{owner}/{repo}/topics
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/topics",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_clones(
        owner: str,
        repo: str,
        per: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/traffic/clones - Get repository clones"""
        # HTTP GET /repos/{owner}/{repo}/traffic/clones
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per is not None:
            query_params["per"] = str(per)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/traffic/clones",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_top_paths(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/traffic/popular/paths - Get top referral paths"""
        # HTTP GET /repos/{owner}/{repo}/traffic/popular/paths
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/traffic/popular/paths",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_top_referrers(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/traffic/popular/referrers - Get top referral sources"""
        # HTTP GET /repos/{owner}/{repo}/traffic/popular/referrers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/traffic/popular/referrers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_get_views(
        owner: str,
        repo: str,
        per: str | None = None,
    ) -> str:
        """GET /repos/{owner}/{repo}/traffic/views - Get page views"""
        # HTTP GET /repos/{owner}/{repo}/traffic/views
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if per is not None:
            query_params["per"] = str(per)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/traffic/views",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_transfer(
        owner: str,
        repo: str,
        new_owner: str,
        new_name: str | None = None,
        team_ids: list | None = None,
    ) -> str:
        """POST /repos/{owner}/{repo}/transfer - Transfer a repository"""
        # HTTP POST /repos/{owner}/{repo}/transfer
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if new_owner is not None:
            query_params["new_owner"] = str(new_owner)
        if new_name is not None:
            query_params["new_name"] = str(new_name)
        if team_ids is not None:
            query_params["team_ids"] = str(team_ids)
        try:
            return await backend.request(
                "POST",
                "/repos/{owner}/{repo}/transfer",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_disable_vulnerability_alerts(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /repos/{owner}/{repo}/vulnerability-alerts - Disable vulnerability alerts"""
        # HTTP DELETE /repos/{owner}/{repo}/vulnerability-alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/repos/{owner}/{repo}/vulnerability-alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_enable_vulnerability_alerts(
        owner: str,
        repo: str,
    ) -> str:
        """PUT /repos/{owner}/{repo}/vulnerability-alerts - Enable vulnerability alerts"""
        # HTTP PUT /repos/{owner}/{repo}/vulnerability-alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "PUT",
                "/repos/{owner}/{repo}/vulnerability-alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_check_vulnerability_alerts(
        owner: str,
        repo: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/vulnerability-alerts - Check if vulnerability alerts are enabled for a repository"""
        # HTTP GET /repos/{owner}/{repo}/vulnerability-alerts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/vulnerability-alerts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_download_zipball_archive(
        owner: str,
        repo: str,
        ref: str,
    ) -> str:
        """GET /repos/{owner}/{repo}/zipball/{ref} - Download a repository archive (zip)"""
        # HTTP GET /repos/{owner}/{repo}/zipball/{ref}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        path_params["ref"] = str(ref)
        try:
            return await backend.request(
                "GET",
                "/repos/{owner}/{repo}/zipball/{ref}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_using_template(
        template_owner: str,
        template_repo: str,
        name: str,
        owner: str | None = None,
        description: str | None = None,
        include_all_branches: bool | None = None,
        private: bool | None = None,
    ) -> str:
        """POST /repos/{template_owner}/{template_repo}/generate - Create a repository using a template"""
        # HTTP POST /repos/{template_owner}/{template_repo}/generate
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["template_owner"] = str(template_owner)
        path_params["template_repo"] = str(template_repo)
        if owner is not None:
            query_params["owner"] = str(owner)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if include_all_branches is not None:
            query_params["include_all_branches"] = str(include_all_branches)
        if private is not None:
            query_params["private"] = str(private)
        try:
            return await backend.request(
                "POST",
                "/repos/{template_owner}/{template_repo}/generate",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_public(
        since: int | None = None,
    ) -> str:
        """GET /repositories - List public repositories"""
        # HTTP GET /repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if since is not None:
            query_params["since"] = str(since)
        try:
            return await backend.request(
                "GET",
                "/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_add_issue_field_values(
        repository_id: int,
        issue_number: int,
        issue_field_values: list | None = None,
    ) -> str:
        """POST /repositories/{repository_id}/issues/{issue_number}/issue-field-values - Add issue field values to an issue"""
        # HTTP POST /repositories/{repository_id}/issues/{issue_number}/issue-field-values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["repository_id"] = str(repository_id)
        path_params["issue_number"] = str(issue_number)
        if issue_field_values is not None:
            query_params["issue_field_values"] = str(issue_field_values)
        try:
            return await backend.request(
                "POST",
                "/repositories/{repository_id}/issues/{issue_number}/issue-field-values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_set_issue_field_values(
        repository_id: int,
        issue_number: int,
        issue_field_values: list | None = None,
    ) -> str:
        """PUT /repositories/{repository_id}/issues/{issue_number}/issue-field-values - Set issue field values for an issue"""
        # HTTP PUT /repositories/{repository_id}/issues/{issue_number}/issue-field-values
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["repository_id"] = str(repository_id)
        path_params["issue_number"] = str(issue_number)
        if issue_field_values is not None:
            query_params["issue_field_values"] = str(issue_field_values)
        try:
            return await backend.request(
                "PUT",
                "/repositories/{repository_id}/issues/{issue_number}/issue-field-values",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_delete_issue_field_value(
        repository_id: int,
        issue_number: int,
        issue_field_id: int,
    ) -> str:
        """DELETE /repositories/{repository_id}/issues/{issue_number}/issue-field-values/{issue_field_id} - Delete an issue field value from an issue"""
        # HTTP DELETE /repositories/{repository_id}/issues/{issue_number}/issue-field-values/{issue_field_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["repository_id"] = str(repository_id)
        path_params["issue_number"] = str(issue_number)
        path_params["issue_field_id"] = str(issue_field_id)
        try:
            return await backend.request(
                "DELETE",
                "/repositories/{repository_id}/issues/{issue_number}/issue-field-values/{issue_field_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_code(
        q: str,
        sort: str | None = None,
        order: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /search/code - Search code"""
        # HTTP GET /search/code
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if q is not None:
            query_params["q"] = str(q)
        if sort is not None:
            query_params["sort"] = str(sort)
        if order is not None:
            query_params["order"] = str(order)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/search/code",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_commits(
        q: str,
        sort: str | None = None,
        order: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /search/commits - Search commits"""
        # HTTP GET /search/commits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if q is not None:
            query_params["q"] = str(q)
        if sort is not None:
            query_params["sort"] = str(sort)
        if order is not None:
            query_params["order"] = str(order)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/search/commits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_issues_and_pull_requests(
        q: str,
        sort: str | None = None,
        order: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
        advanced_search: str | None = None,
    ) -> str:
        """GET /search/issues - Search issues and pull requests"""
        # HTTP GET /search/issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if q is not None:
            query_params["q"] = str(q)
        if sort is not None:
            query_params["sort"] = str(sort)
        if order is not None:
            query_params["order"] = str(order)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if advanced_search is not None:
            query_params["advanced_search"] = str(advanced_search)
        try:
            return await backend.request(
                "GET",
                "/search/issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_labels(
        repository_id: int,
        q: str,
        sort: str | None = None,
        order: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /search/labels - Search labels"""
        # HTTP GET /search/labels
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if repository_id is not None:
            query_params["repository_id"] = str(repository_id)
        if q is not None:
            query_params["q"] = str(q)
        if sort is not None:
            query_params["sort"] = str(sort)
        if order is not None:
            query_params["order"] = str(order)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/search/labels",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_repos(
        q: str,
        sort: str | None = None,
        order: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /search/repositories - Search repositories"""
        # HTTP GET /search/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if q is not None:
            query_params["q"] = str(q)
        if sort is not None:
            query_params["sort"] = str(sort)
        if order is not None:
            query_params["order"] = str(order)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/search/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_topics(
        q: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /search/topics - Search topics"""
        # HTTP GET /search/topics
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if q is not None:
            query_params["q"] = str(q)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/search/topics",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def search_users(
        q: str,
        sort: str | None = None,
        order: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /search/users - Search users"""
        # HTTP GET /search/users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if q is not None:
            query_params["q"] = str(q)
        if sort is not None:
            query_params["sort"] = str(sort)
        if order is not None:
            query_params["order"] = str(order)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/search/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_update_legacy(
        team_id: int,
        name: str,
        description: str | None = None,
        privacy: str | None = None,
        notification_setting: str | None = None,
        permission: str | None = None,
        parent_team_id: int | None = None,
    ) -> str:
        """PATCH /teams/{team_id} - Update a team (Legacy)"""
        # HTTP PATCH /teams/{team_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if privacy is not None:
            query_params["privacy"] = str(privacy)
        if notification_setting is not None:
            query_params["notification_setting"] = str(notification_setting)
        if permission is not None:
            query_params["permission"] = str(permission)
        if parent_team_id is not None:
            query_params["parent_team_id"] = str(parent_team_id)
        try:
            return await backend.request(
                "PATCH",
                "/teams/{team_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_delete_legacy(
        team_id: int,
    ) -> str:
        """DELETE /teams/{team_id} - Delete a team (Legacy)"""
        # HTTP DELETE /teams/{team_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        try:
            return await backend.request(
                "DELETE",
                "/teams/{team_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_get_legacy(
        team_id: int,
    ) -> str:
        """GET /teams/{team_id} - Get a team (Legacy)"""
        # HTTP GET /teams/{team_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_pending_invitations_legacy(
        team_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /teams/{team_id}/invitations - List pending team invitations (Legacy)"""
        # HTTP GET /teams/{team_id}/invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_members_legacy(
        team_id: int,
        role: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /teams/{team_id}/members - List team members (Legacy)"""
        # HTTP GET /teams/{team_id}/members
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        if role is not None:
            query_params["role"] = str(role)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/members",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_remove_member_legacy(
        team_id: int,
        username: str,
    ) -> str:
        """DELETE /teams/{team_id}/members/{username} - Remove team member (Legacy)"""
        # HTTP DELETE /teams/{team_id}/members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/teams/{team_id}/members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_add_member_legacy(
        team_id: int,
        username: str,
    ) -> str:
        """PUT /teams/{team_id}/members/{username} - Add team member (Legacy)"""
        # HTTP PUT /teams/{team_id}/members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "PUT",
                "/teams/{team_id}/members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_get_member_legacy(
        team_id: int,
        username: str,
    ) -> str:
        """GET /teams/{team_id}/members/{username} - Get team member (Legacy)"""
        # HTTP GET /teams/{team_id}/members/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/members/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_remove_membership_for_user_legacy(
        team_id: int,
        username: str,
    ) -> str:
        """DELETE /teams/{team_id}/memberships/{username} - Remove team membership for a user (Legacy)"""
        # HTTP DELETE /teams/{team_id}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/teams/{team_id}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_add_or_update_membership_for_user_legacy(
        team_id: int,
        username: str,
        role: str | None = None,
    ) -> str:
        """PUT /teams/{team_id}/memberships/{username} - Add or update team membership for a user (Legacy)"""
        # HTTP PUT /teams/{team_id}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["username"] = str(username)
        if role is not None:
            query_params["role"] = str(role)
        try:
            return await backend.request(
                "PUT",
                "/teams/{team_id}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_get_membership_for_user_legacy(
        team_id: int,
        username: str,
    ) -> str:
        """GET /teams/{team_id}/memberships/{username} - Get team membership for a user (Legacy)"""
        # HTTP GET /teams/{team_id}/memberships/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/memberships/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_repos_legacy(
        team_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /teams/{team_id}/repos - List team repositories (Legacy)"""
        # HTTP GET /teams/{team_id}/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_remove_repo_legacy(
        team_id: int,
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /teams/{team_id}/repos/{owner}/{repo} - Remove a repository from a team (Legacy)"""
        # HTTP DELETE /teams/{team_id}/repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/teams/{team_id}/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_add_or_update_repo_permissions_legacy(
        team_id: int,
        owner: str,
        repo: str,
        permission: str | None = None,
    ) -> str:
        """PUT /teams/{team_id}/repos/{owner}/{repo} - Add or update team repository permissions (Legacy)"""
        # HTTP PUT /teams/{team_id}/repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        if permission is not None:
            query_params["permission"] = str(permission)
        try:
            return await backend.request(
                "PUT",
                "/teams/{team_id}/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_check_permissions_for_repo_legacy(
        team_id: int,
        owner: str,
        repo: str,
    ) -> str:
        """GET /teams/{team_id}/repos/{owner}/{repo} - Check team permissions for a repository (Legacy)"""
        # HTTP GET /teams/{team_id}/repos/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/repos/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_child_legacy(
        team_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /teams/{team_id}/teams - List child teams (Legacy)"""
        # HTTP GET /teams/{team_id}/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["team_id"] = str(team_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/teams/{team_id}/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_update_authenticated(
        name: str | None = None,
        email: str | None = None,
        blog: str | None = None,
        twitter_username: str | None = None,
        company: str | None = None,
        location: str | None = None,
        hireable: bool | None = None,
        bio: str | None = None,
    ) -> str:
        """PATCH /user - Update the authenticated user"""
        # HTTP PATCH /user
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if name is not None:
            query_params["name"] = str(name)
        if email is not None:
            query_params["email"] = str(email)
        if blog is not None:
            query_params["blog"] = str(blog)
        if twitter_username is not None:
            query_params["twitter_username"] = str(twitter_username)
        if company is not None:
            query_params["company"] = str(company)
        if location is not None:
            query_params["location"] = str(location)
        if hireable is not None:
            query_params["hireable"] = str(hireable)
        if bio is not None:
            query_params["bio"] = str(bio)
        try:
            return await backend.request(
                "PATCH",
                "/user",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_authenticated(
    ) -> str:
        """GET /user - Get the authenticated user"""
        # HTTP GET /user
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/user",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_blocked_by_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/blocks - List users blocked by the authenticated user"""
        # HTTP GET /user/blocks
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/blocks",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_unblock(
        username: str,
    ) -> str:
        """DELETE /user/blocks/{username} - Unblock a user"""
        # HTTP DELETE /user/blocks/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/user/blocks/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_block(
        username: str,
    ) -> str:
        """PUT /user/blocks/{username} - Block a user"""
        # HTTP PUT /user/blocks/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "PUT",
                "/user/blocks/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_check_blocked(
        username: str,
    ) -> str:
        """GET /user/blocks/{username} - Check if a user is blocked by the authenticated user"""
        # HTTP GET /user/blocks/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/user/blocks/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_create_for_authenticated_user(
        body: dict,
    ) -> str:
        """POST /user/codespaces - Create a codespace for the authenticated user"""
        # HTTP POST /user/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/user/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_list_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
        repository_id: int | None = None,
    ) -> str:
        """GET /user/codespaces - List codespaces for the authenticated user"""
        # HTTP GET /user/codespaces
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if repository_id is not None:
            query_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_list_secrets_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/codespaces/secrets - List secrets for the authenticated user"""
        # HTTP GET /user/codespaces/secrets
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/secrets",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_public_key_for_authenticated_user(
    ) -> str:
        """GET /user/codespaces/secrets/public-key - Get public key for the authenticated user"""
        # HTTP GET /user/codespaces/secrets/public-key
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/secrets/public-key",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_delete_secret_for_authenticated_user(
        secret_name: str,
    ) -> str:
        """DELETE /user/codespaces/secrets/{secret_name} - Delete a secret for the authenticated user"""
        # HTTP DELETE /user/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "DELETE",
                "/user/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_user_codespaces_secrets_by_secret_name(
        secret_name: str,
        key_id: str,
        encrypted_value: str | None = None,
        selected_repository_ids: list | None = None,
    ) -> str:
        """PUT /user/codespaces/secrets/{secret_name} - Create or update a secret for the authenticated user"""
        # HTTP PUT /user/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        if encrypted_value is not None:
            query_params["encrypted_value"] = str(encrypted_value)
        if key_id is not None:
            query_params["key_id"] = str(key_id)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/user/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_secret_for_authenticated_user(
        secret_name: str,
    ) -> str:
        """GET /user/codespaces/secrets/{secret_name} - Get a secret for the authenticated user"""
        # HTTP GET /user/codespaces/secrets/{secret_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/secrets/{secret_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_user_codespaces_secrets_by_secret_name_repositories(
        secret_name: str,
        selected_repository_ids: list,
    ) -> str:
        """PUT /user/codespaces/secrets/{secret_name}/repositories - Set selected repositories for a user secret"""
        # HTTP PUT /user/codespaces/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        if selected_repository_ids is not None:
            query_params["selected_repository_ids"] = str(selected_repository_ids)
        try:
            return await backend.request(
                "PUT",
                "/user/codespaces/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_codespaces_secrets_by_secret_name_repositories(
        secret_name: str,
    ) -> str:
        """GET /user/codespaces/secrets/{secret_name}/repositories - List selected repositories for a user secret"""
        # HTTP GET /user/codespaces/secrets/{secret_name}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/secrets/{secret_name}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_codespaces_secrets_by_secret_name_repositories_by_repository_id(
        secret_name: str,
        repository_id: int,
    ) -> str:
        """DELETE /user/codespaces/secrets/{secret_name}/repositories/{repository_id} - Remove a selected repository from a user secret"""
        # HTTP DELETE /user/codespaces/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/codespaces/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_user_codespaces_secrets_by_secret_name_repositories_by_repository_id(
        secret_name: str,
        repository_id: int,
    ) -> str:
        """PUT /user/codespaces/secrets/{secret_name}/repositories/{repository_id} - Add a selected repository to a user secret"""
        # HTTP PUT /user/codespaces/secrets/{secret_name}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["secret_name"] = str(secret_name)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/user/codespaces/secrets/{secret_name}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_update_for_authenticated_user(
        codespace_name: str,
        machine: str | None = None,
        display_name: str | None = None,
        recent_folders: list | None = None,
    ) -> str:
        """PATCH /user/codespaces/{codespace_name} - Update a codespace for the authenticated user"""
        # HTTP PATCH /user/codespaces/{codespace_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        if machine is not None:
            query_params["machine"] = str(machine)
        if display_name is not None:
            query_params["display_name"] = str(display_name)
        if recent_folders is not None:
            query_params["recent_folders"] = str(recent_folders)
        try:
            return await backend.request(
                "PATCH",
                "/user/codespaces/{codespace_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_delete_for_authenticated_user(
        codespace_name: str,
    ) -> str:
        """DELETE /user/codespaces/{codespace_name} - Delete a codespace for the authenticated user"""
        # HTTP DELETE /user/codespaces/{codespace_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "DELETE",
                "/user/codespaces/{codespace_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_get_for_authenticated_user(
        codespace_name: str,
    ) -> str:
        """GET /user/codespaces/{codespace_name} - Get a codespace for the authenticated user"""
        # HTTP GET /user/codespaces/{codespace_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/{codespace_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_export_for_authenticated_user(
        codespace_name: str,
    ) -> str:
        """POST /user/codespaces/{codespace_name}/exports - Export a codespace for the authenticated user"""
        # HTTP POST /user/codespaces/{codespace_name}/exports
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "POST",
                "/user/codespaces/{codespace_name}/exports",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_codespaces_by_codespace_name_exports_by_export_id(
        codespace_name: str,
        export_id: str,
    ) -> str:
        """GET /user/codespaces/{codespace_name}/exports/{export_id} - Get details about a codespace export"""
        # HTTP GET /user/codespaces/{codespace_name}/exports/{export_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        path_params["export_id"] = str(export_id)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/{codespace_name}/exports/{export_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_codespaces_by_codespace_name_machines(
        codespace_name: str,
    ) -> str:
        """GET /user/codespaces/{codespace_name}/machines - List machine types for a codespace"""
        # HTTP GET /user/codespaces/{codespace_name}/machines
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "GET",
                "/user/codespaces/{codespace_name}/machines",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_publish_for_authenticated_user(
        codespace_name: str,
        name: str | None = None,
        private: bool | None = None,
    ) -> str:
        """POST /user/codespaces/{codespace_name}/publish - Create a repository from an unpublished codespace"""
        # HTTP POST /user/codespaces/{codespace_name}/publish
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        if name is not None:
            query_params["name"] = str(name)
        if private is not None:
            query_params["private"] = str(private)
        try:
            return await backend.request(
                "POST",
                "/user/codespaces/{codespace_name}/publish",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_start_for_authenticated_user(
        codespace_name: str,
    ) -> str:
        """POST /user/codespaces/{codespace_name}/start - Start a codespace for the authenticated user"""
        # HTTP POST /user/codespaces/{codespace_name}/start
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "POST",
                "/user/codespaces/{codespace_name}/start",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def codespaces_stop_for_authenticated_user(
        codespace_name: str,
    ) -> str:
        """POST /user/codespaces/{codespace_name}/stop - Stop a codespace for the authenticated user"""
        # HTTP POST /user/codespaces/{codespace_name}/stop
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["codespace_name"] = str(codespace_name)
        try:
            return await backend.request(
                "POST",
                "/user/codespaces/{codespace_name}/stop",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_docker_conflicts(
    ) -> str:
        """GET /user/docker/conflicts - Get list of conflicting packages during Docker migration for authenticated-user"""
        # HTTP GET /user/docker/conflicts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/user/docker/conflicts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def patch_user_email_visibility(
        visibility: str,
    ) -> str:
        """PATCH /user/email/visibility - Set primary email visibility for the authenticated user"""
        # HTTP PATCH /user/email/visibility
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        try:
            return await backend.request(
                "PATCH",
                "/user/email/visibility",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_add_email_for_authenticated_user(
        body: dict | None = None,
    ) -> str:
        """POST /user/emails - Add an email address for the authenticated user"""
        # HTTP POST /user/emails
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/user/emails",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_delete_email_for_authenticated_user(
        body: dict,
    ) -> str:
        """DELETE /user/emails - Delete an email address for the authenticated user"""
        # HTTP DELETE /user/emails
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "DELETE",
                "/user/emails",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_emails_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/emails - List email addresses for the authenticated user"""
        # HTTP GET /user/emails
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/emails",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_followers_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/followers - List followers of the authenticated user"""
        # HTTP GET /user/followers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/followers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_followed_by_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/following - List the people the authenticated user follows"""
        # HTTP GET /user/following
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/following",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_unfollow(
        username: str,
    ) -> str:
        """DELETE /user/following/{username} - Unfollow a user"""
        # HTTP DELETE /user/following/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/user/following/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_follow(
        username: str,
    ) -> str:
        """PUT /user/following/{username} - Follow a user"""
        # HTTP PUT /user/following/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "PUT",
                "/user/following/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_check_person_is_followed_by_authenticated(
        username: str,
    ) -> str:
        """GET /user/following/{username} - Check if a person is followed by the authenticated user"""
        # HTTP GET /user/following/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/user/following/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_create_gpg_key_for_authenticated_user(
        armored_public_key: str,
        name: str | None = None,
    ) -> str:
        """POST /user/gpg_keys - Create a GPG key for the authenticated user"""
        # HTTP POST /user/gpg_keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if name is not None:
            query_params["name"] = str(name)
        if armored_public_key is not None:
            query_params["armored_public_key"] = str(armored_public_key)
        try:
            return await backend.request(
                "POST",
                "/user/gpg_keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_gpg_keys_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/gpg_keys - List GPG keys for the authenticated user"""
        # HTTP GET /user/gpg_keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/gpg_keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_delete_gpg_key_for_authenticated_user(
        gpg_key_id: int,
    ) -> str:
        """DELETE /user/gpg_keys/{gpg_key_id} - Delete a GPG key for the authenticated user"""
        # HTTP DELETE /user/gpg_keys/{gpg_key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gpg_key_id"] = str(gpg_key_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/gpg_keys/{gpg_key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_gpg_key_for_authenticated_user(
        gpg_key_id: int,
    ) -> str:
        """GET /user/gpg_keys/{gpg_key_id} - Get a GPG key for the authenticated user"""
        # HTTP GET /user/gpg_keys/{gpg_key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["gpg_key_id"] = str(gpg_key_id)
        try:
            return await backend.request(
                "GET",
                "/user/gpg_keys/{gpg_key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_installations_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/installations - List app installations accessible to the user access token"""
        # HTTP GET /user/installations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/installations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_installations_by_installation_id_repositories(
        installation_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/installations/{installation_id}/repositories - List repositories accessible to the user access token"""
        # HTTP GET /user/installations/{installation_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/installations/{installation_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_installations_by_installation_id_repositories_by_repository_id(
        installation_id: int,
        repository_id: int,
    ) -> str:
        """DELETE /user/installations/{installation_id}/repositories/{repository_id} - Remove a repository from an app installation"""
        # HTTP DELETE /user/installations/{installation_id}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/installations/{installation_id}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_user_installations_by_installation_id_repositories_by_repository_id(
        installation_id: int,
        repository_id: int,
    ) -> str:
        """PUT /user/installations/{installation_id}/repositories/{repository_id} - Add a repository to an app installation"""
        # HTTP PUT /user/installations/{installation_id}/repositories/{repository_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["installation_id"] = str(installation_id)
        path_params["repository_id"] = str(repository_id)
        try:
            return await backend.request(
                "PUT",
                "/user/installations/{installation_id}/repositories/{repository_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_interaction_limits(
    ) -> str:
        """DELETE /user/interaction-limits - Remove interaction restrictions from your public repositories"""
        # HTTP DELETE /user/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "DELETE",
                "/user/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def put_user_interaction_limits(
        limit: str,
        expiry: str | None = None,
    ) -> str:
        """PUT /user/interaction-limits - Set interaction restrictions for your public repositories"""
        # HTTP PUT /user/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if limit is not None:
            query_params["limit"] = str(limit)
        if expiry is not None:
            query_params["expiry"] = str(expiry)
        try:
            return await backend.request(
                "PUT",
                "/user/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_interaction_limits(
    ) -> str:
        """GET /user/interaction-limits - Get interaction restrictions for your public repositories"""
        # HTTP GET /user/interaction-limits
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/user/interaction-limits",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def issues_list_for_authenticated_user(
        filter: str | None = None,
        state: str | None = None,
        labels: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/issues - List user account issues assigned to the authenticated user"""
        # HTTP GET /user/issues
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if filter is not None:
            query_params["filter"] = str(filter)
        if state is not None:
            query_params["state"] = str(state)
        if labels is not None:
            query_params["labels"] = str(labels)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/issues",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_user_keys(
        key: str,
        title: str | None = None,
    ) -> str:
        """POST /user/keys - Create a public SSH key for the authenticated user"""
        # HTTP POST /user/keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if title is not None:
            query_params["title"] = str(title)
        if key is not None:
            query_params["key"] = str(key)
        try:
            return await backend.request(
                "POST",
                "/user/keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_public_ssh_keys_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/keys - List public SSH keys for the authenticated user"""
        # HTTP GET /user/keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_keys_by_key_id(
        key_id: int,
    ) -> str:
        """DELETE /user/keys/{key_id} - Delete a public SSH key for the authenticated user"""
        # HTTP DELETE /user/keys/{key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/keys/{key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_public_ssh_key_for_authenticated_user(
        key_id: int,
    ) -> str:
        """GET /user/keys/{key_id} - Get a public SSH key for the authenticated user"""
        # HTTP GET /user/keys/{key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["key_id"] = str(key_id)
        try:
            return await backend.request(
                "GET",
                "/user/keys/{key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_list_subscriptions_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/marketplace_purchases - List subscriptions for the authenticated user"""
        # HTTP GET /user/marketplace_purchases
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/marketplace_purchases",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_marketplace_purchases_stubbed(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/marketplace_purchases/stubbed - List subscriptions for the authenticated user (stubbed)"""
        # HTTP GET /user/marketplace_purchases/stubbed
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/marketplace_purchases/stubbed",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_memberships_for_authenticated_user(
        state: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/memberships/orgs - List organization memberships for the authenticated user"""
        # HTTP GET /user/memberships/orgs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if state is not None:
            query_params["state"] = str(state)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/memberships/orgs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_update_membership_for_authenticated_user(
        org: str,
        state: str,
    ) -> str:
        """PATCH /user/memberships/orgs/{org} - Update an organization membership for the authenticated user"""
        # HTTP PATCH /user/memberships/orgs/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "PATCH",
                "/user/memberships/orgs/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_get_membership_for_authenticated_user(
        org: str,
    ) -> str:
        """GET /user/memberships/orgs/{org} - Get an organization membership for the authenticated user"""
        # HTTP GET /user/memberships/orgs/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["org"] = str(org)
        try:
            return await backend.request(
                "GET",
                "/user/memberships/orgs/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_start_for_authenticated_user(
        repositories: list,
        lock_repositories: bool | None = None,
        exclude_metadata: bool | None = None,
        exclude_git_data: bool | None = None,
        exclude_attachments: bool | None = None,
        exclude_releases: bool | None = None,
        exclude_owner_projects: bool | None = None,
        org_metadata_only: bool | None = None,
        exclude: list | None = None,
    ) -> str:
        """POST /user/migrations - Start a user migration"""
        # HTTP POST /user/migrations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if lock_repositories is not None:
            query_params["lock_repositories"] = str(lock_repositories)
        if exclude_metadata is not None:
            query_params["exclude_metadata"] = str(exclude_metadata)
        if exclude_git_data is not None:
            query_params["exclude_git_data"] = str(exclude_git_data)
        if exclude_attachments is not None:
            query_params["exclude_attachments"] = str(exclude_attachments)
        if exclude_releases is not None:
            query_params["exclude_releases"] = str(exclude_releases)
        if exclude_owner_projects is not None:
            query_params["exclude_owner_projects"] = str(exclude_owner_projects)
        if org_metadata_only is not None:
            query_params["org_metadata_only"] = str(org_metadata_only)
        if exclude is not None:
            query_params["exclude"] = str(exclude)
        if repositories is not None:
            query_params["repositories"] = str(repositories)
        try:
            return await backend.request(
                "POST",
                "/user/migrations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_list_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/migrations - List user migrations"""
        # HTTP GET /user/migrations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/migrations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_get_status_for_authenticated_user(
        migration_id: int,
        exclude: list | None = None,
    ) -> str:
        """GET /user/migrations/{migration_id} - Get a user migration status"""
        # HTTP GET /user/migrations/{migration_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["migration_id"] = str(migration_id)
        if exclude is not None:
            query_params["exclude"] = str(exclude)
        try:
            return await backend.request(
                "GET",
                "/user/migrations/{migration_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_delete_archive_for_authenticated_user(
        migration_id: int,
    ) -> str:
        """DELETE /user/migrations/{migration_id}/archive - Delete a user migration archive"""
        # HTTP DELETE /user/migrations/{migration_id}/archive
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["migration_id"] = str(migration_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/migrations/{migration_id}/archive",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_get_archive_for_authenticated_user(
        migration_id: int,
    ) -> str:
        """GET /user/migrations/{migration_id}/archive - Download a user migration archive"""
        # HTTP GET /user/migrations/{migration_id}/archive
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["migration_id"] = str(migration_id)
        try:
            return await backend.request(
                "GET",
                "/user/migrations/{migration_id}/archive",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_unlock_repo_for_authenticated_user(
        migration_id: int,
        repo_name: str,
    ) -> str:
        """DELETE /user/migrations/{migration_id}/repos/{repo_name}/lock - Unlock a user repository"""
        # HTTP DELETE /user/migrations/{migration_id}/repos/{repo_name}/lock
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["migration_id"] = str(migration_id)
        path_params["repo_name"] = str(repo_name)
        try:
            return await backend.request(
                "DELETE",
                "/user/migrations/{migration_id}/repos/{repo_name}/lock",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def migrations_list_repos_for_authenticated_user(
        migration_id: int,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/migrations/{migration_id}/repositories - List repositories for a user migration"""
        # HTTP GET /user/migrations/{migration_id}/repositories
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["migration_id"] = str(migration_id)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/migrations/{migration_id}/repositories",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/orgs - List organizations for the authenticated user"""
        # HTTP GET /user/orgs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/orgs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_list_packages_for_authenticated_user(
        package_type: str,
        visibility: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /user/packages - List packages for the authenticated user's namespace"""
        # HTTP GET /user/packages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if package_type is not None:
            query_params["package_type"] = str(package_type)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/user/packages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_delete_package_for_authenticated_user(
        package_type: str,
        package_name: str,
    ) -> str:
        """DELETE /user/packages/{package_type}/{package_name} - Delete a package for the authenticated user"""
        # HTTP DELETE /user/packages/{package_type}/{package_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        try:
            return await backend.request(
                "DELETE",
                "/user/packages/{package_type}/{package_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_get_package_for_authenticated_user(
        package_type: str,
        package_name: str,
    ) -> str:
        """GET /user/packages/{package_type}/{package_name} - Get a package for the authenticated user"""
        # HTTP GET /user/packages/{package_type}/{package_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        try:
            return await backend.request(
                "GET",
                "/user/packages/{package_type}/{package_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_restore_package_for_authenticated_user(
        package_type: str,
        package_name: str,
        token: str | None = None,
    ) -> str:
        """POST /user/packages/{package_type}/{package_name}/restore - Restore a package for the authenticated user"""
        # HTTP POST /user/packages/{package_type}/{package_name}/restore
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        if token is not None:
            query_params["token"] = str(token)
        try:
            return await backend.request(
                "POST",
                "/user/packages/{package_type}/{package_name}/restore",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_packages_by_package_type_by_package_name_versions(
        package_type: str,
        package_name: str,
        page: int | None = None,
        per_page: int | None = None,
        state: str | None = None,
    ) -> str:
        """GET /user/packages/{package_type}/{package_name}/versions - List package versions for a package owned by the authenticated user"""
        # HTTP GET /user/packages/{package_type}/{package_name}/versions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if state is not None:
            query_params["state"] = str(state)
        try:
            return await backend.request(
                "GET",
                "/user/packages/{package_type}/{package_name}/versions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_packages_by_package_type_by_package_name_versions_by_package_version_id(
        package_type: str,
        package_name: str,
        package_version_id: int,
    ) -> str:
        """DELETE /user/packages/{package_type}/{package_name}/versions/{package_version_id} - Delete a package version for the authenticated user"""
        # HTTP DELETE /user/packages/{package_type}/{package_name}/versions/{package_version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/packages/{package_type}/{package_name}/versions/{package_version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_packages_by_package_type_by_package_name_versions_by_package_version_id(
        package_type: str,
        package_name: str,
        package_version_id: int,
    ) -> str:
        """GET /user/packages/{package_type}/{package_name}/versions/{package_version_id} - Get a package version for the authenticated user"""
        # HTTP GET /user/packages/{package_type}/{package_name}/versions/{package_version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "GET",
                "/user/packages/{package_type}/{package_name}/versions/{package_version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_user_packages_by_package_type_by_package_name_versions_by_package_version_id_restore(
        package_type: str,
        package_name: str,
        package_version_id: int,
    ) -> str:
        """POST /user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore - Restore a package version for the authenticated user"""
        # HTTP POST /user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "POST",
                "/user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_public_emails_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/public_emails - List public email addresses for the authenticated user"""
        # HTTP GET /user/public_emails
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/public_emails",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_create_for_authenticated_user(
        name: str,
        description: str | None = None,
        homepage: str | None = None,
        private: bool | None = None,
        has_issues: bool | None = None,
        has_projects: bool | None = None,
        has_wiki: bool | None = None,
        has_discussions: bool | None = None,
        team_id: int | None = None,
        auto_init: bool | None = None,
        gitignore_template: str | None = None,
        license_template: str | None = None,
        allow_squash_merge: bool | None = None,
        allow_merge_commit: bool | None = None,
        allow_rebase_merge: bool | None = None,
        allow_auto_merge: bool | None = None,
        delete_branch_on_merge: bool | None = None,
        squash_merge_commit_title: str | None = None,
        squash_merge_commit_message: str | None = None,
        merge_commit_title: str | None = None,
        merge_commit_message: str | None = None,
        has_downloads: bool | None = None,
        is_template: bool | None = None,
    ) -> str:
        """POST /user/repos - Create a repository for the authenticated user"""
        # HTTP POST /user/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if name is not None:
            query_params["name"] = str(name)
        if description is not None:
            query_params["description"] = str(description)
        if homepage is not None:
            query_params["homepage"] = str(homepage)
        if private is not None:
            query_params["private"] = str(private)
        if has_issues is not None:
            query_params["has_issues"] = str(has_issues)
        if has_projects is not None:
            query_params["has_projects"] = str(has_projects)
        if has_wiki is not None:
            query_params["has_wiki"] = str(has_wiki)
        if has_discussions is not None:
            query_params["has_discussions"] = str(has_discussions)
        if team_id is not None:
            query_params["team_id"] = str(team_id)
        if auto_init is not None:
            query_params["auto_init"] = str(auto_init)
        if gitignore_template is not None:
            query_params["gitignore_template"] = str(gitignore_template)
        if license_template is not None:
            query_params["license_template"] = str(license_template)
        if allow_squash_merge is not None:
            query_params["allow_squash_merge"] = str(allow_squash_merge)
        if allow_merge_commit is not None:
            query_params["allow_merge_commit"] = str(allow_merge_commit)
        if allow_rebase_merge is not None:
            query_params["allow_rebase_merge"] = str(allow_rebase_merge)
        if allow_auto_merge is not None:
            query_params["allow_auto_merge"] = str(allow_auto_merge)
        if delete_branch_on_merge is not None:
            query_params["delete_branch_on_merge"] = str(delete_branch_on_merge)
        if squash_merge_commit_title is not None:
            query_params["squash_merge_commit_title"] = str(squash_merge_commit_title)
        if squash_merge_commit_message is not None:
            query_params["squash_merge_commit_message"] = str(squash_merge_commit_message)
        if merge_commit_title is not None:
            query_params["merge_commit_title"] = str(merge_commit_title)
        if merge_commit_message is not None:
            query_params["merge_commit_message"] = str(merge_commit_message)
        if has_downloads is not None:
            query_params["has_downloads"] = str(has_downloads)
        if is_template is not None:
            query_params["is_template"] = str(is_template)
        try:
            return await backend.request(
                "POST",
                "/user/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_for_authenticated_user(
        visibility: str | None = None,
        affiliation: str | None = None,
        type: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
        since: str | None = None,
        before: str | None = None,
    ) -> str:
        """GET /user/repos - List repositories for the authenticated user"""
        # HTTP GET /user/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        if affiliation is not None:
            query_params["affiliation"] = str(affiliation)
        if type is not None:
            query_params["type"] = str(type)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        if since is not None:
            query_params["since"] = str(since)
        if before is not None:
            query_params["before"] = str(before)
        try:
            return await backend.request(
                "GET",
                "/user/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_invitations_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/repository_invitations - List repository invitations for the authenticated user"""
        # HTTP GET /user/repository_invitations
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/repository_invitations",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_accept_invitation_for_authenticated_user(
        invitation_id: int,
    ) -> str:
        """PATCH /user/repository_invitations/{invitation_id} - Accept a repository invitation"""
        # HTTP PATCH /user/repository_invitations/{invitation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["invitation_id"] = str(invitation_id)
        try:
            return await backend.request(
                "PATCH",
                "/user/repository_invitations/{invitation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_decline_invitation_for_authenticated_user(
        invitation_id: int,
    ) -> str:
        """DELETE /user/repository_invitations/{invitation_id} - Decline a repository invitation"""
        # HTTP DELETE /user/repository_invitations/{invitation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["invitation_id"] = str(invitation_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/repository_invitations/{invitation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_add_social_account_for_authenticated_user(
        account_urls: list,
    ) -> str:
        """POST /user/social_accounts - Add social accounts for the authenticated user"""
        # HTTP POST /user/social_accounts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if account_urls is not None:
            query_params["account_urls"] = str(account_urls)
        try:
            return await backend.request(
                "POST",
                "/user/social_accounts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_social_accounts(
        account_urls: list,
    ) -> str:
        """DELETE /user/social_accounts - Delete social accounts for the authenticated user"""
        # HTTP DELETE /user/social_accounts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if account_urls is not None:
            query_params["account_urls"] = str(account_urls)
        try:
            return await backend.request(
                "DELETE",
                "/user/social_accounts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_social_accounts_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/social_accounts - List social accounts for the authenticated user"""
        # HTTP GET /user/social_accounts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/social_accounts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def post_user_ssh_signing_keys(
        key: str,
        title: str | None = None,
    ) -> str:
        """POST /user/ssh_signing_keys - Create a SSH signing key for the authenticated user"""
        # HTTP POST /user/ssh_signing_keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if title is not None:
            query_params["title"] = str(title)
        if key is not None:
            query_params["key"] = str(key)
        try:
            return await backend.request(
                "POST",
                "/user/ssh_signing_keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_ssh_signing_keys(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/ssh_signing_keys - List SSH signing keys for the authenticated user"""
        # HTTP GET /user/ssh_signing_keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/ssh_signing_keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def delete_user_ssh_signing_keys_by_ssh_signing_key_id(
        ssh_signing_key_id: int,
    ) -> str:
        """DELETE /user/ssh_signing_keys/{ssh_signing_key_id} - Delete an SSH signing key for the authenticated user"""
        # HTTP DELETE /user/ssh_signing_keys/{ssh_signing_key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["ssh_signing_key_id"] = str(ssh_signing_key_id)
        try:
            return await backend.request(
                "DELETE",
                "/user/ssh_signing_keys/{ssh_signing_key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_ssh_signing_key_for_authenticated_user(
        ssh_signing_key_id: int,
    ) -> str:
        """GET /user/ssh_signing_keys/{ssh_signing_key_id} - Get an SSH signing key for the authenticated user"""
        # HTTP GET /user/ssh_signing_keys/{ssh_signing_key_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["ssh_signing_key_id"] = str(ssh_signing_key_id)
        try:
            return await backend.request(
                "GET",
                "/user/ssh_signing_keys/{ssh_signing_key_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_repos_starred_by_authenticated_user(
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/starred - List repositories starred by the authenticated user"""
        # HTTP GET /user/starred
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/starred",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_unstar_repo_for_authenticated_user(
        owner: str,
        repo: str,
    ) -> str:
        """DELETE /user/starred/{owner}/{repo} - Unstar a repository for the authenticated user"""
        # HTTP DELETE /user/starred/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "DELETE",
                "/user/starred/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_star_repo_for_authenticated_user(
        owner: str,
        repo: str,
    ) -> str:
        """PUT /user/starred/{owner}/{repo} - Star a repository for the authenticated user"""
        # HTTP PUT /user/starred/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "PUT",
                "/user/starred/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_starred_by_owner_by_repo(
        owner: str,
        repo: str,
    ) -> str:
        """GET /user/starred/{owner}/{repo} - Check if a repository is starred by the authenticated user"""
        # HTTP GET /user/starred/{owner}/{repo}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["owner"] = str(owner)
        path_params["repo"] = str(repo)
        try:
            return await backend.request(
                "GET",
                "/user/starred/{owner}/{repo}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_user_subscriptions(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/subscriptions - List repositories watched by the authenticated user"""
        # HTTP GET /user/subscriptions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/subscriptions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def teams_list_for_authenticated_user(
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /user/teams - List teams for the authenticated user"""
        # HTTP GET /user/teams
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/user/teams",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_by_id(
        account_id: int,
    ) -> str:
        """GET /user/{account_id} - Get a user using their ID"""
        # HTTP GET /user/{account_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["account_id"] = str(account_id)
        try:
            return await backend.request(
                "GET",
                "/user/{account_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_create_draft_item_for_authenticated_user(
        user_id: str,
        project_number: int,
        title: str,
        body: str | None = None,
    ) -> str:
        """POST /user/{user_id}/projectsV2/{project_number}/drafts - Create draft item for user owned project"""
        # HTTP POST /user/{user_id}/projectsV2/{project_number}/drafts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["user_id"] = str(user_id)
        path_params["project_number"] = str(project_number)
        if title is not None:
            query_params["title"] = str(title)
        if body is not None:
            query_params["body"] = str(body)
        try:
            return await backend.request(
                "POST",
                "/user/{user_id}/projectsV2/{project_number}/drafts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list(
        since: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /users - List users"""
        # HTTP GET /users
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/users",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_create_view_for_user(
        user_id: str,
        project_number: int,
        name: str,
        layout: str,
        filter: str | None = None,
        visible_fields: list | None = None,
    ) -> str:
        """POST /users/{user_id}/projectsV2/{project_number}/views - Create a view for a user-owned project"""
        # HTTP POST /users/{user_id}/projectsV2/{project_number}/views
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["user_id"] = str(user_id)
        path_params["project_number"] = str(project_number)
        if name is not None:
            query_params["name"] = str(name)
        if layout is not None:
            query_params["layout"] = str(layout)
        if filter is not None:
            query_params["filter"] = str(filter)
        if visible_fields is not None:
            query_params["visible_fields"] = str(visible_fields)
        try:
            return await backend.request(
                "POST",
                "/users/{user_id}/projectsV2/{project_number}/views",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_by_username(
        username: str,
    ) -> str:
        """GET /users/{username} - Get a user"""
        # HTTP GET /users/{username}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_attestations_bulk(
        username: str,
        subject_digests: list,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        predicate_type: str | None = None,
    ) -> str:
        """POST /users/{username}/attestations/bulk-list - List attestations by bulk subject digests"""
        # HTTP POST /users/{username}/attestations/bulk-list
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        path_params["username"] = str(username)
        if subject_digests is not None:
            query_params["subject_digests"] = str(subject_digests)
        if predicate_type is not None:
            query_params["predicate_type"] = str(predicate_type)
        try:
            return await backend.request(
                "POST",
                "/users/{username}/attestations/bulk-list",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_delete_attestations_bulk(
        username: str,
        body: dict,
    ) -> str:
        """POST /users/{username}/attestations/delete-request - Delete attestations in bulk"""
        # HTTP POST /users/{username}/attestations/delete-request
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/users/{username}/attestations/delete-request",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_delete_attestations_by_subject_digest(
        username: str,
        subject_digest: str,
    ) -> str:
        """DELETE /users/{username}/attestations/digest/{subject_digest} - Delete attestations by subject digest"""
        # HTTP DELETE /users/{username}/attestations/digest/{subject_digest}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        path_params["subject_digest"] = str(subject_digest)
        try:
            return await backend.request(
                "DELETE",
                "/users/{username}/attestations/digest/{subject_digest}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_delete_attestations_by_id(
        username: str,
        attestation_id: int,
    ) -> str:
        """DELETE /users/{username}/attestations/{attestation_id} - Delete attestations by ID"""
        # HTTP DELETE /users/{username}/attestations/{attestation_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        path_params["attestation_id"] = str(attestation_id)
        try:
            return await backend.request(
                "DELETE",
                "/users/{username}/attestations/{attestation_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_attestations(
        username: str,
        subject_digest: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
        predicate_type: str | None = None,
    ) -> str:
        """GET /users/{username}/attestations/{subject_digest} - List attestations"""
        # HTTP GET /users/{username}/attestations/{subject_digest}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        path_params["username"] = str(username)
        path_params["subject_digest"] = str(subject_digest)
        if predicate_type is not None:
            query_params["predicate_type"] = str(predicate_type)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/attestations/{subject_digest}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_users_by_username_docker_conflicts(
        username: str,
    ) -> str:
        """GET /users/{username}/docker/conflicts - Get list of conflicting packages during Docker migration for user"""
        # HTTP GET /users/{username}/docker/conflicts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/docker/conflicts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_events_for_authenticated_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/events - List events for the authenticated user"""
        # HTTP GET /users/{username}/events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_org_events_for_authenticated_user(
        username: str,
        org: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/events/orgs/{org} - List organization events for the authenticated user"""
        # HTTP GET /users/{username}/events/orgs/{org}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        path_params["org"] = str(org)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/events/orgs/{org}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_public_events_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/events/public - List public events for a user"""
        # HTTP GET /users/{username}/events/public
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/events/public",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_followers_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/followers - List followers of a user"""
        # HTTP GET /users/{username}/followers
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/followers",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_following_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/following - List the people a user follows"""
        # HTTP GET /users/{username}/following
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/following",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_check_following_for_user(
        username: str,
        target_user: str,
    ) -> str:
        """GET /users/{username}/following/{target_user} - Check if a user follows another user"""
        # HTTP GET /users/{username}/following/{target_user}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        path_params["target_user"] = str(target_user)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/following/{target_user}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def gists_list_for_user(
        username: str,
        since: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/gists - List gists for a user"""
        # HTTP GET /users/{username}/gists
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if since is not None:
            query_params["since"] = str(since)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/gists",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_gpg_keys_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/gpg_keys - List GPG keys for a user"""
        # HTTP GET /users/{username}/gpg_keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/gpg_keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_get_context_for_user(
        username: str,
        subject_type: str | None = None,
        subject_id: str | None = None,
    ) -> str:
        """GET /users/{username}/hovercard - Get contextual information for a user"""
        # HTTP GET /users/{username}/hovercard
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if subject_type is not None:
            query_params["subject_type"] = str(subject_type)
        if subject_id is not None:
            query_params["subject_id"] = str(subject_id)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/hovercard",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def apps_get_user_installation(
        username: str,
    ) -> str:
        """GET /users/{username}/installation - Get a user installation for the authenticated app"""
        # HTTP GET /users/{username}/installation
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/installation",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_public_keys_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/keys - List public keys for a user"""
        # HTTP GET /users/{username}/keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def orgs_list_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/orgs - List organizations for a user"""
        # HTTP GET /users/{username}/orgs
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/orgs",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_list_packages_for_user(
        package_type: str,
        username: str,
        visibility: str | None = None,
        page: int | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /users/{username}/packages - List packages for a user"""
        # HTTP GET /users/{username}/packages
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        if package_type is not None:
            query_params["package_type"] = str(package_type)
        if visibility is not None:
            query_params["visibility"] = str(visibility)
        path_params["username"] = str(username)
        if page is not None:
            query_params["page"] = str(page)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/packages",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_delete_package_for_user(
        package_type: str,
        package_name: str,
        username: str,
    ) -> str:
        """DELETE /users/{username}/packages/{package_type}/{package_name} - Delete a package for a user"""
        # HTTP DELETE /users/{username}/packages/{package_type}/{package_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "DELETE",
                "/users/{username}/packages/{package_type}/{package_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_get_package_for_user(
        package_type: str,
        package_name: str,
        username: str,
    ) -> str:
        """GET /users/{username}/packages/{package_type}/{package_name} - Get a package for a user"""
        # HTTP GET /users/{username}/packages/{package_type}/{package_name}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/packages/{package_type}/{package_name}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_restore_package_for_user(
        package_type: str,
        package_name: str,
        username: str,
        token: str | None = None,
    ) -> str:
        """POST /users/{username}/packages/{package_type}/{package_name}/restore - Restore a package for a user"""
        # HTTP POST /users/{username}/packages/{package_type}/{package_name}/restore
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["username"] = str(username)
        if token is not None:
            query_params["token"] = str(token)
        try:
            return await backend.request(
                "POST",
                "/users/{username}/packages/{package_type}/{package_name}/restore",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_users_by_username_packages_by_package_type_by_package_name_versions(
        package_type: str,
        package_name: str,
        username: str,
    ) -> str:
        """GET /users/{username}/packages/{package_type}/{package_name}/versions - List package versions for a package owned by a user"""
        # HTTP GET /users/{username}/packages/{package_type}/{package_name}/versions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/packages/{package_type}/{package_name}/versions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_delete_package_version_for_user(
        package_type: str,
        package_name: str,
        username: str,
        package_version_id: int,
    ) -> str:
        """DELETE /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id} - Delete package version for a user"""
        # HTTP DELETE /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["username"] = str(username)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "DELETE",
                "/users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_get_package_version_for_user(
        package_type: str,
        package_name: str,
        package_version_id: int,
        username: str,
    ) -> str:
        """GET /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id} - Get a package version for a user"""
        # HTTP GET /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["package_version_id"] = str(package_version_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def packages_restore_package_version_for_user(
        package_type: str,
        package_name: str,
        username: str,
        package_version_id: int,
    ) -> str:
        """POST /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore - Restore package version for a user"""
        # HTTP POST /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["package_type"] = str(package_type)
        path_params["package_name"] = str(package_name)
        path_params["username"] = str(username)
        path_params["package_version_id"] = str(package_version_id)
        try:
            return await backend.request(
                "POST",
                "/users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_for_user(
        username: str,
        q: str | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /users/{username}/projectsV2 - List projects for user"""
        # HTTP GET /users/{username}/projectsV2
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if q is not None:
            query_params["q"] = str(q)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_get_for_user(
        project_number: int,
        username: str,
    ) -> str:
        """GET /users/{username}/projectsV2/{project_number} - Get project for user"""
        # HTTP GET /users/{username}/projectsV2/{project_number}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2/{project_number}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_add_field_for_user(
        username: str,
        project_number: int,
        body: dict,
    ) -> str:
        """POST /users/{username}/projectsV2/{project_number}/fields - Add field to user owned project"""
        # HTTP POST /users/{username}/projectsV2/{project_number}/fields
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        path_params["project_number"] = str(project_number)
        body = body if isinstance(body, dict) else {"data": body}
        try:
            return await backend.request(
                "POST",
                "/users/{username}/projectsV2/{project_number}/fields",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_fields_for_user(
        project_number: int,
        username: str,
        per_page: int | None = None,
        before: str | None = None,
        after: str | None = None,
    ) -> str:
        """GET /users/{username}/projectsV2/{project_number}/fields - List project fields for user"""
        # HTTP GET /users/{username}/projectsV2/{project_number}/fields
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2/{project_number}/fields",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_get_field_for_user(
        project_number: int,
        field_id: int,
        username: str,
    ) -> str:
        """GET /users/{username}/projectsV2/{project_number}/fields/{field_id} - Get project field for user"""
        # HTTP GET /users/{username}/projectsV2/{project_number}/fields/{field_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["field_id"] = str(field_id)
        path_params["username"] = str(username)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2/{project_number}/fields/{field_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_add_item_for_user(
        username: str,
        project_number: int,
        type: str,
        id: int | None = None,
        owner: str | None = None,
        repo: str | None = None,
        number: int | None = None,
    ) -> str:
        """POST /users/{username}/projectsV2/{project_number}/items - Add item to user owned project"""
        # HTTP POST /users/{username}/projectsV2/{project_number}/items
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        path_params["project_number"] = str(project_number)
        if type is not None:
            query_params["type"] = str(type)
        if id is not None:
            query_params["id"] = str(id)
        if owner is not None:
            query_params["owner"] = str(owner)
        if repo is not None:
            query_params["repo"] = str(repo)
        if number is not None:
            query_params["number"] = str(number)
        try:
            return await backend.request(
                "POST",
                "/users/{username}/projectsV2/{project_number}/items",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_items_for_user(
        project_number: int,
        username: str,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
        q: str | None = None,
        fields: dict | None = None,
    ) -> str:
        """GET /users/{username}/projectsV2/{project_number}/items - List items for a user owned project"""
        # HTTP GET /users/{username}/projectsV2/{project_number}/items
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if q is not None:
            query_params["q"] = str(q)
        body = fields if isinstance(fields, dict) else {"data": fields}
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2/{project_number}/items",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_update_item_for_user(
        project_number: int,
        username: str,
        item_id: int,
        fields: list,
    ) -> str:
        """PATCH /users/{username}/projectsV2/{project_number}/items/{item_id} - Update project item for user"""
        # HTTP PATCH /users/{username}/projectsV2/{project_number}/items/{item_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        path_params["item_id"] = str(item_id)
        if fields is not None:
            query_params["fields"] = str(fields)
        try:
            return await backend.request(
                "PATCH",
                "/users/{username}/projectsV2/{project_number}/items/{item_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_delete_item_for_user(
        project_number: int,
        username: str,
        item_id: int,
    ) -> str:
        """DELETE /users/{username}/projectsV2/{project_number}/items/{item_id} - Delete project item for user"""
        # HTTP DELETE /users/{username}/projectsV2/{project_number}/items/{item_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        path_params["item_id"] = str(item_id)
        try:
            return await backend.request(
                "DELETE",
                "/users/{username}/projectsV2/{project_number}/items/{item_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_get_user_item(
        project_number: int,
        username: str,
        item_id: int,
        fields: dict | None = None,
    ) -> str:
        """GET /users/{username}/projectsV2/{project_number}/items/{item_id} - Get an item for a user owned project"""
        # HTTP GET /users/{username}/projectsV2/{project_number}/items/{item_id}
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        path_params["item_id"] = str(item_id)
        body = fields if isinstance(fields, dict) else {"data": fields}
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2/{project_number}/items/{item_id}",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def projects_list_view_items_for_user(
        project_number: int,
        username: str,
        view_number: int,
        fields: dict | None = None,
        before: str | None = None,
        after: str | None = None,
        per_page: int | None = None,
    ) -> str:
        """GET /users/{username}/projectsV2/{project_number}/views/{view_number}/items - List items for a user project view"""
        # HTTP GET /users/{username}/projectsV2/{project_number}/views/{view_number}/items
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["project_number"] = str(project_number)
        path_params["username"] = str(username)
        path_params["view_number"] = str(view_number)
        body = fields if isinstance(fields, dict) else {"data": fields}
        if before is not None:
            query_params["before"] = str(before)
        if after is not None:
            query_params["after"] = str(after)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/projectsV2/{project_number}/views/{view_number}/items",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_received_events_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/received_events - List events received by the authenticated user"""
        # HTTP GET /users/{username}/received_events
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/received_events",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_received_public_events_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/received_events/public - List public events received by a user"""
        # HTTP GET /users/{username}/received_events/public
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/received_events/public",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def repos_list_for_user(
        username: str,
        type: str | None = None,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/repos - List repositories for a user"""
        # HTTP GET /users/{username}/repos
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if type is not None:
            query_params["type"] = str(type)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/repos",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_users_by_username_settings_billing_premium_request_usage(
        username: str,
        year: int | None = None,
        month: int | None = None,
        day: int | None = None,
        model: str | None = None,
        product: str | None = None,
    ) -> str:
        """GET /users/{username}/settings/billing/premium_request/usage - Get billing premium request usage report for a user"""
        # HTTP GET /users/{username}/settings/billing/premium_request/usage
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if year is not None:
            query_params["year"] = str(year)
        if month is not None:
            query_params["month"] = str(month)
        if day is not None:
            query_params["day"] = str(day)
        if model is not None:
            query_params["model"] = str(model)
        if product is not None:
            query_params["product"] = str(product)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/settings/billing/premium_request/usage",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def billing_get_github_billing_usage_report_user(
        username: str,
        year: int | None = None,
        month: int | None = None,
        day: int | None = None,
    ) -> str:
        """GET /users/{username}/settings/billing/usage - Get billing usage report for a user"""
        # HTTP GET /users/{username}/settings/billing/usage
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if year is not None:
            query_params["year"] = str(year)
        if month is not None:
            query_params["month"] = str(month)
        if day is not None:
            query_params["day"] = str(day)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/settings/billing/usage",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def get_users_by_username_settings_billing_usage_summary(
        username: str,
        year: int | None = None,
        month: int | None = None,
        day: int | None = None,
        repository: str | None = None,
        product: str | None = None,
        sku: str | None = None,
    ) -> str:
        """GET /users/{username}/settings/billing/usage/summary - Get billing usage summary for a user"""
        # HTTP GET /users/{username}/settings/billing/usage/summary
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if year is not None:
            query_params["year"] = str(year)
        if month is not None:
            query_params["month"] = str(month)
        if day is not None:
            query_params["day"] = str(day)
        if repository is not None:
            query_params["repository"] = str(repository)
        if product is not None:
            query_params["product"] = str(product)
        if sku is not None:
            query_params["sku"] = str(sku)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/settings/billing/usage/summary",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_social_accounts_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/social_accounts - List social accounts for a user"""
        # HTTP GET /users/{username}/social_accounts
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/social_accounts",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def users_list_ssh_signing_keys_for_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/ssh_signing_keys - List SSH signing keys for a user"""
        # HTTP GET /users/{username}/ssh_signing_keys
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/ssh_signing_keys",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_repos_starred_by_user(
        username: str,
        sort: str | None = None,
        direction: str | None = None,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/starred - List repositories starred by a user"""
        # HTTP GET /users/{username}/starred
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if sort is not None:
            query_params["sort"] = str(sort)
        if direction is not None:
            query_params["direction"] = str(direction)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/starred",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def activity_list_repos_watched_by_user(
        username: str,
        per_page: int | None = None,
        page: int | None = None,
    ) -> str:
        """GET /users/{username}/subscriptions - List repositories watched by a user"""
        # HTTP GET /users/{username}/subscriptions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        path_params["username"] = str(username)
        if per_page is not None:
            query_params["per_page"] = str(per_page)
        if page is not None:
            query_params["page"] = str(page)
        try:
            return await backend.request(
                "GET",
                "/users/{username}/subscriptions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def meta_get_all_versions(
    ) -> str:
        """GET /versions - Get all API versions"""
        # HTTP GET /versions
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/versions",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

    @server.tool()
    async def meta_get_zen(
    ) -> str:
        """GET /zen - Get the Zen of GitHub"""
        # HTTP GET /zen
        path_params: dict[str, str] = {}
        query_params: dict[str, str] = {}
        body: dict | None = None
        try:
            return await backend.request(
                "GET",
                "/zen",
                params=query_params or None,
                body=body,
                path_params=path_params or None,
            )
        except BackendError as exc:
            return json.dumps(exc.to_dict(), indent=2)

