# MCP Server for {{ design.server_name }}

{{ design.server_description }}

Generated by [mcp-anything](https://github.com/mcp-anything/mcp-anything).

## Installation

```bash
pip install -e .
```
{% if design.target_install_hint %}

### Target application dependencies

This server calls into the target application. Install its dependencies:

```bash
{{ design.target_install_hint }}
```
{% endif %}

## Usage

### As an MCP server (stdio transport)

```bash
mcp-{{ design.server_name | kebab_case }}
```

### Claude Desktop configuration

Add to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{ design.server_name }}": {
      "command": "mcp-{{ design.server_name | kebab_case }}"
    }
  }
}
```

## Available Tools

{% for tool in design.tools %}
### `{{ tool.name }}`

{{ tool.description }}

{% if tool.parameters %}
**Parameters:**
{% for param in tool.parameters %}
- `{{ param.name }}` ({{ param.type }}){% if not param.required %} — optional{% endif %}: {{ param.description }}
{% endfor %}
{% endif %}

{% endfor %}

{% if design.resources %}
## Available Resources

{% for resource in design.resources %}
- `{{ resource.uri }}` — {{ resource.description }}
{% endfor %}
{% endif %}
