"""Test configuration for {{ design.server_name }} MCP server."""

import pytest


@pytest.fixture
def server_module():
    """Import the generated server module."""
    from {{ package_name }} import server as server_module
    return server_module


@pytest.fixture
def server():
    """Create a test server instance."""
    from {{ package_name }}.server import server
    return server


@pytest.fixture
def backend(server_module):
    """Expose the generated backend instance when present."""
    return getattr(server_module, "_backend", None)
